[package]
name = "cli-manager-hook-schema"
version = "0.1.0"
edition = "2021"
license = "AGPL-3.0-or-later"
description = "Shared CLI Hook schema and configuration planning for CLI-Manager"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
toml_edit = "0.25.4"
