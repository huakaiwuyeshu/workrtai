# CLI-Manager 功能清单

> 更新时间：2026-09-08（覆盖至 V1.4.0 + TEMP）
> 参考来源：`README.md`、`CHANGELOG.md`、当前前端与 Tauri/Rust 后端源码只读梳理。

## [V1.4.0] - 2026-09-08

### 项目栏已置顶快捷入口

- 项目侧边栏新增默认展开的“已置顶”文件夹式快捷分组，仅在存在置顶项目时显示；项目可通过行内 Pin 操作或右键菜单置顶/取消置顶，置顶按项目稳定 ID保存，项目改名、移动分组或切换配置后仍指向原项目，删除项目会清理无效记录。
- 项目筛选新增“已置顶”，可只查看置顶项目；普通“全部 / 已开启”视图在非搜索状态下保留置顶区域，搜索激活时隐藏重复区域。置顶项目沿用普通项目的选中、启动、基础终端操作、Worktree、WSL/SSH、外部终端和批量启动行为，置顶列表不参与树拖拽排序。
- 展开/收起侧边栏、紧凑密度和键盘操作均保留置顶入口；项目筛选入口继续遵循设置开关且默认隐藏，置顶按钮位于启动按钮左侧，分组折叠状态、Pin tooltip、aria-label及中英文文案同步生效。

### Git 工作区布局与入口

- 大型仓库 Git 变更面板支持数万文件的完整虚拟列表：按视口挂载行，大列表后台建树并提供分批降级；目录批量暂存/未跟踪选择覆盖全部后代，保留分组、折叠、右键及文件/目录拖拽。
- Git 状态刷新按仓库合并在途请求，写操作后等待新快照；相同快照复用已有树，项目/子仓库切换与关闭面板隔离过期结果（V1.4.0，#257）。
- Git 提交历史三栏工作区取消窄面板下的固定最小宽度和外层横向滚动，保留提交列表独立滚动，并统一终端面板滚动条主题样式。
- Git 入口统一由终端操作栏打开，移除项目侧栏底部重复的 Git 按钮；变更侧栏与底部历史工作区通过 Tab 切换。
- Git 工作区引用栏、提交列表和提交详情之间的分隔线支持拖动自定义宽度，工作区顶部边界支持拖动调整高度。
- Git Tab 双向切换独立组件：侧栏“历史”打开底部自适应三栏提交历史并关闭 Git 侧栏；底部“变更”关闭历史工作区并恢复原版侧栏文件树与提交操作，兼容合并/独立辅助面板，Git 入口高亮跟随任一视图。

### Worktree 完成与强制合并

- 主工作区有未提交改动时，普通 Worktree 合并继续阻断；完成对话框仅在该错误状态下提供强制保存并合并入口，并要求二次确认，确认框不响应外部点击或 Escape 关闭。
- 强制流程由 Rust 在 Git 边界保存 staged、unstaged、untracked 改动，合并后使用本次 stash OID 恢复并保留 stash；已有 stash 不会被误用或删除。
- stash 保存/恢复失败、merge 冲突和恢复冲突均展示底层输出与下一步指引，任何未确认恢复成功的状态都保留 Worktree，不自动清理目录或分支；中英文文案和无障碍标签同步生效。

### Codex Goal Hook 状态与通知

- 兼容旧 Hook 二进制缺少 goal 字段的上报：本机 bridge 在所有通知出口之前按会话 ID 只读补全已存在 Goal 的状态，避免完成通知遗漏；远程事件与数据库归属不确定时不推测完成。

- Codex `/goal` 的每个阶段性 `Stop` Hook 会先按 `session_id` 只读核对 Codex `thread_goals`，当前布局优先读取 `goals_1.sqlite`，旧版本回退 `state_5.sqlite`；数据库不可读、缺失或字段异常时保持安全的非完成态。
- goal active 的中间回合保持 Tab 运行中并抑制完成型 Toast、任务栏和系统/第三方通知；complete 才进入绿色完成，重复与乱序终态按 goal/session 有界去重并防止回退。
- paused/blocked 显示关注态，budget/usage limit 显示失败态；WSL 使用发行版内 SQLite online backup 快照，SSH/远端 spool 保留可信状态，普通 Codex turn 和其他 CLI Hook 行为不变。

## [TEMP] - 2026-09-08

### Git 工作区工具栏质感

- Git 日志工具栏的仓库下拉框与 Fetch、拉取、推送操作使用统一的紧凑控件规格，并通过低饱和纯色表面、柔和语义色描边、轻微内高光和悬停/按下反馈增强层次；仓库选择使用 200px 紧凑宽度及终端面板配色一致的应用内展开层，支持选中态、方向键、Enter/Escape、焦点管理及外部点击关闭，原有仓库切换逻辑和禁用条件保持不变。

### 后端可靠性与安全边界加固

- Provider 文档完整遍历 JSON/TOML 数组、inline table 与 table array 的敏感字段，保存时保留既有密钥并拒绝新增明文；crash context、Hook、WSL、HTTP 与建议服务错误在记录或返回前脱敏和限长。
- Provider 全局切换在全部 Home 验证后才提交并清理 journal/备份，补偿回滚不再被自身 pending journal 阻塞；数据库备份拒绝 WAL busy、验证恢复指纹并使用独占文件名。
- daemon、Codex proxy、Capability、Hook、外部命令和 WSL 输入建立有界读取、输出、待处理集合与完整阶段 deadline；SSE 支持 CRLF 和跨 chunk UTF-8，Origin 使用结构化 URI 精确校验。
- 终端背景图只操作受控根下允许扩展的常规文件，不跟随 symlink 清理；同步快照拒绝覆盖已有 ZIP，文件同路径 copy/move 在删除前失败关闭，OpenCode 投递失败可安全重试。
- 注释扫描观察项维护可审计处置台账；依赖公开协议、跨存储事务或真实 Unix/WSL/SSH 环境的项目保留后续任务。桌宠 ID 与卸载路径问题不属于本次实现范围。

### 实时统计一键截图

- 实时统计标题栏新增与刷新按钮同风格的截图图标，一键将当前启用卡片的完整滚动区域生成长图并复制到系统图片剪贴板。
- 长图保留当前主题、中英文、卡片顺序、Canvas/SVG 图表；截取时不改变面板滚动位置，不包含截图/刷新按钮及弹出对话框。
- 增加生成中、成功及失败提示，阻止重复点击；超长图片等比缩放并限制像素预算，异常时释放离屏副本和原生图片资源。
- 普通截图至少使用 2 倍像素密度，高分屏最高跟随至 3 倍；极端长图仍在尺寸与总像素预算内等比缩放，兼顾完整内容和文字、图表清晰度。

### 多 Agent 工具记录、MCP 健康与 WSL 恢复

- 统一原生历史工具事件的 MCP 分类、调用去重和结果状态，修复 MCP 在历史索引读回后被当作内置工具的问题；已有派生索引按解析器版本重新生成。
- 补全 Codex custom_tool_call_output 和编排脚本内的静态调用点；推断调用单独标记、单独计数，不计入实际调用总数或 MCP 健康证据。
- 推断工具统计不再显示额外嵌套调用点计数提示，“嵌套工具”和“MCP”分组标题保持简洁，不附加“推断”后缀。
- 五类 Agent 能力诊断仅使用当前绑定会话的明确结果，探测未提供健康信息时保留已有证据，并解释“健康未知”。
- 六类已有恢复命令统一支持 WSL 发行版、Linux/UNC 路径、带空格目录与 worktree 匹配；Linux cwd 通过 wsl.exe --cd 传递，启动前验证目录，避免 Win32 267。未支持恢复的来源禁用入口。

### 前端功能模块组织

- 前端按 14 个功能域组织，App 负责组合，共享 UI/平台适配/全局偏好/翻译与数据契约独立定位；跨功能 API 采用单一职责源模块，不通过大聚合入口连带加载 UI 和状态。旧 components/hooks/stores/lib/terminal 实现目录已迁移。
- 文件编辑器控制、Markdown 跳转和视图可分别定位；源代码/预览跳转继续复用同一文件 Store 和请求序号，组件接口、未保存确认、固定 Diff 租约与界面行为保持兼容。
- 终端状态通过独立 `features/terminal/state.ts` 入口访问，避免状态导入同时加载终端 UI；保留唯一 Store，Hook/子代理动作工厂共用原始 set/get/API，计时器、序号和串行保存队列不重复创建。
- 终端标签、Workspan、分屏、悬浮卡、拖拽与工具栏按职责拆分，保留组件接口和懒加载边界；完整视图、回调函数体及依赖数组逐项校验。
- XTerm 终端视图与控制 Hook 分开定位，链接/附件和诊断辅助逻辑独立；PTY 订阅、恢复、快照和释放仍由同一控制 Hook 顺序执行。
- Hook 设置、历史 Store 和项目侧栏按职责迁入 `features/settings`、`features/history`、`features/projects`；历史请求序号/标题队列与唯一 Store 保持同一所有者，侧栏状态与菜单视图可分别定位，未改变界面或业务行为。

### AI 开发与维护工具

- 后端方法注释持续完善：SSH Agent 发布供应链/启动计划/传输/代理/AskPass 与辅助入口、构建脚本、桌面/SSH Agent 入口、Agent 文件操作/附件上传/历史索引/检索/恢复预检/bridge 协议/安装/回滚/卸载/版本/布局/能力诊断/Git 主模块/Diff/提交历史/工作区工具/Hook 配置主模块、JSON 辅助与配置事务测试/Hook 运行时与事件落盘、历史解析/索引/编辑/备份/智能标题/用量统计、状态栏/配置档案、桌宠/能力诊断/数据库修复、本地/WSL/SSH Git 与文件操作/Live Server/Worktree、桌面远程 SSH/cc-connect/接管/更新与回归测试、共享能力诊断/迁移注册与回归夹具、共享文本编码、Hook/Kimi 配置、远程历史解析、进程/Shell/WSL、daemon 客户端/服务端/HTTP 路由/SSH 桥接/PTY 管理、桌面 Hook 配置/监听/仲裁、项目分组路径继承/事务删除、系统通知/任务栏提醒/声音校验、Codex 接管代理/协议转发/恢复校验、子代理转录读取/路径解析/发现/订阅、Shell 扫描/原生图标/外部终端/系统路径打开/终端背景图片/PTY 命令入口/启动准备/边界/颜色/平台实现、daemon 服务端测试/路由监听/启动/进程治理/熔断/发现/协议/传输/回放/事件、同步/备份服务、IPC 与数据库恢复、通知适配器/分发/发送入口/HTTP/事件模型、CC Switch 数据库桥接、供应商全局应用/预览缓存/热切换/日志恢复/导入/旧引用迁移/问题处理/路由服务/代理设置/作用域/启动快照/旧历史恢复/路由命令/服务协调/接管编排/导入测试/Home 选择/缓存/全局配置回归测试/配置生成/本机/WSL 配置文件访问/IPC 入口/仓储辅助/目录管理/引用检查/配置文档处理/仓储回归测试/密钥管理/故障转移队列/公共配置/运行时配置/历史迁移测试/数据库初始化/迁移/备份/Grok 配置投影/环境诊断/模型查询、共享网络客户端、辅助文本请求、命令建议/路径补全、WebDAV 客户端、文件监听、图形策略、数据目录/迁移、系统凭据、日志/崩溃收集、运行诊断及系统功能模块已补充 5930 个函数/方法的中文说明，覆盖私有辅助及测试；解释数据处理、配置所有权、失败边界和持久化/进程副作用，不改变功能。
- 后端资源与构建、发布、安装、跨进程测试脚本也按 callable 清单补充中文说明；JS/TS、Shell、PowerShell 和 Rust 内嵌程序分别使用 AST、语法解析及宿主字符串等价证据验证，注释规范已纳入后端 code-spec，后续 AI 任务可按相同低 Token 流程增量验收。
- 所有手写代码通过 2000 物理行和单行 500 字符的严格检查；压缩 JSX 展开后核对编译语义，Rust SQL/刷新脚本拼接后保持原始字节，不增加运行时依赖。
- 历史与 cc-connect 服务拆成可独立定位的职责模块，测试共享夹具并按领域分组；Unix Codex 包装脚本文本展开后保持输出内容不变。
- Rust 功能与基础设施实现完成归属整理，lib/commands 用显式模块路径保留原 IPC、可见性和 helper/daemon 命名空间；历史、Git、供应商、Hook 与 PTY 等实现及测试可直接定位，编译期资源仍指向原文件。
- 独立架构检查覆盖手写源代码、脚本和测试；限制文件 2000 物理行、单行 500 字符及反向依赖。Rust 分组导入和命名空间入口会解析到物理层，临时基线已清空，禁止新增豁免。
- 架构报告按大文件输出长度、字节、粗略 Token 估算和导入数，辅助 AI 按领域读取；开发启动与生产构建不增加该检查的额外开销。
- 翻译维护入口为 `src/shared/i18n/messages/<domain>.<locale>.ts`，14 个领域分别维护中英字典，既有翻译函数与繁体转换保持不变；组件 CSS 使用 16 个有序样式模块，主题、终端背景与历史 Markdown 规则保持原序。

### Git 工作区 PR #252 合并修复

- Git 提交历史、三栏工作区与增强工具完整接入当前终端底部 Dock；本地、WSL、SSH 共用 Transport，终端、分屏、Workspan、工作区背景和 SFTP 文件能力保持挂载与兼容。
- SSH Agent `0.1.14` / protocol `1.15` 同时保留 1.14 的 `fileGet`/`fileDelete`，并通过 `gitHistory`、`gitWorkspaceTools` 对历史浏览与增强操作分别做发送前能力检查。
- 用量数据库启动自愈覆盖必需表、索引、最终视图列和 migration marker；Git rewrite 恢复引用避免秒级重名；引用树和增强工具弹窗支持无嵌套交互元素、键盘焦点可见、Escape 关闭与焦点回归。

### 终端 WSL AI CLI 图片粘贴

- 内置终端支持 `Alt+V` 从 Windows 剪贴板粘贴图片；截图位图和资源管理器复制的图片文件统一转为应用附件目录内的 PNG，再传给 WSL 中的 AI CLI，避免依赖 WSL 内的剪贴板工具或不完整的 PowerShell PATH。
- 支持 PNG/APNG、JPEG/JPG/JFIF、GIF、WebP、BMP/DIB、TIFF、ICO 等常用桌面格式；HEIC/HEIF 明确不支持，SVG/AVIF 按当前安全解码能力失败关闭；输入文件、像素、符号链接与附件数量均受 Rust 后端限制。
- 图片输入按工具能力分级：Claude/Codex 原生路径、Gemini/Qwen/OpenCode/Kimi/Crush/Pi 使用 `@` 文件引用、Aider 使用 `/add`；WSL 路径自动转为 `/mnt` 形式，不支持的工具不会发送二进制或宿主机原始路径。

- Web 网络与菜单（1.3.9）：支持显式受信任局域网/虚拟组网 HTTP、独立浏览器域名和 HTTPS 反向代理、本机回环设备入口；HTTP 环境使用安全随机数生成请求 ID，菜单提供浏览器内操作及错误反馈。工作区快照采用提前获取写锁的事务处理并发发布。

- Web 终端同步（1.3.9）：通过工作区快照发现桌面已有终端、恢复多标签并同步移除已关闭窗口；原有会话直接连接，重复打开定位现有标签。标签复用桌面 CLI 图标、圆角样式；桌面控制列数时网页独立缩放显示。

> 更新时间：2026-09-10（覆盖至 V1.3.10 + TEMP）
> 参考来源：`README.md`、`CHANGELOG.md`、当前前端与 Tauri/Rust 后端源码只读梳理。

- Web 终端：网页端支持同时保留多个真实终端标签，启动第二个项目不会断开第一个终端；标签按项目名切换并同步项目上下文，可分别关闭且关闭操作同步结束桌面端 PTY。连接未就绪或设备重连时会自动补发关闭命令，已失效会话按幂等关闭处理；当前标签高频渲染、后台标签低频有界处理输出，各会话独立保存缓冲与渲染游标；页面切换与断线仍使用 detach。


## [V1.3.10]

- Web 手机标签切换：Safari 切回隐藏终端时完整重绘 xterm 画布，避免缓冲区有内容但画布空白；项目抽屉默认收起目录层级，仍可逐级展开并启动项目或 Worktree。
- Web 真机调试：开发代理可配置后端浏览器 Origin，在不替换正式 Web 服务的情况下通过其他网卡地址热更新验证手机界面。

- Web 切换恢复：可见状态和桌面/Web 控制权变化时清理尺寸缓存，隐藏标签切回重新适配，保留双向滚动和字号设置。
- Web 图片粘贴：等待保存操作完成，使用原会话 xterm 的标准粘贴模式发送图片路径，不自动回车；保存失败、目标失效和旧桌面结果不兼容时明确失败。

- Web 手机手动字号：超出显示区域可上下左右移动；字号调整保持底部，滑到底部按钮同时处理终端历史与外层溢出，键盘缩短视口后仍能到达输入行。
- Web 图片：手机按钮直接触发系统文件选择，沿用原生相册入口；图片解码/压缩/提交提供中英文反馈，支持空 MIME 图片的解码尝试和有界压缩，失败不再静默。
- Web 导航：返回设备列表保留会话，同一设备重新进入保留全部标签及当前选择；主动关闭仍从标签 × 发出，保持桌面同步关闭。

- Web 输入与桌面共用 Hook 提醒处理；安装/卸载通过原生位数 PowerShell 核实进程路径，清理失败时停止卸载。

- Web 手机终端新增图片上传；桌面 Web 支持从剪贴板粘贴图片，收起操作栏时同步隐藏底部导航。
- Web 设备列表过滤历史回放中的过期设备更新，安装版/开发版显示使用规范徽标。


- Web 手机空间优化：终端区域不再重复显示项目名和工作目录；辅助操作栏可收起为右下角悬浮按钮，状态保存在当前浏览器，展开/收起后终端自动适配可用高度。

- Web 手机按键：方向按钮展开浮层十字方向盘，Enter 固定最右侧；支持外部点击收起、连续方向输入与中英文，其他辅助键横向滚动，不增加终端布局高度。

- Web 手机项目入口：顶部文件夹按钮按需展开项目/Worktree 抽屉，复用现有项目树和启动接口；关闭全部终端后仍可选择项目重新启动。手机树禁用拖拽以便滚动、显示快捷启动按钮，断线时禁用启动；电脑 Web 保持常驻侧栏。

- Web 手机布局隐藏完整终端状态卡，仅保留紧凑的当前项目/工作目录提示；系统键盘辅助栏补充 ←/↑/↓/→，发送标准 ANSI 方向键序列。

- Web 终端状态卡显示当前标签对应的项目名称与完整工作目录，手机长路径自动换行并支持选中复制；路径只对已鉴权且具备设备访问权的浏览器返回。

- Web 手机终端：主终端和子代理宽屏左右 50/50、窄屏上下 50/50并可收起；适配可见视口和软键盘，提供主动打开键盘、备用文本输入、Enter/Tab/Esc/Ctrl+C，手机不自动抢焦点弹键盘。无法获取几何信息的浮动键盘不保证自动识别。

### Web 终端与子任务

- 电脑 Web 左侧目录树常驻显示，内部文件夹分组与 Worktree 节点默认折叠，点击原有箭头展开；不新增电脑端目录按钮。

- Web 热部署：严格模式重新挂载不再丢失组件挂载前收到的终端回放，避免静止会话显示空白；不改变 PTY 尺寸及输入协议。

- Web 独立显示设置（1.3.10）：终端右上角支持 8–36px 手动字号、Ctrl+滚轮、适合宽度/完整显示；显示区域宽高可通过滑块调整为30–100%，超出时滚动，一键恢复默认。浏览器本地记忆并同步多个标签；不修改桌面设置，显示参数不触发共享 PTY resize。
- 桌面镜像终端按宽高完整适配并限制默认字号不放大，子代理分屏导致主终端变窄时仍保持正常字体；使用实际字号布局保证鼠标选字与点击坐标准确，Web 接管时正常 fit。
- Web 不参与终端自动查询应答；桌面按会话及输出序号避免重复历史回复，同时保留新进程首次回放中的启动握手。
- 子代理与主终端分别展示：沿用桌面真实转录及父会话关系，多子任务切换、Markdown、运行/结束/空态、展开收起和窄屏适配；持续更新有界节流，快照截断明确提示，重连及移除同步。

## [V1.3.9]

### Web 管理 P1

- Web 终端渲染与同步（1.3.9）：可见桌面视口持有 PTY 尺寸，隐藏后允许 Web 接管，恢复桌面时重新提交本地尺寸；网页镜像按可用宽度缩放且保留固定滚动条宽度，历史回放遵循各帧网格变化。
- Web 输出与恢复（1.3.9）：发送采用事件唤醒和持久鉴权连接，多会话公平调度；Web 设备设置提供 96/256/512 KiB 最大批次（默认 96），仅为编码批次上限、不等满批，不随备份同步。分片起止标记保证原帧收齐才绘制，拥塞有限重试并明确报错；浏览器重连后可恢复状态。
- Web 光标（1.3.9）：关闭光标定时闪烁，Codex 的显示光标控制序列延迟确认并能被后续隐藏取消，支持跨分片解析；非活动标签不主动聚焦。
- 安装生命周期（1.3.9）：覆盖安装和卸载前通过 NSIS 清理当前安装目录的主程序、Web/PTY daemon、Codex proxy、ConPTY OpenConsole；先正常关闭，超时核验 PID/启动时间/路径后终止，保留用户数据，其他安装目录不受影响。
- ConPTY 安装资源（1.3.9）：保留 x64/x86/ARM64 架构目录，避免 DLL/EXE 同名覆盖，并与桌面兼容组件的加载路径一致；清理同时识别架构目录和旧版扁平目录中的 OpenConsole。

- 管理工作台在已选择的项目或 Worktree 上下文中支持文件列表、文件名/内容搜索、文本和图片预览；浏览器只提交项目/Worktree ID 与相对路径，文件内容由桌面端读取并受传输大小限制。
- Git 管理支持查看指定变更文件的 Unified Diff，可选择空白处理和上下文行数；状态、分支、暂存、提交、拉取和推送操作继续使用服务端幂等与桌面原生确认。
- 独立 Web 服务支持 `--bind` / `--port`、`CLI_MANAGER_WEB_BIND` / `CLI_MANAGER_WEB_PORT` 和数据目录 `web-server.toml` 的动态监听配置，开发启动脚本与 Vite 代理自动跟随后端端口；非回环监听要求 Secure Cookie。
- 设置页支持 Web 服务手动启动、停止、重启和自动启动开关；可监听回环、本机局域网/虚拟网卡 IP 或全部网卡，校验本机地址并配置精确 Origin，运行中保存配置会重启且失败自动回滚；密码仅保存到系统凭据存储。

### Web 管理 P0 基线

- Web 工作台终端支持右上角关闭按钮，关闭网页观察连接并保留桌面 PTY；桌面终端启动后补充二帧布局和 viewport 重绘，改善首屏灰块/未绘制区域。
- 桌面 Web 设备命令在阻塞任务池执行，避免失效后台端口拖住全部界面按钮；回环连接有 200ms 期限与 3 秒失败冷却，断开/未配对暂停高频命令轮询，恢复后重新发布工作区。启动识别 PID 复用，不误停其他应用、不删除用户配置。
- Web 终端输出使用独立订阅流与批量 xterm 写入；attach 支持可选渲染游标。隔离浏览器回放与突发输出测试已通过，真实网络断线、持续输出及桌面启动卡顿仍待现场验收。

- 安装版 Windows 网络 socket 禁止辅助进程继承，避免停服后端口仍占用；启停与配置应用串行，停止超时保留管理句柄和“正在停止”状态。设置页定期刷新实际状态但保留未保存输入。
- 大终端回放按原始字节分片并限制编码批次大小，保留 ANSI、尺寸、reset/replay 和完整原帧 ACK；旧挂接队列失效，xterm 卸载取消待完成写入。输出拒收不再断开设备，界面明确显示离线或终端错误，旧 daemon 自动按协议 6 的兼容流程升级。

- Web 断线恢复：历史失效通知合并刷新、旧回放通知不重复请求；浏览器握手/心跳和设备连接均有超时，停止会取消旧连接，项目/会话切换不会拆建浏览器通道。内嵌服务显式关闭升级后的 WebSocket，支持同端口重启；旧 daemon 使用受限检查/退出流程升级到本地协议 6。

- Web 工作台以真实 xterm 终端作为主界面：项目/Worktree 从侧栏选择并通过桌面既有启动流程创建 PTY，浏览器与桌面共享同一 PTY 的 ANSI 输出和键盘输入；Web 使用独立 observer 连接隔离 replay/ACK，桌面已挂载时独占 resize，未挂载时 Web 自动接管，结构化对话卡片不再参与主流程，实时终端字节不落 Web SQLite。
- 真实终端通道复用账号、浏览器会话和设备归属校验；移动端 scope 只能访问授权设备，终端命令和输出均有大小/尺寸边界，桌面端按 16ms 合并输出并使用有界命令队列。
- 切换项目、设备、返回主机和退出登录会释放当前 Web observer 连接但不关闭桌面 PTY；服务或浏览器重连后按当前 PTY ID 重新 attach，回放只进入 Web 连接，不会重置桌面 xterm。
- 内嵌 Web 服务在监听及存储初始化成功后才报告运行；启动时先绑定端口，端口冲突不会打开第二个数据库连接、修改管理员或把现有设备标记离线。
- Web 会话只有在 CLI 发出 `session_started`、建立真实 Session 后才进入会话列表并允许恢复；initialize 前失败只结束当前 operation，不会持久化临时 operation ID。升级前遗留的失败假会话自动隐藏，浏览器刷新历史时会清除对应的无效 Session 缓存。
- Web 历史恢复按 Session ID、来源和项目/Worktree 目录精确查询桌面历史，未命中时强制刷新历史索引后再次校验，不受界面仅加载前 20 条摘要的限制；项目侧栏是唯一项目上下文，失效会话不会覆盖当前项目选择。
- Web 结构化会话会剥离项目启动参数中的终端 `resume` 选择器，由已校验的会话上下文唯一决定新建或恢复；CLI 提前退出返回有界、非敏感诊断，不回传 stderr 原文。
- 内嵌 Web 服务停止/重启会回收旧服务线程、活跃连接和 SQLite pool 后再启动；会话事件与操作状态并发写入通过预留 SQLite writer 避免 `SQLITE_BUSY_SNAPSHOT`。
- Web 对话以项目侧栏作为唯一项目/Worktree 选择入口，顶部仅展示当前上下文；项目快捷启动、历史恢复和消息发送共享同一上下文状态。
- 项目级 Codex Provider profile 在桌面没有外部 `CODEX_HOME` 环境变量时仍从 Provider Home 管理的实际配置目录加载。
- 安装包显式包含主程序及 Codex proxy、PTY daemon、Web daemon 三个后台程序，避免自动发现二进制时漏装 Web 传输组件。
- Web 对话使用 Rust 托管的结构化 CLI 进程与协议就绪信号，不依赖终端 Hook；Codex 和 Claude 回复正文、增量与轮次终态同步到浏览器。
- 会话消息持久化、按序号去重和断线补齐；用户点选历史时按需同步正文，已有桌面终端占用的会话不能被 Web 同时写入。
- 设置页及 Web 主机页提供手机二维码入口，支持单次票据过期/刷新/停止；手机浏览器只授权指定电脑，账号用户可以列出和撤销已授权浏览器。
- 手机扫码要求可达 HTTPS 服务地址，内嵌服务仍只绑定本机；界面适配移动软键盘、安全区和后台连接恢复，中英文均支持。
- 新增浏览器端 Web 管理基础工程，支持登录、Cookie 会话、设备配对、在线/离线状态、心跳和断线重连。
- 桌面端通过 Web Device Bridge 与 web daemon 发布真实分组、项目、Worktree 工作区树；工作台支持启动新会话、恢复已有会话和发送提示词，并展示完整操作状态。
- 远程操作采用服务端幂等键、设备能力校验和桌面端原生确认；设备离线、拒绝、失败和超时均返回结构化状态。配对或重连后立即同步工作区，回执重试有界。
- Web 工作区和历史会话只下发项目/Worktree 不透明 ID，不暴露桌面绝对路径；浏览器操作由桌面端按登记项目解析真实工作目录。

### 会话历史内容排序与 Codex 标题

- 会话历史的对话、原文、过程、变更、工具、子任务视图支持按页签独立切换正序/倒序；倒序从最新消息/记录开始并按需向更早内容分页，排序偏好持久化后在重启、切换会话和重新打开历史时恢复。
- Codex 历史标题读取对应 `session_index.jsonl` 的 `thread_name`，覆盖 Windows 本地、WSL 与 SSH；手工别名和有效 AI 生成标题仍优先于该来源标题，索引缺失或失配时安全回退。
- 修复原文倒序时虚拟列表的消息高度测量索引错位，避免相邻消息内容发生重叠。

### Tauri 开发构建与缓存管理

- Windows 
pm run tauri dev` 会在启动 Tauri 前统一预构建 `cli-manager` 与 `cli-manager-codex-proxy`，两者共享 Cargo 的 feature、target、profile、target-dir 和 `TAURI_CONFIG` fingerprint 输入；源码未变化时由 Cargo fingerprint 复用已有产物，Codex 远程托管所需 proxy 仍保持自动可用。
- Rust 开发构建降低第三方依赖调试符号级别，保留主程序基本源码定位能力，减少 `target/debug` 的 PDB、对象文件和增量缓存体积；
pm run tauri:clean:dev` 可清理当前仓库 dev 产物，
pm run tauri:clean` 可清理全部 Cargo 产物。清理不会自动发生，也不会影响应用数据或其他 worktree。
- 清理前应关闭当前 Tauri/Cargo 进程；多个 worktree 或 clone 需要逐个确认绝对路径后单独清理，不使用宽泛路径批量删除。
- Vite 冷启动或依赖重新优化较慢时，开发服务探测会保留更长等待窗口，避免 Tauri 将尚未完成响应的 CLI-Manager 服务误判为端口冲突。

### 工作区背景铺满范围

- 终端背景设置新增“背景铺满工作区”开关；关闭时仅终端 Pane 显示背景图，开启时由主工作区唯一背景层覆盖侧边栏、Tab 区域和终端辅助面板。
- 工作区背景范围复用现有透明度、适配、位置、模糊与暗化配置，XTerm 不重复绘制图片；路径缺失或资源解析失败时回退为现有主题背景，开关默认关闭。（Refs #248）
- 修复铺满模式下侧栏树节点、辅助面板内容、面板 Tab 和浅色主题应用标题栏使用不透明背景导致共享背景无法连续透出的问题；设置页与会话历史页保持原有不透明主题 surface，进入这两个页面时应用标题栏也不显示背景，历史统计页继续支持工作区背景透出。

### 终端辅助面板左右停靠

- 终端辅助面板支持左/右停靠，合并面板与独立面板共用工作区布局设置；默认右侧，操作工具栏会随辅助面板停靠到同侧外缘。
- 左侧多面板按终端中心到外侧保持既有语义顺序，拖拽 sash 使用方向感知宽度公式并在边界反向时立即恢复；恢复默认只改变布局位置，不清除面板宽度。（Refs #248）
- 标题栏通过“…”布局菜单统一显示/隐藏项目侧栏和终端辅助面板，并可分别切换左右停靠；右侧项目栏的边框、折叠箭头和宽度拖拽会随停靠侧镜像，操作工具栏与辅助面板同侧并位于外缘，支持“操作栏 | 辅助面板 | 终端 | 项目侧栏”组合，隐藏区域不会销毁具体面板。菜单选中项使用主色文字、图标与勾选标记，不绘制块状选中背景。（Refs #248）

### 顶层 Workspan Tab 上下停靠

- 顶层 Workspan Tab 支持顶部/底部停靠，保留横向排序、拖拽、滚轮、重命名、关闭、溢出列表和菜单交互；默认顶部，切换不重建 Pane 或 PTY。
- 底部 Tab 栏与终端内容保持同一 flex 文档流，自动让终端获得剩余高度；底部溢出列表向上展开，列表和关闭确认浮层按视口边界避让，Pane 内 Session Tab 仍保持顶部。（Refs #248）
- 标题栏布局菜单可直接显示/隐藏 Workspan Tab 栏、切换上下位置和恢复默认；隐藏只移除 Tab 栏占位，不销毁 Workspan、Tab 顺序或 PTY 会话；当前没有可用 Workspan Tab 时相关菜单项禁用并提示原因。（Refs #248）

### 修复 Codex 后台终端输出停滞

- 修复窗口隐藏、最小化或 WebView 暂停时终端输出不再推进、界面提前显示结束而 Codex 任务仍在运行的问题；隐藏文档使用定时回退继续驱动 xterm，恢复可见时重新调度，写入完成后的 ACK 时序保持不变。
- daemon 对慢客户端按会话暂停实时投递，不再让客户端 ACK 反向阻塞 PTY 生产线程；未投递输出保存在既有 SessionBuffer/spool，降到低水位后按 sequence 有序补发，并继续受客户端队列和会话缓存上限约束。（Fixes #245）
- 补发不会把 checkpoint 快照当作 live 输出追加；检测到缓存截断造成的 sequence 缺口时关闭慢客户端，由既有重连 attach/replay_reset 恢复完整终端状态；spool 回读不占用全局客户端锁，避免单个大 backlog 阻塞其他会话。

### 侧边栏项目树

- 文件夹右键菜单支持批量修改本组及子文件夹项目 Shell，并设置新建项目使用的绑定路径；新建项目可在“继承父级 / 自定义路径”间切换。
- 继承模式会动态解析最近的上级绑定路径；拖拽移入文件夹继续继承，移到根层时固化为移动前的有效路径，拖拽约束仅阻止破坏继承节点连续性的排序。
- 侧边栏不绘制树形连接线；继承项目的链式标记位于项目图标左侧，继承文件夹的链式标记位于折叠箭头左侧，均不改变其他节点布局。
- 清空文件夹绑定路径（包括切换为“继承父级”）且存在后代继承节点时，保存前提示影响范围；确认后将后代固化为当前有效路径，取消则保持不变。
- 文件夹绑定路径复用终端路径存在性校验，校验失败时阻止保存。
- 当前所处文件夹未绑定路径时，“继承父级”选项保持禁用；点击后会提示先设置该文件夹的绑定路径。
- 修复文件夹编辑的 Shell 批量范围误包含无关项目、清除绑定漏处理当前文件夹直接项目，以及删除分组后继承项目保留失效路径的问题；路径级联与 Shell 更新在同一 SQLite 事务中完成。
- 分屏、Worktree 历史、侧边栏终端启动与路径健康检查统一使用项目的有效继承路径；移除误加入 npm 项目的 pnpm 锁文件。
- 备份与同步完整保存文件夹绑定路径和项目继承模式，同时兼容恢复旧格式备份。
- 兼容旧数据库或旧恢复数据中缺失 `bound_path` 的分组记录，避免侧边栏渲染和拖拽判定崩溃。
- 启动前自动修复旧数据库缺失的文件夹绑定路径与项目路径模式列；新建目录失败时显示本地化错误提示。
- 子级目录不再显示继承路径的链式图标，路径继承行为保持不变。

### Grok Build 终端换行快捷键

- 修复 Grok Build 会话按下 `Alt+Enter` 无法换行的问题；设置中的三种终端换行组合键在匹配时都会发送 Grok Build 所需的 `ESC + CR` 序列。
- 除会话、项目、终端标题和启动命令元数据外，还会识别当前终端实际提交的精确 `grok` 启动命令，并以当前可见 TUI 输入提示作为运行时兜底边界；普通 Shell 与 Claude 的既有输入行为保持不变，PTY/IPC 契约不变。
- Grok/Codex 使用其他终端换行设置时，原生 `Alt+Enter` 会放行给 xterm 发送 `ESC + CR`；普通 Shell 与其他 CLI 的未匹配组合键仍按原规则处理。

### 终端滚动位置快捷操作

- 终端存在可回看的历史输出且当前视口不在底部时，在对应终端 Tab 右下角显示底部跳转按钮；点击后回到最新输出位置并隐藏按钮，按钮与字号控件垂直排列。
- 滚动状态按终端实例独立维护，兼容 Tab 切换、分屏、多会话、持续输出与窗口 resize；按钮 tooltip 和 aria-label 支持中英文。
- 快捷键设置默认提供 `Ctrl+End` 跳转到底部，以及 `PageUp` / `PageDown` 按一页快速翻动；仅普通缓冲区拦截，alternate buffer 保留给全屏 TUI。

### SSH 终端 Host 级文件粘贴

- 任何带有效 SSH Host 会话且该 Host 已安装可用 Agent 的 SSH 终端，都可通过 Ctrl+V、右键粘贴或文件拖放上传图片和任意受限普通文件；以会话自身 Host/remotePath 为准，不再要求已登记 SSH 项目，文本粘贴及本地/WSL 行为不变。
- SSH Host 可独立配置远程上传目录；Host SFTP 面板直接使用该目录，空值使用 Agent 默认附件目录；终端粘贴则仍把该配置作为父目录，并仅在其下创建隔离的 `cli-manager-ssh-agent/attachments` 子目录。
- Agent `0.1.11` / protocol `1.12` 通过 `fileAttachCustomRoot` 协商自定义目录；旧 Agent 保留默认目录兼容，自定义目录缺少能力时不会回退或泄露本机路径。

### SSH Host 远程附件传输

- SSH Host 列表移除认证方式徽标，认证方式仍可在编辑页配置并用于连接测试和 OpenSSH 认证。
- 每个 Host 的上传按钮打开 SFTP 风格双栏远程文件面板：本地栏默认打开 Windows/macOS 桌面并展示文件列表，复用文件浏览器图标，支持点击进入子目录、返回上级、刷新、手动输入或选择本地目录，点击文件即可加入上传队列；远程栏展示当前目录下的文件与子目录，同样复用文件浏览器图标，支持进入子目录、返回上级、刷新和手动输入远程路径，左右列表固定高度并在内容超出时滚动，底部传输队列显示等待、上传中、完成和失败状态。
- Host 级上传仅依赖当前 Host 的 SSH 配置和已安装 Agent，不依赖登记 SSH 项目或活动终端；文件直接写入当前远程上传目录，不自动生成 UUID 子目录。终端粘贴仍使用 Agent 管理的隔离附件目录。
- Agent `0.1.12` / protocol `1.13` 通过 `filePut` 提供 Host 面板直传；旧 Agent 缺少该能力时显示升级提示，不回退为终端附件上传。
- SSH 项目文件浏览器新增 SFTP 按钮并复用 Host 附件面板；远程文件支持选择本地路径下载和确认后删除，目录仅允许删除空目录。Agent `0.1.13` / protocol `1.14` 新增 `fileGet` 分块下载与 `fileDelete` 能力，旧 Agent 显示升级提示。
- 远程目录新增文件夹选择器入口；可通过远程目录列表进入子目录、返回上级并选择当前目录作为上传目标，保留手动输入远程路径方式。
- 启动前自动修复旧数据库缺失的 `ssh_hosts.attachment_root` 列及对应 migration 登记，编辑 Host 时不再因 
o such column: attachment_root` 失败；编辑器错误提示固定在表单滚动区外。
- 修复 SSH Agent 发布测试的常量作用域错误，确保 `0.1.11` 发布构建可通过。
- 修复远端 SSH Agent 原地升级后 SFTP 删除仍复用旧 bridge 能力的问题；能力缺失时精确替换对应 bridge、重新握手并最多重试一次，旧 Agent 仍返回稳定的升级提示。

## [V1.3.8]

### 历史会话智能命名

- 设置 → 会话历史 → 历史会话智能命名支持一份仅本机保存的全局自定义 Prompt；非空内容作为所选 Native Provider 的系统指令，清空或恢复内置默认即回退默认指令。候选用户消息仍独立发送，不支持模板变量，不参与设置同步；Prompt 持久化成功后显示本地化提示，写入期间不会重复提交。
- 生成智能标题通过 Tauri 异步 command 等待 Provider 响应，数秒的请求期间应用保持可交互；点击后详情按钮立即显示加载状态并阻止重复生成，列表同步显示该会话的生成进度；IPC 请求/响应结构、自动队列、Provider 超时和标题落库语义不变。
- 正式版与 
pm run tauri dev` 可同时运行并共享同一份应用数据。智能命名的 SQLite 预留和结果落库会先串行获取写锁、最多等待 15 秒；遇到仍被其他任务占用的数据库会显示“本地历史数据库正忙”，不再提示检查 Provider、模型或网络。

## [V1.3.7]

### OpenCode 会话与终端兼容

- OpenCode Hook 按规范 `sessionID` 绑定根会话，并维护父子会话关系；子 Agent 的创建、忙闲、错误和删除事件不会覆盖主终端 Tab 的 Session ID，也不会把子 Agent 状态冒充主会话状态。
- OpenCode 历史会话支持“继续对话”，恢复命令只使用历史摘要中的真实 `ses_...` 会话 ID，生成 `opencode --session <id>`；数据库 locator 与重复恢复参数不会传给 CLI。
- OpenCode 历史会话支持安全删除：仅允许默认 OpenCode SQLite 数据库中的合法 `ses_...` locator，在一个事务中按 `part → message → session` 顺序删除目标会话关联记录；找不到目标、数据库锁定或 schema 不兼容时不会部分删除，成功后历史索引缓存失效。
- OpenCode TUI 使用独立剪贴板适配模块处理 Windows `Ctrl+C`、`Ctrl+V` 与 `Ctrl+Shift+V`；复制成功后同步清理 xterm 与输入选择状态，无选中文本时保留中断语义。该模块只在结构化识别为 OpenCode 的终端启用，不改变 Claude、Codex、Grok、CC/CX、Pi 的键盘和鼠标输入路径。

## [TEMP]

### 侧边栏底部同步状态

- 侧边栏底部同步状态文案固定单行显示，图标不再被压缩，宽度不足时以省略号截断而不换行。

### 会话历史

- 会话历史列表和详情页保留会话级、消息级批量选择入口；编辑或删除后会同步刷新历史目录，避免刷新页面后回退到旧内容。
- V2 历史目录读取增加源文件指纹校验，检测到外部或本地写入变化时自动回退实时解析，确保编辑/删除结果可见。
- 手动刷新历史索引会同步重载当前会话详情；若当前会话已被删除，则自动清除残留详情，避免继续显示旧记录。
- V2 详情编辑守卫统一使用当前源文件修改时间，避免正常编辑被误判为外部文件变化。
- 历史消息编辑与插入文本框提高默认和最大高度，便于长文本查看与修改。
- 历史消息编辑与插入文本框会按内容自动增高，并在超出视口后保留内部滚动。
### 终端预览面板独立主题

- 设置 → 终端 → 预览主题：可选「跟随终端」（默认）或从终端主题库挑一个预设，作用于 Markdown 预览、子代理转录、会话回放与终端内 Git diff 查看器（含面板底色、文字、边框、滚动条与代码块明暗）。终端本体、终端 Tab、统计/供应商/系统资源面板与 Statusline 预览不跟随该设置。
- 终端主题库与预览主题共用同一套预设网格（分组 + 搜索 + 色板卡片 + 当前徽标）；预览面板的主题解析、明暗判定与 CSS 变量作用域统一由共享解析器提供，切换即时生效，分屏一致。设置持久化并参与 WebDAV `preferences` 同步，未知预设 id 自动回退为跟随终端。

### 浅色终端 CLI 深色背景块擦除

- 浅色终端主题下的 Claude Code 与 Pi 会话，CLI 按自身深色主题绘制的黑色背景块（如发送命令后回显的用户消息行）会被自动擦除；判定按 xterm 单元格实际渲染色进行，支持 RGB 与 256 色/ANSI 调色板背景，只清除接近中性的深色背景，彩色徽标、diff 底色与浅色高亮保留。
- 生效条件为「浅色主题 + Claude/Pi 会话身份」，不依赖 TUI 横幅可见性；同一行深色单元格少于 4 个（TUI 块状光标）保留，整行反显仍按既有宽度规则清除，孤立反显单元格保留为软件光标。深色主题、Codex 与普通 Shell 的渲染行为不变。

### 终端 TUI 鼠标交互

- 全屏 Grok 等启用终端鼠标协议的 TUI 直接接收普通点击与拖动，不再要求按住 Alt；宿主文本选择遵循标准终端行为（Shift 拖选）。OSC 52 本机剪贴板写入、查询开关与 `Ctrl+Shift+C` 复制选区保持不变。（Refs #228, #212）

### cc-connect 微信授权与多平台配置

- 微信二维码授权使用独立的平台准备边界：微信 allowlist 为空或旧值无效时可以重新扫码；其他已启用但 allowlist 未完成的平台只在本次授权 Profile 中自动回到禁用状态，保留输入值和凭据，已配置完整的平台保持在线。普通保存和 cc-connect 启动继续对所有启用平台执行严格显式用户 ID 校验。
- 全新远程连接 Profile 不预启用空白 Telegram；平台下拉框只切换当前编辑对象，已启用平台摘要同时展示 Telegram、飞书、微信和企业微信的真实启用状态并可直接切换。保存仍统一校验全部已启用平台，但错误会明确指出具体平台及必填、通配符或格式原因；未启用草稿不阻断其他平台保存。
- Windows 本地 Codex 远程托管支持真实 `.cmd/.bat/.ps1` 启动器位于含空格、中文或 `\\?\` 扩展前缀的目录；CMD 脚本通过固定 `call` 边界安全启动，Provider 参数、app-server stdio 与退出码保持透传。探针的 GBK/OEM 本地化错误使用现有编码探测显示可读中文。
- 从历史会话保存到侧边栏的 Codex、Claude Code、Pi 与 OpenCode 项目可以直接远程托管；Agent 启动参数会剥离项目中持久化的旧会话选择项，托管会话只使用当前 `cliSessionId` 恢复，避免旧 `resume` / `--session` 参数与 cc-connect 注入的会话身份冲突。该行为与所选消息平台无关，对飞书、Telegram、微信和企业微信一致。
- 本地 Codex 托管 app-server 复用 Native Provider 域生成的完整运行 profile：代理通过 TOML 结构化解析将快速服务、请求压缩及其他有效设置转换为 app-server 支持的 `-c` 覆盖，再追加项目登记的 Provider、模型和目录锁定；普通 Codex 命令继续使用原 profile。配置或最终 Windows 命令行超限时明确拒绝，不做静默截断。
- 开启 cc-connect 日志后可观察 Codex app-server 启动、initialize、thread/resume、turn/start、完成、错误和审批阶段，关闭时不增加日志；阶段记录不包含消息正文、回复内容、文件内容、Session ID 或 Provider 密钥。

### 应用启动与数据升级

- 启动流程将设置、会话恢复、主数据库升级和项目加载拆分为独立阶段；阶段运行超过 15 秒时继续等待并显示中英文长时间加载提示，不再把正常执行中的大型 SQLite 迁移误报为 `startup_timeout:stores`。真实初始化异常仍进入启动失败页，本地用量记录和既有迁移内容不会被删除或重写。
- 百万级 `usage_records` 的历史项目路径回填不再作为 migration 32 的单次启动长事务执行：启动前兼容登记继续使用原始 SQLx checksum，数据库 Schema 就绪后由单飞后台任务按 2,000 行短批次渐进回填。任务只更新空路径，支持退出后下次启动继续；绝对路径、唯一配置项目及同会话 route 继承保持原语义，重名项目和 SSH 项目不做猜测映射。
- 健康的用量数据库再次打开时只做 Schema 完整性检查，不再重复执行历史请求日志全量回填和视图重建；活跃历史文件更新先通过 `request_logs(file_path)` 索引取得请求 ID，再按 `usage_records.record_id` 主键删除旧用量记录，避免百万级主库同步反复长时间持有 SQLite 写锁。

### 终端 OSC 52 剪贴板

- 内置终端解析 OSC 52 与 tmux DCS 包装的剪贴板写入，通过已有 `copyTextToClipboard` / Tauri clipboard-manager 写入本机剪贴板，覆盖本地、WSL 与 SSH 远程全屏 TUI（Grok / Claude / Codex）。
- 实时输出才应用剪贴板写入；replay/reset 只剥离序列不写剪贴板。查询（`?`）仅在用户显式开启默认关闭的读取权限后回应当前本机剪贴板，异步读取完成后会再次确认授权；超过 2,000,000 个 Base64 字符的回复或满载队列中的请求会被跳过，读取权限不参与备份/同步。空 payload 和非法 Base64 不改剪贴板。可见流中不再留下 OSC 52 原文。（Refs #211）
- 「设置 -> 快捷键」可独立关闭 OSC 52 写剪贴板与读取本机剪贴板；tmux DCS 的 ST 终止符、无填充 Base64 和大剪贴板编码均受支持。`Ctrl+Shift+C` 复制终端选区并仅在终端区域阻止 DevTools 检查元素。

### Hook 审批通知

- Codex 子 Agent 的待审批状态在共享 Hook 入口统一仲裁：包含明确审批说明的事件立即同步到终端状态、桌面宠物、任务栏、系统/第三方及远程托管通知；缺少显式证据的事件先保留 15 秒，并仅在 rollout 未继续写入、没有匹配工具进展且子 Agent/会话未结束时保守提醒。不同父会话和子 Agent 独立跟踪，不以工具名称或本地化文案作为永久过滤条件。
- 仲裁只会暂缓本地/WSL Codex 子 Agent 的模糊审批；SSH spool 权限请求直接投递。暂缓记录按 source、环境、tab、session 与子 Agent 严格隔离，跨来源、跨环境或缺失 session 的事件不会消解其他记录；转录路径需通过现有 WSL UNC/允许根校验，越界路径不参与文件轮询。
- Codex Hook 的 `PreToolUse` / `PostToolUse` 会以内部 `ToolStart` / `ToolStop` 事件补充审批进度证据：进度先到或审批先到均可按 tool ID 关联，缺少 ID 时只在同一父会话、Tab、环境和子 Agent 内按精确工具名匹配。内部事件在共享 Rust 仲裁层消费，不进入桌面宠物、应用 toast、任务栏、系统/第三方或远程托管通知；真实未解决审批继续在有界等待后恰好提醒一次。旧安装缺少生命周期 Hook 时状态显示为部分安装，可通过重新安装安全升级。

### 终端 Tab 图标

- OpenCode 与 Pi 终端 Tab 图标继承当前 Tab 前景色，在深色、浅色和分屏终端主题下保持清晰可见。
- 终端会话 Tab 与 Workspan Tab 支持鼠标中键关闭，复用现有关闭确认、焦点与会话清理流程，不影响拖拽或重命名。

### 侧边栏项目树

- 项目行终端数徽章按 CLI 工具着色（Claude Code 橙、Codex 绿、Gemini CLI 紫，其余默认蓝），便于在同一分组里快速区分不同工具的项目。
- 分组行显示项目数徽章：含子分组递归、不计 Worktree，超过 99 显示 `99+`；折叠分组后仍可判断规模，与折叠侧边栏窄条徽章使用同一统计口径。
- 分组与项目支持外观标记：可选 10 色调色板颜色，或点击紧凑标记输入框打开本地 Emoji 15 数据直接渲染的 Emoji 选择器（搜索、九个可切换分类、可见原生滚动条），并提供离线可用的飞书、小红书、哔哩哔哩品牌标记，也可自定义标记替换图标。默认不着色、跟随统一的系统色，只有手动设置才改变；项目与分组行设置颜色后均显示左侧细色条，单色图标也跟随颜色。折叠侧边栏窄条图标同样跟随，Worktree 行保持既有绿色身份。
- 外观标记有两个入口且共用同一面板：新建分组内联行的图标位（不点直接回车仍是一步建组）、分组/项目右键菜单「外观标记」（菜单内内联展开）；项目新建、编辑、克隆弹框不显示外观标记，编辑保存不会覆盖原有标记。
- 同一分组/项目的外观在侧边栏、History 侧栏与 Stats 项目筛选三处一致，颜色与图标使用同一份解析实现。
- 外观标记随 WebDAV 备份与恢复往返；用旧版本快照恢复时不报错，缺失或非法的外观值退回系统色，恢复对话框会提示工作区恢复将覆盖本机外观标记。
- 外观列具备缺列自愈：打开数据库前会校准 `icon` / `color` 列与迁移登记状态，缺列时先补齐、未登记时补登记，修复项目列表外观标记的 
o such column: color` 报错并避免迁移重放失败；外观迁移使用 v34，并兼容已登记的旧 v33 外观迁移与路由用量诊断迁移冲突。
- 项目列表右键菜单靠近窗口边缘时会按实际菜单尺寸自动翻转、钳制并在必要时允许菜单内部滚动；展开外观面板时保留原菜单位置，Emoji 快选优先向右弹出并自动避让视口边缘。选择器不再命令式挂载异步 Web Component，因此不会因右键菜单重渲染留下空白；分类标签可切换，并使用应用样式的原生滚动条，避免内容跳走、遮挡或无法下拉。
- 侧边栏折叠后项目仍显示与新建项目、展开项目树一致的 CLI 工具图标；单击跳转已有终端 Tab，双击启动新终端，项目分组浮层使用更高不透明度避免内容穿透。
- 项目行悬停时会始终预留“启动”操作位，仅切换可见性，不再因操作按钮从隐藏态插入布局而挤压标题、徽标或产生抽搐。

- 路由接管后的故障转移队列支持两种模式：关闭自动故障转移时使用单选队列进行手动热切换，不显示优先级徽标或排序手柄；开启后使用多选队列按优先级自动故障转移，已入队供应商按 `provider.sort_index` 自动连续置顶为 `#1/#2/...`，未入队供应商保持相对顺序并排在其后。侧边栏与设置页共用该派生顺序并通过轮询同步供应商排序和 daemon 熔断状态，设置页参数草稿不会被后台轮询覆盖；切换故障转移模式不会重置已保存队列或目录顺序。自动队列未包含原当前供应商时，首个成功完成请求的队列供应商仍会通过安全热切换成为当前供应商；失败、取消或未完整结束的流不会更新当前状态。
- 终端右侧供应商快捷面板复用其他终端辅助面板的 36px 标准标题栏，保留全局切换与设置入口，展示路由状态，并支持自动故障转移队列的勾选、排序和手动热切换；自动故障转移开启时供应商卡片本身不执行全局或手动切换，仅通过队列控件调整成员与优先级。路由卡片使用两列布局，左侧路由状态固定为 140px，并在标题下直接显示跟随当前 CLI Home 接管状态的“路由运行中/路由未开启”，不再显示切换模式文字或独立状态胶囊，右列展示两个不带辅助小字的单行开关；供应商正文保持单列，品牌图标与状态圆点垂直居中，当前项由卡片高亮表达，右侧用不同颜色圆点区分可用、不可用、半开和熔断。
- 供应商快捷面板的路由卡片提供第三个开关「展示不可入队」，默认关闭：关闭时列表过滤掉状态为「不可入队」的供应商，打开后展示完整列表。该开关只做视图过滤，不影响故障转移队列成员、目录顺序与优先级编号，队列提交与拖拽重排仍按全量供应商计算；已熔断/半开供应商与当前生效供应商始终展示，存在隐藏项时列表计数显示为「可见数/总数」，全部被过滤时提示打开该开关。
- 供应商快捷面板的「CLI 类型」分段控件、路由状态卡片与「供应商列表」标题行为固定区域，仅下方供应商列表滚动；面板级错误提示同样固定，不随列表滚走。
- 未接管本地路由时，点击供应商会先完成全局切换预览，再在所选供应商卡片下方显示终端配色的紧凑确认条；确认条仅保留目标供应商、生效范围和确认/取消操作，确认后继续使用预览指纹执行安全应用。
- 自动故障转移开启时，供应商快捷面板、设置页“供应商目录”主列表与路由页故障切换列表均按同一队列顺序展示；主目录卡片同步显示 `#N`、加入/移出和上下调整操作。三处支持直接拖动供应商卡片调整目录顺序，设置页保留上下箭头和键盘排序操作，侧边栏隐藏重复的上下移动图标但保留拖拽/键盘手柄；侧边栏拖拽卡片仅沿纵轴移动，不会因横向偏移触发底部滚动条；任一入口修改后其他入口显示相同顺序。
- 本地代理测试具有总超时保护；Claude 自定义模型显示名称（含 `[1m]` 变体）可作为路由模型映射别名转发到实际供应商模型。
- 故障转移支持 daemon 级渠道遍历诊断日志，可查看候选顺序、跳过原因、请求结果和最终渠道。

- 故障转移队列高对比度显示当前渠道；重置状态后默认恢复到队列第一项可用供应商。

- 供应商设置页缓存上次浏览位置与页签；故障转移队列明确显示当前正在使用的渠道。

- 路由服务启用配置与 daemon 实时状态分离校准；应用启动并完成 daemon 连接后会自动按持久化意图恢复 listener，同时恢复本地与 WSL 接管所需的地址及实际端口；设置页刷新仍提供运行期间二次协调，界面开关以实际运行状态为准。

- 供应商路由队列支持已入队标识与优先级调整；流式渠道异常会立即触发后续渠道，成功的故障转移渠道会作为下一轮请求的优先渠道；项目级 Codex/Claude 供应商启动参数分别使用 `--profile` / `--settings`。

- 原生供应商路由界面将本地路由服务、按 Claude/Codex/Grok 类型切换的“接管 {app} 路由”和自动故障转移联动展示；故障转移仅在接管后显示并默认折叠，队列隐藏 UUID/冗余密钥信息并在供应商行内展示健康/降级状态，移除侧边栏快捷开关，高级路由配置折叠保留并支持自定义监听端口。

### SSH 保存密码与 MFA 混合认证

- 使用系统凭据库保存 SSH 登录密码时，普通密码提示继续由一次性 AskPass 自动填写；Keyboard-interactive 的 MFA、OTP、验证码和 PIN 等后续挑战改为在所属 SSH 终端关闭回显后人工输入，不再循环提示或串到其他会话。Windows ConPTY 下复用 AskPass 继承的标准输入/错误输出，避免输入验证码后连接被关闭；后台连接测试、目录浏览和 Agent one-shot 显式保持非交互，并在日志目录提供不含敏感内容的 `ssh-askpass.log` 诊断日志。

### 本地 Token 用量与请求记录

- 供应商路由开启后，请求日志可记录真实上游 usage、实际 Provider、请求模型与出站模型；历史分析和今日项目用量统一使用路由数据优先、本地会话日志回退并去重的口径，显示 usage 缺失、未归属和数据来源状态，费用按当前模型价格实时计算；兼容 CRLF 流式响应、路由写入后的统计缓存刷新、失败尝试记录和客户端中断收尾。
- 路由失败、跳过和流式异常记录会保存稳定错误码，并只从允许的上游 JSON 错误字段提取脱敏、限长的诊断摘要；请求日志状态列优先显示对应报错而非泛化的“usage 不适用”，可打开详情查看供应商、HTTP 状态、错误码与安全异常文本。完整请求/响应正文、headers、密钥和 Token 不写入本地记录；旧记录没有诊断文本时继续显示可用的本地化回退说明。
- 历史用量分析提供请求级本地统计，覆盖 Claude、Codex、Gemini、OpenCode、Grok；展示请求数、输入/输出/缓存创建/缓存读取、真实总 Token、缓存命中率、趋势、来源构成、模型构成、成本和未定价 Token。
- 现有 `request_logs` 表负责增量同步、稳定事件去重和查询。Claude/Codex 沿用原有 Windows/WSL 配置目录策略；Gemini、OpenCode、Grok 使用本机默认历史目录/数据库，OpenCode 使用现有 SQLite 会话定位。
- 请求记录页支持五类来源、项目、模型、日期范围筛选，并与统计看板复用同一份本地请求数据；分页查询针对路由去重匹配建立索引，避免大历史库重复扫描；现有项目排行、会话分析、热力图、效率图及独立 ccusage 分支保持不变。
- 健康用量 Schema 被统计、路由或请求日志同步重复打开时只做只读一致性检查，不再重放历史数据全量导入；活跃会话替换通过文件唯一索引和请求记录主键定向清理，在百万级数据库中避免长事务阻塞项目、Hook、Replay、SSH 与 Provider 写入。

### 实时统计刷新

- 实时统计轮询检测到会话文件变化后直接读取最新会话详情，手动刷新支持一次强制读取，减少因历史索引缓存导致的数据滞后。
- 会话信息卡片的时长优先按历史 transcript 的首尾有效事件时间计算；Codex rollout 文件创建时间与修改时间相同时仍可显示真实时长，无有效事件时间时回退文件元数据。

### Agent MCP 与 Skills 会话诊断

- 实时统计新增可排序/隐藏的「Agent 能力」卡片，严格使用当前终端 Hook 绑定的 Session ID，不以项目最近会话兜底；支持 Claude、Codex、Pi、Grok Build、OpenCode，摘要使用跟随终端主题的 Agent 品牌图标徽章并显示激活 MCP 健康分布与可用 Skills 数量，点击两类摘要可直接进入对应详情页签；详情按 MCP/Skills 分类筛选并完整展示来源、状态、脱敏错误和 24 小时制证据时间。
- MCP 的生效状态与健康状态分离：仅静态配置的 MCP 标记为“未知”，会话内成功/失败调用或 Agent 原生只读检查才标记正常/异常；禁用 MCP 保留在详情但不计入激活数量。用户级与项目级 Codex TOML 均按完整文档解析，合法的顶层键、注释和 MCP 表不会被误报为配置解析错误。Skills 覆盖用户、项目、兼容目录和已安装插件缓存，区分可用、禁用、权限拒绝、被高优先级项覆盖及配置无效。
- Codex 原生深度检查兼容当前 CLI 的 `auth_status` 字段：已认证与未认证 MCP 分别显示正常/异常；详情将“已启用/已禁用”与运行健康状态并列，避免把静态配置的启用状态误显示为健康未知。
- 本地与 WSL 使用各自环境中的固定只读诊断命令，SSH 由 Agent `0.1.10` / protocol `1.11` 的 `agentCapabilitiesV1` 能力执行，旧远端 Agent 只提示升级且不读取桌面本机路径。OpenCode 可从能力详情安装 CLI-Manager 标记所有权的会话插件；同名非托管插件不会被覆盖。
- Pi MCP Adapter 的共享全局、Pi 全局和项目级 `mcpServers` 配置已纳入能力发现，支持展示激活与 `disabled: true` 的 MCP；仅静态配置仍显示健康状态未知，但已发现配置不再误报 Pi MCP 可观测性未知。读取仅保留名称、作用域、启用状态和传输类型，不启动 MCP Server，也不返回命令、URL、参数、环境变量或密钥。
- Pi 生命周期 Hook 上报改为非阻塞并设 1 秒本地 bridge 超时，bridge 慢、失败或不可用时不会阻塞 Pi 开始处理消息；原有会话开始、运行中和停止状态绑定保持不变。

### 终端 Markdown 预览

- 已登记历史来源的终端 Markdown 分屏预览在启用终端背景图时仍可正常点击打开，预览面板会透出当前终端相同的背景图、适配方式、位置、透明度、模糊和暗化效果；关闭背景图或当前会话临时隐藏背景时恢复不透明主题面板。
- 已配置 CLI 工具的终端右上角始终保留预览入口；Pi 等已登记历史来源按自身 source 加载绑定会话，未绑定会话或没有可读取历史时保持入口并提示不可用。
- 共享 Markdown 渲染器支持 `$...$` 行内公式和 `$$...$$` 块级公式，终端预览、历史消息、文件 Markdown、Prompt 和更新说明统一使用 KaTeX 渲染；代码块中的公式文本不会被解析，长块级公式可横向滚动，原始 HTML 仍保持禁用。
- 终端预览识别顶层 `md` / `markdown` 源码围栏并解包后渲染，内部代码块仍按代码显示；预览头部提供可滚动的当前会话历史回答选择器，按回答序号和时间切换查看此前的 Markdown 内容，并支持 `Ctrl/⌘ + 滚轮`缩放预览字体。预览文本跟随“设置 -> 通用”的字体与字号，缩放后显示继承当前终端主题色的字号提示，可缩小、放大或恢复，并在最后一次操作 2 秒后自动隐藏。
- 已恢复且已登记历史来源的终端只要绑定了 `cliSessionId` 就可以直接打开历史 Markdown 预览，不依赖本轮 Hook 完成事件。

### 文件预览

- 文本文件源码视图与 Markdown 渲染预览跟随“设置 -> 通用”的应用字体和字号；在预览区域按住 Ctrl 滚动鼠标滚轮可按 1px 临时缩放字体（8px～32px），源码和渲染模式共享当前缩放值，不写入全局设置。缩放后显示当前字号及缩小、放大、恢复操作，并在最后一次操作 2 秒后自动隐藏。普通滚轮、图片预览、无法预览文件和 Git Diff 保持原有行为。
- Markdown 源码与格式化预览支持 `Ctrl + 鼠标右键`激活链接，格式化预览也支持普通点击和键盘激活：当前文档锚点在当前视图定位，相对文件及跨文件锚点在当前项目与 Windows/WSL/SSH/Worktree 环境中打开，HTTP(S) 与邮件链接交由系统应用。中文、Emoji、重复标题、引用式/自动链接、链接图片及编码路径保持可导航；越界路径、无效协议和缺失目标显示本地化提示，不修改 Tauri 应用 URL。
- Markdown 预览中的无语言 fenced block（例如 ASCII 流程图）按块级等宽内容展示，保留原始空格与换行，正文使用终端前景色并支持长内容横向滚动。
- 文件源码预览的 Monaco Unicode 歧义字符提示跟随应用界面语言显示中文或英文，字符高亮和安全提示仍保持启用。
- 文件编辑器中的普通已打开文件标签支持与终端 Tab 一致的终端主题右键菜单，可关闭当前、其它、左侧或右侧文件；空目标项自动禁用，批量目标有未保存内容时可一次选择保存、丢弃或取消，且只影响当前文件工作区，不会关闭固定 Git Diff 标签或出现终端新建、复制、分屏等操作。

### 桌面远程连接

- 设置“远程连接”使用纵向排列且随内容区伸缩的两个独立入口区分“本机 cc-connect”和“Web 设备连接”；入口无需打开详情即可展示当前配置/运行状态，两套配置分别在右侧抽屉中展示，原有保存、启停、重启、配对、凭据与日志能力保持不变。

### Web 工程基础

- 新增独立 `apps/web` React/Vite 浏览器工程，与 Tauri 桌面入口和运行时依赖隔离；根目录提供 `web:dev`、`web:typecheck`、`web:build`、`web:server:check`、`web:server:test`、`web:server:run` 脚本。
- 首批提供桌面三栏与移动单栏工作台、移动底部导航、浅色/深色/跟随系统主题、本地主题与语言记忆，以及 `zh-CN` / `en-US` 双语文案。
- 工作台接入本地单用户登录、会话过期恢复、设备配对与选择、服务端历史侧栏、浏览器事件重连和结构化 Prompt/Operation 时间线；建议卡只填充可编辑草稿，离线时保留草稿但禁止发送。
- Web 工作台左侧项目树由桌面端上传的真实分组、项目与 Worktree 快照构建，保持桌面端的层级和紧凑选中样式；历史会话挂载在对应项目或 Worktree 下，不再反向生成项目列表。
- 登录后默认进入桌面主机首页，按在线优先、最后心跳时间倒序展示全部已配对设备；首页提供名称/主机名/平台搜索和在线状态筛选，主机信息使用每行多张的自适应卡片网格展示，设备区域固定为 720px 高度并在卡片超出时内部滚动。卡片显示系统、桌面端软件版本与最后心跳，不展示 CPU、内存、架构和分辨率；悬浮卡片可确认删除并解除当前账户配对。无设备时可直接配对，点击主机进入现有工程工作台，工作台可返回主机列表重新选择设备。

### Web 服务与桌面协议

- 新增独立 `apps/server` Rust 服务和 `crates/web-protocol` 共享协议包；单进程同时提供静态 Web、HTTP API、Browser WebSocket 和 Device WebSocket，SQLite3 使用 WAL、外键和 busy timeout，首版不依赖 Redis、消息队列或多实例数据库。
- Web 设备桥接已拆为独立 `cli-manager-web-daemon` 守护进程；daemon 负责 Device WebSocket、配对凭据、重连、历史上报和 operation 队列，桌面进程继续负责本地项目、PTY、CLI、文件、Git、Worktree、Hook 和原生确认。
- 安装版与 Dev 版使用独立持久化软件 ID 和 Web 配置文件，配对 Token、连接、项目快照互不覆盖；同一台电脑共享机器 ID，Web 端可区分“安装版”和“开发版”。旧版 `deviceId` 首帧仍可兼容接入。
- 管理员密码通过环境变量初始化并使用 Argon2 哈希保存；浏览器会话使用 `HttpOnly`、`SameSite=Strict` Cookie，非回环监听必须启用 Secure Cookie，跨域只接受显式配置的单一 Origin。
- 设备首帧必须完成协议版本与设备 Token 校验；未配对设备仅允许配对和心跳。设备发送队列和浏览器广播均有界，慢客户端断开后通过持久化 sequence 游标补传，重复项目快照序号会被去重。
- 对话与管理操作按用户幂等键去重，同一键对应不同设备、类型或载荷时返回冲突；状态严格按 `submitted -> waiting_device/accepted -> running -> terminal` 推进，纯校验或桌面拒绝可直接进入 `rejected`，只有桌面回执能进入终态。
- 服务启动时将数据库中的旧在线设备统一标记为离线；设备重连后重新登记在线连接并补发待桌面接收的操作。桌面端只上报安全的分组、项目与 Worktree 快照，不上报历史会话列表；空会话全量快照会清理服务端遗留的旧会话摘要。
- Tauri 桌面端提供由 Rust 生命周期托管的 Web 设备适配器，窗口隐藏或设置组件卸载不会中断连接；设备 Token 仅保存于系统原生凭据库。Web 新建/继续对话必须显式携带来源、项目、工作目录和会话上下文；新建对话以桌面端已注册项目/Worktree 为准，继续对话额外匹配本机历史会话。桌面同时校验 CLI 与 Hook 状态，并对原生/WSL 路径执行 canonicalize 边界校验；Prompt 仅在 `SessionStart` 后写入，`Stop` / `StopFailure` 分别作为成功/失败终态，SSH 项目在 P0 保持不支持。
- Web 管理抽屉覆盖 SSH 主机摘要/检测/远程目录、项目文件生命周期、Git、Worktree 与 Claude/Codex Hook；入口按设备 capability 启用。危险操作同时要求 Web 展示目标确认和桌面原生确认，文件/Git/Worktree 根路径只由桌面历史和本地项目解析；返回结果不包含凭据、私钥/代理信息、本机 Worktree/Hook 路径或 OpenSSH 原始 stderr。
- Web 左侧项目树复用桌面端的分组、项目、Worktree 层级与 CLI/Worktree 图标，不展示会话节点；设备在线且声明 `project.management` 时支持分组/项目同层排序和跨分组拖拽，桌面端校验完整同级 ID 与循环分组后持久化，Worktree 保持只读不可拖拽。
- Web 项目树补齐项目、分组和 Worktree 右键菜单；在线设备可通过悬浮 Play 按钮快捷启动项目、Worktree 或分组，并通过 `project.start` / `project.action` 按节点 ID 转发外部终端、分屏、目录/文件、历史、编辑、重命名、Provider、批量 Shell 和 Worktree 等桌面能力。危险动作继续由 Web 服务校验确认标记、桌面 action bus 调起原生确认，并且不向浏览器传递路径、环境变量或 Provider 敏感配置。
- 桌面 operation 队列有界且按服务端 ACK 移除；队列满时继续消费 ACK，容量恢复后重连补取延迟请求。桌面或 WebView 在 accepted/running 阶段中断时返回 `operation_interrupted`，不会自动重复执行管理写操作。
### SSH 通用文件能力

- SSH Host 终端或 SSH 项目只要已安装可用的 SSH Agent，即可浏览远端文件，并把终端粘贴或拖放的任意非空普通文件上传到远端私有缓存；单文件固定上限 20 MiB，保留安全原文件名，目录和符号链接不上传。这些通用文件能力不要求项目配置 Claude/Codex 等 CLI 工具；远端历史仍按来源独立校验。
- Agent `0.1.6` 继续兼容原有 5 MiB 图片上传；任意类型文件要求 Agent `0.1.7` / protocol `1.10` 的 `fileAttachAny` 能力，旧 Agent 不会误粘贴桌面本机路径。

### 原生供应商域 Phase 1（纵向切片）

- 独立 `providers.db` 维护 CLI-Manager 原生供应商目录，按 Claude / Codex / Grok Build 分型，支持供应商新增、编辑、复制、删除、启停、当前项设置，以及 Base URL、模型和 API 格式等可见字段；原有 cc-switch 只读供应商页继续保留为导入来源入口。
- 原生供应商页拆分为类型标签、目录卡片、详情编辑器、API 密钥区、供应商表单和密钥表单；密钥默认只显示脱敏提示，支持编辑时加载明文密钥、编辑标签/备注/标签、显式查看明文、手动上下排序和激活，激活会更新对应 CLI 配置投影；删除当前密钥必须先选择同供应商启用密钥替换，替换与投影、删除在后端同一事务中完成。
- 供应商卡片启停开关控制该供应商是否可用于全局、项目和 Worktree 选择；当前供应商或仍被项目/Worktree 引用的供应商不可停用。多密钥列表分别标识当前密钥和启停状态，避免混淆“当前”与“可用”。
- 类型级通用配置提供 Claude JSON、Codex/Grok TOML 编辑、格式校验和供应商覆盖预览，编辑器默认收起且可安全重复展开；切换 CLI 类型前会保护未保存草稿。通用配置只校验格式（Claude 必须是 JSON 对象、Codex/Grok 必须是合法 TOML），不按字段名拦截密钥或敏感字段，读取与保存都原样回显不脱敏。
- 项目或 Worktree 切换供应商时，Claude 生成的项目快照会保留通用配置、供应商配置和当前密钥合并后的完整 `settings.json`。
- Codex 全局配置将 API 密钥写入 `auth.json` 根级 `OPENAI_API_KEY`，`config.toml` 仅保存模型、模型提供商和其他非密钥配置，不额外声明 `env_key`。
- Codex 原生供应商启动不会用临时目录替换完整 `CODEX_HOME`：全局供应商使用已应用到真实 Home 的配置，项目/Worktree 覆盖仅通过启动配置参数与子进程密钥环境切换 endpoint/model/key，继续继承原 MCP、Hook、沙箱策略、插件、历史会话和实时统计。
- 供应商详情或编辑弹框内触发删除时，删除确认框和遮罩位于父级弹框之上，操作按钮不会被遮挡。
- Grok Build 原生供应商保留真实 `GROK_HOME` 与现有全局配置行为；项目和 Worktree 暂不支持供应商切换，入口会直接提示，不写入覆盖也不改动全局配置。旧临时 Home 会话在快照清理前备份并合并回真实历史目录，失败时不删除源数据；历史读取直接使用 `.grok\\sessions` 根目录，不会重复拼接 `sessions`。
- 通用配置使用可折叠代码编辑器：Claude 提供 JSON 编辑/对象校验，Codex/Grok Build 提供 TOML 编辑与非写入语法校验；折叠态收成单行（标题 + 格式徽章 + 刷新/保存），说明与优先级文案仅在展开后显示。供应商目录与 CLI Home surface 位于 CLI 类型切换上方。
- 供应商域共用的代码编辑器自行接管受控值同步：按发出顺序记录尚未回灌的编辑值，连续快速输入时不会因受控值落后而整篇改写模型，光标停在输入处、字符不错位；刷新、保存回填、切换供应商或文档、生成配置等外部替换会保留并恢复选区与滚动位置，只读预览刷新也不跳回顶部。
- 供应商目录为全宽单行列表：每行显示厂商图标、名称、当前徽章、模型/endpoint 摘要与可用性状态圆点，拖拽手柄常驻卡片行首，启停开关常驻，箭头排序与复制/删除收进 hover 才显形的更多操作菜单，拖拽排序走 @dnd-kit（搜索过滤时禁用以保证提交全量顺序）。点击任意一行打开供应商详情弹窗，按“基本信息 / 生效配置 / API 密钥 / 完整配置”分 Tab 展示，基本信息内直接提供全局预览与应用入口；关闭弹窗时若完整配置有未保存改动会先确认，目录不会残留上一项的选中样式或行焦点环，内部选择缓存仍用于恢复详情上下文；关闭后焦点落在现有目录 surface 导航控件，编辑表单关闭仍先回到详情弹窗。Codex 与 Grok Build 复用同一维护结构。
- 供应商详情的生效配置使用 JSON/TOML 代码编辑器样式，Codex/Grok Build 从内部 JSON envelope 解包展示 TOML；新增/编辑表单可维护当前供应商专属配置，保存时保留密钥管理区拥有的敏感字段。
- Codex/Grok Build 的新增/编辑表单提供 CCS 风格高级选项（上游格式、模型映射、User-Agent、Header/Body 覆盖、Goal mode、远程压缩）；空的供应商专属配置会按基础字段自动生成可读的 Claude JSON、Codex TOML 或 Grok TOML，用户手动编辑后保留原始未知字段。
- 全局应用确认显示当前 CLI 对应的 `.claude`、`.codex` 或 `.grok` 配置目录；用户可直接点击应用，由系统自动完成受指纹保护的应用前检查，不再强制先点击预览。
- 完整文档编辑器支持 Claude `settings.json`、Codex `auth.json` 与 `config.toml`、Grok Build `config.toml`；后端负责语法校验、嵌套 TOML 摘要、敏感字段脱敏与密钥管理边界，切换供应商或 CLI 类型时不会静默带入未保存草稿。
- Home 全局应用、项目/Worktree 解析、环境诊断或 CCS 导入向导仍按供应商域后续阶段继续交付。

### 原生供应商域 Phase 2（进行中）

- Codex 模型映射支持连续输入与多行编辑，输入内容变化不再造成失焦；行身份仅用于界面，不写入供应商配置。
- Codex 生效配置及字段来源的模型与实际全局应用保持一致，包括带 `/` 的模型 ID；公共配置继承与未知 TOML 字段继续保留。
- 全局配置应用预览显示目标文件、路径、受管字段及新建/更新/无变化状态，不展示快照和文件指纹，内部校验机制保持不变。

- 供应商设置中的目录、详情、API 密钥、导入、CLI Home、路由、表单及模型维护操作统一复用项目按钮样式；普通文本按钮使用常规字重，详情页删除与编辑保持同层级的轻量操作样式，主、次、轻量、危险、成功与图标按钮在主题切换、禁用、加载和键盘访问时保持一致。
- cc-switch 导入向导在扫描预览后默认勾选“导入密钥”；用户仍可在提交前取消，密钥继续仅保存在后端 `providers.db`，列表、预览和日志不返回明文。
- WSL 供应商切换与本地路由接管复用短生命周期配置计划和成功探测结果；同一 Home 的配置读取、可写检查、暂存写入与验证采用批量 WSL 调用，NAT 网关 route/addr 查询合并执行，同时保留 Home/供应商来源复核、文件指纹冲突保护、权限校验与失败回滚。
- 侧边栏供应商快捷切换与设置中的全局切换保持一致：读取当前 CLI Home 的本机/WSL 身份，对该 Home 执行预览、确认、全局应用与路由接管，不主动清理已有项目/Worktree 覆盖；切换面板显示本机/WSL 模式图标与 WSL 发行版名称。
- CLI Home 运行环境切换不会自动触发耗时的 WSL Home 识别或环境诊断；用户点击刷新后才按当前环境识别并更新结果。切换到 WSL 时自动列出已安装发行版供选择，优先保留当前发行版，不存在时选择列表第一项。
- CLI Home 设置重新打开时恢复已保存的活动运行环境、WSL 发行版、Home 来源和手动目录，不会用本地 `host` 默认值覆盖已持久化选择。
- CLI Home 的 Claude、Codex、Grok Build 类型切换不会重复触发 Home/WSL 识别；下方派生路径、CLI 可用性、配置目标、Hook/历史根目录和冲突诊断按当前类型显示。
- CLI Home 将本机与 WSL 作为独立运行环境管理；切换环境只显示对应环境的缓存配置，未识别的新环境不沿用另一套 Home、Hook、历史或配置路径。
- CLI Home 进入时优先显示已缓存的活动 Home，不因 WSL 校验、全局当前状态或发行版列表扫描阻塞编辑；发行版列表在后台辅助加载。
- CLI Home 保存/恢复只处理当前环境的 Home 校验与持久化，完整供应商状态和环境诊断由显式刷新触发；WSL 手动 Home 校验合并目录、读写权限检查，减少重复启动。
- Home 解析支持自动/手动本地目录、绝对路径、WSL UNC 路径、多发行版独立保存和恢复自动模式；切换到 WSL 自动模式时识别默认发行版及其真实用户 Home，不沿用本地 `host` 身份。Local/WSL 在自动和手动模式下均可打开目录选择器，选择后切换为手动模式，再统一派生 Claude、Codex、Grok 配置目标。派生路径卡片左侧使用 Claude、Codex、Grok 品牌图标区分 AI Agent，右侧使用不同图标与颜色区分普通目录、历史目录、配置文件和认证文件，同时保留明确的中英文文本标签。显式 Hook 目录与历史来源优先级更高，不会因 Home 变化被静默改写。Grok 历史默认根目录为 `.grok/sessions`，显式采用需用户确认。
- WSL Home 路径可直接编辑或粘贴；首次输入会把草稿切换为手动模式，目录选择器以当前 UNC Home 为初始位置，取消选择不改变草稿。
- 环境诊断返回 CLI/版本、Home 来源、目标访问、当前供应商、冲突指纹、Hook/历史对齐和待恢复状态，默认不返回明文；设置页支持复制脱敏诊断和打开已存在的目标目录。
- 全局供应商预览/应用具备目标指纹校验、分阶段写入、备份、验证、失败补偿、操作日志与启动恢复；只有所有目标验证成功后才提交原生当前供应商状态，旧终端会话保持固化快照。
- 供应商类型标签支持鼠标与键盘方向键、Home/End 切换，并在切换后保持可见焦点；Claude / Codex / Grok Build 三个标签显示对应 CLI 工具图标（与项目树、历史列表共用 `CliToolIcon` 图标体系），未选中项图标半透明。
- 供应商功能的界面文案统一为「供应商」，不再带「原生」前缀（设置页标签、目录标题、搜索、新增/编辑弹框、空态与错误提示、备份摘要）；内部标识与文档中的域名称仍沿用 
ative_provider` / 原生供应商域。
- 全局应用成功后会刷新当前供应商目录与详情，当前供应商和活动密钥状态不会停留在旧快照。
- 历史列表、搜索、统计、提示词和编辑链路统一接收显式 Claude/Codex/Grok 历史根目录，Home 变更不会自动迁移显式历史来源。
- Claude 供应商维护补齐 CCS 兼容的 API 格式与认证字段下拉选择、完整 URL 开关、Sonnet/Opus/Fable/Haiku/Subagent 模型映射、显示名称、回退模型和 1M 标记。
- 供应商新增/编辑弹框的「高级选项」中可直接维护 API 密钥，不必再先保存供应商再去「API 密钥」页签补配：密钥输入框占满整行以完整显示，多密钥时通过输入框内的小图标菜单切换；新增时填写的密钥随供应商一并创建并自动激活（留空则只建供应商，保留先建壳后补密钥的用法）；编辑时输入框回显当前激活密钥的明文，旁边的下拉框可切换要查看/编辑的密钥，改动只写回所选密钥的值，**激活项不受该弹框影响**，仍在「API 密钥」页签中调整。密钥值未变更时不会写回，避免把供应商推进「密钥已变更、需重新预览应用」状态。
- 「获取模型」可使用表单里尚未保存的临时密钥，新增供应商时即可拉取 `/v1/models`；该密钥仅用于当次请求、不落库，也不会写入调试日志。已有供应商在不填写表单密钥时仍按激活密钥取模型。
- 供应商的模型、Claude 回退模型与各角色模型改为带建议的输入框：`/v1/models` 拿到结果时只展示接口返回的模型，不与本地维护的模型混列；接口没调过、失败或返回空时回退到「当前已配置 → 模型价格表 → 其他供应商在用」三级本地候选并分组展示。字段已有值时点开展示全部候选而非只剩当前一条，键入内容才按子串过滤，过滤为空时退回全部；任何时候都允许直接输入不在候选中的模型 ID。候选值不带 `[1M]` 后缀，长上下文仍由各角色行的独立勾选控制。
- 取模型失败按原因给出可操作提示，区分地址未填、密钥未填或未激活、密钥含非法字符、请求未送达、响应非 JSON、返回成功但无模型，以及 HTTP 401/403、404 与其他状态码；HTTP 状态在解析响应体之前判定，错误响应体为 HTML 或非标准 JSON 时不再把 401/404 误报成「响应无法解析」。
- 取模型结果只归属它来自的供应商与地址：切换 CLI 类型、切换供应商、修改 API 地址或重新打开表单弹框都会清空上一次拉到的模型列表，不会出现在某一类型下拉取过模型后、另一类型的新建/编辑表单里带着上一批模型的情况；在途请求在列表失效后回来也不会写回。
- 供应商新增/编辑弹框的 Header/Body 覆盖 JSON 编辑器收窄为 84px，底部「取消 / 保存」操作栏吸底常驻；设置页内的嵌套弹框按 Esc 只关闭自身，不再连带关闭整个设置页。
- 内置的 Fluxion AI 赞助供应商（Claude / Codex / Grok Build 各一条）可以被真正删除：删除时在 `providers.db` 的 `settings.builtin_provider_dismissals.v1` 内按 `<appType>:<providerId>` 登记退订，与删除同事务提交，此后 `providers.db` 初始化不再重新播种该条目，删除结果跨目录刷新与重启保持。退订按 CLI 类型独立，删除某一类型的内置项不影响其他类型；仍存在的内置项继续保留改名、排序、停用和密钥等自定义。删除是永久操作，需要时可手动新增同名供应商。
- 2026-08-03 验收收尾已重新执行 TypeScript、Rust 与 provider focused checks；WSL、真实 Home 写入、Tauri UI 手测和 macOS runtime 仍需在对应环境完成，当前不宣称发布验收完成。

## [V1.3.5] - 2026-08-07

### Windows 便携版与数据存储

- Windows x64 发布同时提供免安装便携 ZIP。程序旁存在 `portable.flag` 时按便携版运行，默认数据根目录为程序目录下的 `data`；安装版默认仍为 `%USERPROFILE%\\.cli-manager`。便携程序与 `data` 目录整体复制到新位置后可继续读取原数据。
- “设置 -> 通用”底部提供独立的数据存储区，显示当前分发、默认/自定义模式、当前路径和默认路径。安装版与便携版都可选择自定义目录；用户选择的目录本身就是数据根目录，不追加 `.cli-manager` 或 `data`。
- 切换目录必须完整重启。空目标目录可选择迁移全部当前数据或直接使用；非空目标目录禁止自动合并与覆盖，只允许直接使用。存在终端、daemon 会话、后台同步、SSH Agent 安装或更新任务时禁止切换，应用不会自动终止任务。
- CLI-Manager 管理范围仅包括原数据根目录下的数据库及 WAL/SHM、设置与会话 Store、日志、缓存、附件、备份、Provider、桌宠等内容；外部 `.claude`、`.codex`、WSL Home、SSH 主机数据和项目目录保持原样。
- 安装版与同版本便携版共用应用标识和单实例域，不能同时运行。便携版可检查新版本和查看发布说明，但更新操作只打开 Release 页面下载新版 ZIP，不调用 Tauri 安装器。

## [V1.3.3] - 2026-07-29

### 终端状态标记

- Pane 标记仅在当前可见布局包含多个 Pane 时显示，单 Pane 即使有多个 Tab 也不画线；焦点 Pane 默认混合当前终端弱化色与背景色。主会话 Hook 完成、错误、审批状态分别使用独立颜色，状态色优先于焦点色。后台状态线为 1px/50%，焦点线为 2px/100%；应用失焦、最小化或进入托盘时只移除焦点强调。
- 标记支持完整边框和顶部标记两种样式，顶部标记两侧竖线为内容高度的 2%；所有线条均以终端内容区为边界，不包围 Pane Tab 栏。样式选择使用跟随终端主题的双 Pane Demo，并在活动 Pane 内容区复用实际标记结构；完成、错误和审批颜色可作为预览状态切换，两种样式 Demo 会同步跟随所选颜色。旧 `tab-frame` 配置自动迁移为顶部标记。设置默认关闭并可配置三类状态颜色，缺失或非法开关值按关闭处理；持久化配置参与偏好同步，非法样式或颜色逐项回退默认值。
- PTY、文件编辑器与子 Agent 转录 Pane 都显示焦点线；只有当前 Pane 的活动主会话 Tab 使用 Hook 状态色。隐藏 Workspan 继续沿用汇总圆点，深层嵌套分屏、Pane 全屏与项目范围过滤保持原布局和会话生命周期。

### Pi Coding Agent 兼容

- Pi 中文输入会从可见 viewport 的成对横线（含滚动提示横线）识别无提示符编辑器；硬件光标位于编辑器内时优先使用，否则只读取区域内的反色软件光标。组合文字保持在输入行，Windows 原生候选框锚定下边框；全屏、缩放、分屏或输入期间 reflow 后都会按新终端几何重新定位，不再横向漂移或上下错位。
- 新 PTY 缺省声明 truecolor；Windows 不改 `TERM`，WSL 自动转发 `COLORTERM`。Pi 内置深浅主题的 pending/success/error 工具背景在写入终端前精确清除，保留前景语义色、用户/自定义背景、真实 Diff 与其他 ANSI/OSC。
- Pi 本地历史会话可使用 `pi --session <session-id>` 精确恢复；恢复命令清除冲突的 continue/resume/session/fork 参数并保留模型、`--session-dir` 等普通参数。重复目录项目优先使用精确 Worktree 或当前筛选的同来源候选，错误来源不自动绑定；SSH Pi 恢复暂不支持。

### Codex 终端输入与会话恢复

- 项目直启 Codex、普通终端及右上角新建终端内手动启动 Codex 时，换行组合键统一严格遵循终端快捷键设置；当前可见 TUI 和会话固化 CLI 类型共同参与识别，不再额外要求 Alt 或 Ctrl。
- Grok Build 与 Codex 共用 composer 换行序列 `\x1b\r`：匹配到的宿主换行快捷键写入该序列，未匹配的 Alt+Enter 放行给 xterm；本地 PowerShell、WSL 与 SSH 行为一致。Kimi Code 与普通 Shell 仍发送 `\n`。
- 从历史记录“继续对话”创建的 Codex/Claude 等 CLI 终端会立即保存所选 Session ID；Hook 也会对账内存与持久化快照中的真实 ID，首次绑定、切换或磁盘缺失 ID 时立即自愈保存。关闭后恢复多个 Tab 时分别携带各自明确 ID，仅在确实没有 ID 时使用 `--last` / `--continue` 兜底。

### Hook 任务栏提醒与安装状态

- Windows 主窗口失焦时，符合共享 Hook 事件筛选的事件可独立触发任务栏提醒；默认闪烁 5 次，可设置 1～20 次或持续闪烁到窗口聚焦。系统 Toast 与任务栏提醒分别启停，窗口聚焦会主动清除提醒，外部未绑定 Tab 的 Hook 也可触发。
- Hook 设置页顶部可统一刷新 Claude、Codex、Pi、Grok 安装状态；四种桥接关闭后仍展示真实状态徽标，但继续隐藏模块、路径、安装/卸载操作，检测不会自动启用桥接或修改 Hook。

### Git Diff 审阅

- Git 变更 Diff 弹窗按面板当前筛选顺序审阅文件，可使用按钮或 `F7` / `Shift+F7` 连续导航 Hunk 和文件，并展示仓库相对路径、状态、文件序号与增删统计。
- Diff 支持 Split / Unified 显示模式并持久化偏好；打开源文件时定位到当前 Hunk 的新文件起始行，删除文件保持禁用，本地、WSL、SSH 与嵌套仓库使用同一前端交互。
- 当前 Diff 可固定到项目文件编辑器并以多标签持续审阅；固定页独立持有引用计数 Git Transport，Git 面板关闭后仍可实时刷新和执行整文件、Hunk、行级回滚，项目、Workspan、SSH Host 和嵌套仓库之间保持上下文隔离。
- Diff 生成支持精确比较、忽略行尾空白、忽略全部空白及 3/10/20 行上下文，设置持久化并参与偏好同步；本地、WSL 与 SSH 保持相同结果边界，忽略空白时仅允许整文件回滚，旧 SSH Agent 继续支持默认精确比较与 3 行上下文。
- Diff 行级回滚支持鼠标与键盘单选、连续范围选择和清除，并以数量、勾选标记和无障碍播报反馈状态；弹窗会锁定并恢复焦点，输入法组合时不会被 Esc 误关闭。
- 大 Git Diff 超过 64 KiB 后使用 Worker 解析并按 Hunk 虚拟渲染，仅对已挂载 Hunk 高亮；超过 256 KiB 或 5000 行关闭语法高亮，超过 768 KiB 或 20000 行时由本地、WSL 与 SSH 统一拒绝且不暴露部分回滚。旧 SSH Agent 缺失大小元数据时保持兼容，支持的文件类型不变。
- Diff 弹框与固定页跟随终端主题并提供完整工具栏交互状态；内容区不再显示黑色圆角外框，用户可持久选择自动换行或单行横向滚动，关闭换行时 Split 中线与左右宽度固定并同步滚动两侧内容。查看源文件成功后弹框自动关闭，Pin 后后续文件默认在编辑器打开，固定页可切回默认弹框方式。
- Git 变更面板首次打开 Diff 时直接定位到用户点击的文件，多文件审阅不再需要重复点击。
## [V1.3.2]

### 终端工作区会话恢复

- 应用启动检测到上次遗留的真实 PTY 标签时，可按本机偏好选择“启动时询问”或“自动恢复”；恢复检测每个应用进程仅处理一次，React StrictMode 初始化重入或切换设置页不会重复弹窗或重复恢复。
- 启动恢复提示弹窗不会因点击应用空白区域或按 Escape 被误关闭，必须点击“恢复”或“不恢复”按钮后才会结束提示并执行对应流程。
- daemon 中仍存活的会话优先 attach，不重跑启动命令；Claude/Codex 会话通过原生 resume 续接对话，普通 Shell 恢复静态滚动历史，并还原 Workspan、Pane 分屏树、标签顺序与活动项。
- “恢复上次终端会话”总开关继续位于开发者设置；关闭后常规设置页的恢复方式选择器置灰，启动时清理当前开发/安装环境各自的工作区快照，不影响 SQLite 历史会话。

### 退出与后台任务策略

- “退出时存在运行任务”的处理方式完整支持每次询问、退出应用并由 daemon 后台续跑、最小化到托盘、终止任务并退出四种策略；窗口关闭、普通关闭确认后的退出与托盘退出统一消费该设置。
- 关闭按钮行为设为“询问”时，无运行任务仍显示普通关闭确认；有运行任务时按已保存策略分流，不再无条件重复打开运行任务弹窗。
- 在运行任务弹窗勾选“记住我的选择”后，设置会先完成持久化再执行后台、最小化或终止退出动作；设置写入失败只记录警告，不阻止用户已选择的退出动作。

### 分屏与 Workspan 实时统计

- 单个实时统计面板跟随当前焦点 Pane；鼠标、触摸和键盘聚焦均会同步活动会话。
- 本地 Hook 通过稳定事件 ID、有限重试和 daemon 有界去重降低瞬时投递丢失；旧终端或外部 Grok/CLI 仅在来源、项目/Worktree 路径及近期 PTY 活动能够唯一定位时恢复绑定，多候选时拒绝猜测。
- Windows、WSL 与 Worktree 路径参与统一比较；SSH 仍要求精确远端会话身份，不回退到本地候选。

### 终端选择与复制

- Shell 或 TUI 开启鼠标上报时，普通鼠标拖动仍选择终端文本；按住 Alt 后，点击和拖动才发送给底层应用。
- 终端选区、菜单复制和复制全部内容优先通过 Tauri 原生剪贴板写入，并保留浏览器开发环境降级路径；Windows 短文本可进入 `Win+V` 剪贴板历史。

## [V1.3.1] - 2026-07-22

### Grok Build Hook 与实时统计

- Grok 审批通知通过 `PreToolUse` 的危险写操作 matcher 映射为 `PermissionRequest`；完整安装、单模块卸载与安装状态检测使用同一事件契约，且不会误删共享的工具生命周期 Hook。
- 终端实时面板严格绑定当前 Grok sessionId；catalog 尚未收录新会话时直接读取对应会话文件，后台全量索引刷新不阻塞 2 秒轮询。“今日项目用量”仍按项目与 Worktree 汇总。

### CLI 启动参数历史

- 新建或编辑项目时，CLI 启动参数输入框可展开当前 CLI 工具的本机历史，并支持按输入内容搜索全部历史；匹配结果按次数和最近使用时间排序并最多显示前 10 条，项目成功保存后累计使用次数。
- 克隆项目不显示历史下拉、不累计次数。历史不依赖现有项目记录，并随偏好设置快照同步，恢复时覆盖本地记录。

### 终端文件路径跳转

- 终端工具栏“文件”开启时，Ctrl+单击项目或 Worktree 内的相对文件、目录路径会打开右侧文件面板；文件树自动展开父目录并选中目标目录。
- 文件路径支持 `:行`、`:行:列` 和 `(行,列)`，打开编辑器后会定位、选中并高亮目标行；绝对路径仍按原有逻辑打开系统资源管理器。

### 桌面宠物菜单与远程托管

- 桌宠设置可分别关闭快捷操作菜单和悬停自动展开；快捷菜单关闭时仍保留任务卡片，悬停展开关闭时可通过右键打开。普通任务与托管会话直接展示最多三张卡片，更多内容在毛玻璃容器内纵向滚动。
- 宠物尺寸支持在 40%～150% 之间按 5% 调整；菜单缩放以宠物底部中心为锚点，程序化展开、收起和缩放不会把菜单窗口坐标误保存为宠物位置。
- Windows 远程 Codex 托管通过随安装包提供的 `cli-manager-codex-proxy.exe` 启动 app-server，代理使用 GUI 子系统并隐藏其 CMD/EXE 子进程窗口；普通 Codex 命令仍透传参数、Provider 覆盖与退出码。macOS/Linux wrapper 保持 `0755`。本机解析 `codex` / `codex.exe` 启动器时跳过 cc-connect 写入的 `remote-manager/bin` 托管目录，绑定该目录之外的真实 Codex 绝对路径，避免 PATH 里的 wrapper 递归调用自己；Windows 仍按 `.exe/.cmd/.bat/.ps1` 查找真实启动器。取消托管后按目标 Session 恢复，阻止会话漂移或错误回退到新会话。
- Codex 托管 wrapper 的路径解析回归辅助代码仅在 Unix 编译，Windows 开发构建不再携带无调用测试辅助函数或输出对应告警。
- 已停止的 SSH Codex 会话可复用项目登记的 Host、认证、代理、远端目录与 `CODEX_HOME` 交由 cc-connect 托管，取消后在同一主机、目录和 Session 恢复；仅 Hook/daemon 明确完成或失败，或 PTY 已退出/出错时允许托管，状态未知时失败关闭。
- 项目登记了 Codex Provider 时，远程启动会加载与本地 TUI 相同的完整 Native Provider profile，并再次锁定对应 Base URL、模型和 wire API；Provider API Key 只保存在子进程环境变量中。CLI-Manager 只修改自身的 cc-connect 集成和启动环境，不修改第三方 cc-connect 项目或二进制。
- 本地托管从项目配置一次解析结构化 `cli_tool + cli_args`，并将同一启动器用于预检、Claude/Pi/OpenCode 的 cc-connect `cmd` 与 Codex app-server 代理；支持绝对路径和 Windows `.cmd/.bat/.ps1`，拒绝自由 shell 片段及会抢占托管会话的 resume/session 参数。项目自由 `startup_cmd` 不参与托管。
- 交接状态仅兼容 schema v1 缺失的 Agent；schema v2 必须显式保存有效 Agent，损坏记录不会被默认为 Codex。

### SSH 显式地址直连隔离

- 手工填写主机地址且未配置跳板机的连接，在私钥、密码、凭据引用和 Keyboard-interactive 模式下通过 OpenSSH `-F none` 隔离用户默认 `~/.ssh/config`；配置文件权限错误、无关 Host 规则或语法错误不会再阻断这些由 CLI-Manager 完整描述的认证方式。
- Agent 与 SSH Config 模式仍读取系统默认配置，以保留 `IdentityAgent` 和 `Host *` 等设置；Config 别名、跳板路由及明确选择的自定义 Config 文件继续按原逻辑解析。

### SSH 项目 WebDAV 恢复

- WebDAV 和本地备份会同步 SSH 主机分组、可移植主机档案、远程路径和项目主机绑定；恢复后无需重新创建主机或重新绑定远程项目。
- 密码、凭据引用、私钥与自定义 SSH Config 路径、ProxyCommand 不会进入备份。目标设备缺少本机认证材料时会改为交互认证，而不会伪造或上传凭据。

## [V1.3.0] - 2026-07-20

### SSH Config 导入

- SSH 主机设置支持从原生 Windows、Linux、macOS 用户的默认 `~/.ssh/config` 扫描并批量导入具体 `Host` 别名，不扫描 WSL 配置。
- 导入弹窗默认填充系统 `.ssh` 目录，也可选择其他目录；导入前可预览来源文件、跳过已存在别名、多选候选主机并选择目标 SSH 分组。完成后提示成功、失败和重复跳过数量；目录、文件或 Include 无效时显示本地化失败原因，不写入部分数据。
- 解析支持 UTF-8 BOM、CRLF/LF、多别名、递归 Include、相对/绝对路径、`~`、环境变量与通配符，检测循环引用并跳过通配符/否定 Host 及条件 Include。
- 默认目录继续由系统 OpenSSH 自动读取配置；自定义目录保存本机配置文件引用，连接测试、远程目录查询和 PTY/daemon 终端启动统一使用 `ssh -F`。配置路径不参与同步、备份导出或普通日志。

### 国际化 / 繁体中文支持

- 设置中的界面语言新增 `繁體中文`，持久化语言偏好与自动检测同时支持 `zh-CN`、`zh-TW`、`en-US`，并识别 `zh-HK`、`zh-MO`、`zh-Hant` 等繁体地区或脚本标记。
- 前端国际化以现有 `zh-CN` 文案为基底，通过 OpenCC 在运行时映射生成 `zh-TW`，避免重复维护两套中文词典；现有英文文案继续独立保留。
- 设置、统计看板、历史会话、Prompt Library、状态栏配置、桌面宠物联动、关闭按钮两类弹框及常见通知文案切换到繁体时不再误回退英文；桌宠窗口内的状态标签、操作按钮、宠物名称和目标标题也会随 `zh-TW` 转为繁体显示，并对少量简繁同形词做繁中术语覆写（例如状态“工作中”）。日期、时间和数字格式按当前语言 locale 输出，并继续强制 24 小时制时间显示。

## [V1.2.9] - 2026-07-18

### 桌面宠物

- 提供独立悬浮窗桌面宠物（窗口标签 `desktop-pet`），可在主应用外常驻显示；设置页可启用/关闭、选择宠物、调整尺寸（小/中/大）、置顶、锁定位置、显示状态文案与会话名、终端全屏时自动隐藏，以及重置/记忆窗口位置。
- 宠物情绪跟随终端 Tab 与 daemon 后台任务状态：执行中 → working、待处理 → waiting、完成 → success、失败 → error、空闲 → idle、无可用任务 → sleeping；同时汇总运行中/待处理数量，并提供任务列表点击跳回对应会话。
- 状态推导综合打开终端、持久化会话、后台任务与 PTY 输出活跃度；主窗口通过配置/快照事件与宠物窗同步，宠物窗可打开主窗口、打开设置或隐藏自身。
- 桌宠窗口按状态气泡和宠物舞台的实际渲染边界动态适配尺寸，支持顶部/水平内容扩展、DPI 缩放、工作区边界限制与恢复基础窗口大小；动态调整保持宠物底部中心和用户位置锚点稳定，菜单展开时不与菜单窗口几何计算冲突。
- 右键菜单采用紧凑纵向操作区和向左展开的毛玻璃列表；远程托管按“选择已启用平台 → 选择已停止会话”操作，支持 Telegram、飞书、微信与企业微信。本地会话支持 Codex、Claude Code、Pi 和 OpenCode，显示 Agent、Provider/配置来源与 OpenCode 审批能力提示；SSH 仍仅支持 Codex，Grok Build、WSL 和普通终端不进入托管。
- 支持 `.clipet` 宠物包（ZIP + `manifest.json` + 资源），引擎含 `image-v1` 与兼容 Codex Pets 的 `codex-sprite`；仅接受 png/webp/经清洗 svg，限制包大小与条目数，拒绝 HTML/JS/可执行文件、符号链接与路径穿越。
- 设置页图鉴支持远程目录、本地缓存与内嵌兜底，可按 SHA-256 / 大小 / 最低应用版本校验后下载安装；支持本地导入 `.clipet`、卸载 CLI-Manager 管理的宠物。内置 `builtin.cli-cat`，预置 terminal-robot / pixel-fox / mint-slime。
- 桌宠 ID 必须是受限字符组成的单个普通路径组件；安装清单、目录读取与卸载统一拒绝 `.`、`..` 及其空白变体，递归卸载不会逃逸受管理安装根。
- 可发现本机 Codex Pets 目录中的外部宠物并加入已安装列表；外部 Codex 宠物只读展示，不支持应用内卸载。
- 工作状态上下抖动可单独开关，幅度 0–5 px（默认关闭）；关闭或设为 0 时保留宠物包自身动画，不叠加整体位移。

### 退出与后台任务

- “退出时也检测已完成的任务”默认关闭；开启后，退出检测会额外纳入 Claude/Codex Hook 明确标记为已完成或失败的会话，并允许保留到 daemon 后台任务中心。普通 Shell 命令结束、待处理通知和非 PTY 视图不会被误判为已完成 CLI 任务。

### Worktree 今日项目用量

- 终端实时统计中的今日项目用量按父项目路径与全部 active Worktree 路径一次聚合；主项目 Tab 和同项目 Worktree Tab 展示相同合计。后端按唯一历史会话统计，父目录包含 Worktree 时不会重复计算，当前 checkout 尚无最新历史会话时仍可展示项目级用量。
- 今日项目用量按项目 ID、来源和完整项目/Worktree 路径作用域缓存；切换项目时先显示对应项目的最近有效结果，后台刷新不会串显其它项目。

### 文件浏览器 Gitignore

- 文件浏览器优先读取项目根 `.gitignore`，支持标准的根路径锚点、任意层级目录规则、`*` / `**` 通配和 `!` 否定规则；Windows 本地与普通 UNC 项目按大小写不敏感匹配，WSL/POSIX 项目保持大小写敏感；命中忽略规则的目录与文件都留在文件树原位，仅整行降低透明度表示忽略态。
- 项目 watcher 报告 `.gitignore` 创建、修改或删除后会立即重载匹配规则；文件缺失或不可读时回退到内置缓存、构建产物和常见临时文件规则，手动忽略路径与默认折叠目录名继续叠加进淡化判定。

## [TEMP] - 2026-07-13

### SSH 远端历史

- SSH 项目的会话历史能力与远程桥接来源一致：仅 Claude Code 和 Codex CLI 可读取远端历史；SSH Grok Build 点击历史会显示“Grok 暂不支持查看会话历史”的本地化提示，其他未支持 SSH CLI（含 Kimi Code）显示通用提示，不再暴露内部错误。本地与 WSL 的 Grok Build、Kimi Code 历史保持可用。
- SSH Claude/Codex 项目可从统一历史工作区读取已绑定远端目录中的会话，支持分页列表、搜索、消息/工具调用/子 Agent 详情、Diff 与 usage 摘要；详情按需通过复用 Agent bridge 分块传输，列表摘要和统计事实进入现有 `history-catalog.db`，断线时明确显示 stale/offline 状态。
- 远端 Agent 只维护可重建的增量派生索引，使用 machine/user/source/config root 作为稳定来源身份，并通过跨进程单写锁、游标、轮转与 tombstone 避免重复全量扫描；不复制完整历史目录，不允许远端路径进入本地文件、Git、供应商、编辑或删除 API。
- 已有完整远端索引的打开与翻页直接读取已发布分页，不获取写锁、不重新扫描目录或改写索引；本地已有摘要时立即展示并后台刷新，无本地摘要时等待 Agent 首批结果。首次或增量构建优先最近修改的会话，无变化不重写索引。
- 本地 `history-catalog.db` 会随 schema 升级补齐会话关系、消息分片和工具事件外键支撑索引，减少本地索引刷新接近完成时的长事务写锁对 SSH 远程历史落库的阻塞。
- SSH 项目历史入口统一使用远程项目路径和远程 sourceInstance 过滤；本地收藏快照只作为本地历史缺失文件的兜底，不会混入远程项目历史列表。
- SSH 项目历史用量统计默认从已落库 catalog 与聚合缓存读取，按远程 `sourceInstanceId` 和项目路径隔离；只有用户手动刷新才强制同步远端索引，避免打开统计看板反复重扫历史并拖慢会话列表。
- 应用启动后立即打开 SSH 项目历史时，远程历史命令会等待 PtyHost daemon 就绪；远程项目空列表会自动补一次强制远端刷新，手动刷新仅刷新远程历史，不触发本地历史项目同步候选提示。
- SSH Agent 重装或升级只轮换当前安装身份，不改变由 machine/user/source/config root 构成的稳定远程历史来源；启动后可继续复用已有 catalog，并原子更新最新安装元数据。
- 远程历史普通详情在没有 Hook transcript 引用时通过 Agent 索引定位；Desktop 在线路上固定发送非 null 字符串，兼容已发布 Agent `0.1.3`，无需先升级 Agent 即可打开详情。
- 远端历史支持同源恢复：Agent 重新校验原 JSONL、远端 cwd、Host/用户/config root 与 Claude/Codex 可执行文件；匹配项目时创建新的交互式 SSH PTY，无匹配项目时可使用原远端目录。当前客户端已有同一会话会直接跳转，其他 consumer 正在运行时阻止并发恢复。

### 历史项目同步

- Codex / Claude 历史项目同步弹窗支持对已选择项目批量忽略并二次确认，也支持在单个项目行直接忽略；同步成功的项目会自动进入持久化忽略列表，后续同步扫描不再展示或提醒这些项目。

### 历史会话消息选择

- 会话消息批量删除模式支持点击整条可编辑消息进行选中或取消，保留选择框状态提示，并支持 Enter / Space 键盘操作；普通浏览模式和不可编辑的结构化消息不受影响。

### 历史会话恢复

- 恢复会话的项目配置选择弹窗支持按目录分组展开与折叠；输入搜索条件时自动展开匹配分组，关闭按钮使用轻量圆形图标样式。

### Hook 与 cc-switch 跨环境兼容

- Claude Code / Codex Hook 配置目录和 cc-switch 数据库可独立位于 Windows、WSL、macOS 或 Linux 的本机环境；Hook 命令按各 CLI 目标环境生成，数据库访问按数据库路径选择运行时。
- Windows 版可读取和更新 WSL 内的 `cc-switch.db`：读取通过对应发行版内 Python SQLite 在线备份后由 Rust 解析，Hook 与状态栏通用配置写入在 WSL 内使用事务和旧值校验完成，不通过 UNC/Plan 9 直接写 SQLite。
- WSL 中缺少 Python SQLite 运行时或发生并发更新时返回可诊断错误，不自动安装依赖、不回退到不安全写入。

### 历史用量与请求日志

- 历史用量分析使用应用窗口内全屏工作区，保留项目和时间筛选、日期下钻及既有统计能力；Token / 费用趋势独占中间整行，底部依次展示项目排行、模型排行、24 小时活跃和 Token 构成。
- 新增“请求日志”页签，将本机 Claude/Codex CLI 会话日志中的 usage 事件增量写入 CLI-Manager 自身 `request_logs` 表；应用启动同步一次，运行期间每 60 秒同步，并支持手动刷新。
- 请求日志支持来源、项目、模型、会话关键字和日期范围筛选，提供今天、近 7 天、近 30 天快捷日期和可折叠高级条件；紧凑工具栏、五项同排自适应汇总卡片、吸顶表头、列内容居中、来源品牌图标、异常用量提示、来源/项目/模型快捷筛选，以及合并在固定操作列中的会话 ID 复制与历史会话跳转提升高密度日志检索效率。仅保存 usage 元数据，不保存 Prompt、响应正文、密钥、伪 HTTP 状态或伪耗时。
- 路由开启时，真实路由响应是请求用量权威源；同一来源、会话、模型、输出及缓存归一化输入匹配的本地会话记录仅作回退，不重复进入请求列表、汇总卡片或历史统计。
- 请求日志同步不调用、不读取、不依赖 cc-switch；费用按 CLI-Manager 当前模型价格动态计算，未匹配价格的 Token 单独标记为未定价。
- ccusage 看板改为应用窗口内全屏布局，仅重排现有趋势、峰值摘要、Token 构成、热点图和模型排行，不增加功能、指标或数据源。

### Claude 状态栏

- 设置页提供内置 Claude 状态栏可视化配置器，支持完整组件目录、最多三行布局、组件搜索、按当前选中行点击添加、从组件库拖到指定行或组件位置、行内删除、跨行拖拽和属性区跨行移动，以及颜色/粗体、自定义文本/符号/命令、Powerline 与实时预览。
- CLI-Manager 可执行文件通过隐藏的 `__statusline` 子命令直接接收 Claude Code 状态 JSON 并输出状态栏，无需额外安装 Node、Bun 或 ccstatusline-zh。
- 支持检测、安装和卸载 Claude `statusLine`，写入时保留其他 Claude 设置并创建备份；支持一次性导入 `~/.config/ccstatusline/settings.json` 的旧配置。
- 支持可视化编辑 Codex 原生 `/statusline` 对应的 `[tui].status_line`：选择模型、上下文、5 小时/周额度、Git、Token、权限、会话和任务进度等项目，调整显示顺序并预览后写回 `config.toml`；其他 Codex 配置保持不变。
- Codex 状态栏支持 Codex 当前官方项目及兼容别名；配置加载失败时会将内部错误码转换为中英文可读提示，说明 `[tui].status_line` 的兼容问题、配置检查位置和修正方式；加载中或加载失败时不再误显示状态栏已关闭，未知项目会保留在编辑器中供移除。
- 状态栏设计器将 Claude Code 和 Codex 分成独立工作区：Claude 使用三行组件画布和属性编辑，Codex 使用原生项目选择与拖拽排序；共享实时预览支持 60–180 列宽度和全部内置终端主题，同时页面控件自动适配当前应用主题与配色。
- Claude Code 与 Codex 分别维护多份命名状态栏配置，支持保存即应用、复制、重命名、切换和删除非当前配置；首次进入自动采纳已有实际配置，检测到外部修改时可另存为新配置。
- 软件内文本输入与确认操作统一使用跟随应用主题的内部弹窗，覆盖状态栏配置、导入冲突、未保存内容切换、配置删除、字体安装和终端背景移除，不使用 WebView `window.prompt` 或 `window.confirm`。
- 支持将 Claude/Codex 状态栏配置库整体导出为版本化 JSON，并在导入时逐项选择覆盖、跳过或重命名；导入不会自动切换当前配置，也不会携带供应商密钥或其他 CLI 设置。
- Claude/Codex 实时预览使用本地化状态名称说明各项含义；Codex 预览位于原生状态栏标题下方。Claude Powerline 支持字体检测与用户确认安装、分隔符、起止端帽、组件对齐、跨行主题延续及 10 套内置主题，颜色选择显示色块和中英双语名称。
- Claude Code 实际状态栏为数据组件显示中文短标签，Git 分支使用 `⎇` 图标；上下文组件以 16 格进度条展示已用量、上下文上限和使用百分比；Powerline 首组件左侧保留三个空格，其他间距保持紧凑。
- Hook 与状态栏会先写入本地 CLI 配置，再自动尝试写入 cc-switch 通用配置；通用配置指 cc-switch 在切换供应商时合并到所有启用“应用通用配置”的供应商里的共享片段。未配置或未检测到 cc-switch 时，本地配置文件写入仍作为托底生效。
- 安装、卸载、保存或切换 Claude 状态栏时会同步 CC Switch `common_config_claude`，切换供应商后仍保留 CLI-Manager 状态栏；同步仅处理带 `__statusline` 标记的配置，不覆盖其他通用字段、Hook 或第三方状态栏。Codex 原生状态栏会同步到 `common_config_codex` 的 `[tui].status_line`，并保留现有 `[features]`、Hook 信任块和其他 TOML 表。

### 终端文件路径快捷打开

- 终端输出中的 Windows、UNC/WSL UNC 及可转换的 Linux 绝对文件路径支持 `Ctrl + 单击`；路径尾部的 `:行号`、`:行号:列号` 及其后的源码片段会被自动剥离；悬停时按真实目标类型显示文件或文件夹图标，打开后统一使用 Windows 资源管理器定位目标。

### 终端渲染稳定性

- 终端采用 VS Code 风格分层：React 终端宿主统一通过 `TerminalProcessManager` 访问 PtyHost；WebView 使用鉴权二进制 WebSocket 接收输出与 Replay，高频输出不经过 Tauri Base64 事件。
- daemon 对输出进行 5ms 合并并使用序列号、字符 ACK 与 100000/5000 高低水位背压；达到高水位时只暂停对应慢客户端的实时投递，PTY 输出继续进入有界 SessionBuffer/spool，ACK 降到低水位后按 sequence 补发；attach Replay 记录输出发生时的列数、行数和序列，按 Replay 后实时输出的顺序恢复。
- Attach 使用 replay/attached/live 写入屏障；XTerm 尚未提交的帧由 `TerminalProcessManager` 跨组件重挂持有，只有 xterm write callback 完成后才按序 ACK。初始与断线重连 Replay 都按历史 resize 串行恢复，完成当前容器 fit 后才继续 live 输出，历史尺寸不会写回 live PTY。
- daemon 每客户端使用独立 writer queue，慢客户端不会持全局锁阻塞其他会话；内存 Replay 超过 2 MiB 后将更早的完整安全帧写入会话 spool，关闭会话或启动新 daemon 时清理，不静默裁剪活跃会话输出。
- WebSocket 具备鉴权、请求、心跳超时与自动重连；关闭请求先建立 tombstone，失败或断线后不会把已关闭 Tab 重新 attach，最后会话关闭时同步取消待执行重连，避免残留空闲连接与心跳。UI 会话对账不终止 daemon 后台任务。
- 隐藏终端持续交给 xterm 解析输出，不再保存隐藏输出后批量重放；全局终端 scheduler 在 document hidden 或 rAF 停滞时使用 timer/watchdog 回退，恢复可见时重新调度，且仍只在 xterm write 回调后提交 ACK；低内存模式只延迟释放 WebGL，切回时恢复渲染与视口。大缓冲区纵向 resize 立即生效，横向 reflow 采用最长 34ms 的首帧 + 尾帧实时节流；macOS/Linux 同步重排光标所在逻辑行。快速缩小时会在每次实际 resize 前保留最近一张正确画布并随容器实时拉伸，PTY 输出提交后更新该画面，尺寸稳定 72ms 后再显示底层渲染器，避免暴露 xterm/WebGL 把左侧内容短暂画到右侧的中间帧。
- 横向 resize 前会为用户当前查看的 normal-buffer 首行建立临时 marker，等待 xterm 完成异步视口同步后恢复到同一逻辑文本位置；实时底部跟随和 alternate buffer 不受影响。
- 拖动顶层 Workspan Tab 悬停其他 Workspan 500ms 后会自动激活目标，可继续拖到目标 Pane 边缘完成左、右、上、下分屏；Pane 内 Terminal Tab 继续使用相同的中心移动与边缘分屏落点。
- 普通 Tab、Workspan 或隐藏终端切回时先等待 xterm 自然恢复完整 viewport；两帧内没有完整渲染才触发强制刷新，并在恢复期间保持绘制层遮罩，避免大片空白或每次切换都逐行重绘。
- 左侧边栏拖动期间直接预览 DOM 宽度，松手后才提交 React 状态和持久化；终端 Pane 不再对 left、top、width、height 使用几何动画，降低连续 ResizeObserver 与 xterm reflow 开销。
- Windows PTY 直接调用 ConPTY API，并继续支持兼容 ConPTY DLL；macOS/Linux 使用 `openpty`、控制终端和进程组。生产路径不再依赖 `portable-pty`。

### SSH 远程项目与主机管理

- 设置页新增 SSH 主机管理，支持地址连接和 SSH Config 两种来源；认证支持 SSH Agent、私钥、用户/密码、每次询问密码和 Keyboard-interactive/MFA。用户/密码默认保存到系统凭据库，系统 OpenSSH 通过一次性 AskPass 通道读取，密码不进入 SQLite、日志、同步数据或命令行。
- SSH 主机支持多级分组树展示，可新增根分组和子分组；新增/编辑主机弹框固定高度，顶部标签随滚动联动，连接来源切换不丢失已输入字段，测试结果显示在测试按钮旁并按成功/失败/等待使用不同颜色。
- “新增终端”支持本地项目 / SSH 远程项目切换；SSH 远程项目可选择任意支持的 SSH 主机认证方式，包括用户/密码，并支持远程路径检测和目录浏览。项目列表保持原有分组结构，不额外显示 SSH 徽章。
- SSH 远程终端由 Rust 结构化 Launch Plan 生成系统 OpenSSH 参数，可在远程目录启动 CLI、使用分屏与 Workspan，并由 daemon 托管与恢复。远端 Claude/Codex Hook 已通过显式安装的 `cli-manager-ssh-agent` 接入；SSH 文件面板支持 Agent 约束下的只读树、文件名/代码搜索、文本/图片预览和远端路径复制，Git 面板与本地/WSL 共用完整交互，支持仓库切换、状态、Diff、暂存/取消暂存、回滚、提交、Fetch/Push/Pull、分支与冲突处理；终端实时统计与今日项目用量复用远端历史 Agent/catalog，Worktree 和远程资源监控仍保持禁用或降级提示。

## [V1.2.7] - 2026-07-11

### Linux 图形兼容与分发

- Linux 启动会在 WebKitGTK 创建前解析图形策略；自动模式仅在 Wayland + NVIDIA 专有驱动环境设置 `__NV_DISABLE_EXPLICIT_SYNC=1`，并支持系统默认、禁用 DMABUF 和禁用加速合成三级手动降级。用户已有的标准 WebKit/NVIDIA 环境变量优先，不会被应用覆盖。
- 开发者设置在 Linux 下提供图形兼容模式和可复制诊断，显示会话类型、桌面环境、NVIDIA 判定、请求模式、实际模式和已应用 workaround；修改模式后重启生效。
- Linux 主窗口默认在首屏就绪后显示；启动超过 3 秒会显示加载阶段，单阶段超过 15 秒展示可重试错误，不再以白屏或黑屏代替启动反馈。
- 终端 WebGL context loss 后回退到默认渲染器；NVIDIA Wayland 或显式 WebKit 降级模式下，隐藏终端延迟释放 WebGL 资源但保留 PTY、scrollback 和后台输出缓冲。
- 仓库提供 `cli-manager-bin` AUR 包装模板。AUR wrapper 会把安装标记为包管理器渠道，禁用应用内下载安装，升级由 AUR helper 或 pacman 负责。

### PTY 守护进程与后台任务

- 应用启动后会连接或拉起 `cli-manager-daemon` 独立守护进程；内置终端 PTY 可由 daemon 托管，应用真退出后任务继续运行，重开应用后优先 attach 原会话并按“已有回放 -> 恢复期间实时输出”的顺序恢复画面，不重跑启动命令、不重复 resume。
- daemon 与安装包一起作为 sidecar 分发，Windows、macOS、Linux 用户不需要单独下载；开发版和安装版使用独立发现文件，互不 attach。
- Windows 下 daemon 使用 Job Object 兜底治理子进程树；daemon 正常退出或被强杀时，系统会回收其托管的 PTY 子进程，降低孤儿进程风险。
- 退出应用时如存在运行中任务，会在一个弹窗中提供“退出应用，任务在后台继续执行”“最小化到托盘”“终止任务，退出应用”：后台继续会真退出 UI 并保留 daemon 任务，最小化到托盘不关闭任何终端，终止任务会关闭 PTY 并清理恢复快照但不删除 Claude/Codex 原始历史。
- 终端右侧工具栏提供后台任务中心；入口可通过“设置 -> 侧边栏 -> 终端工具栏”控制显示和排序，默认在无后台任务时禁用、有任务时显示呼吸提示。
- 后台任务中心展示任务名称、项目、创建时间和任务状态，支持恢复、丢弃或删除；恢复后的 Claude/Codex Tab 保留对应 CLI 图标，任务完成后仍保留在列表中，只有用户手动恢复、删除或丢弃后才消失。
- 后台任务状态由 daemon 根据 hook 上报维护，区分执行中、待处理、已完成和失败；PTY/shell 是否仍存活只作为兜底，不再把 shell 存活误判为任务仍在执行。
- Claude/Codex hook 会上报到 daemon 的稳定 hook 端口；应用未运行时 daemon 缓存最近 hook，PermissionRequest 可唤起应用并定位到对应后台会话。

## [V1.2.8] - 2026-07-09

### 历史会话消息编辑

- 历史会话详情按钮与列表右键菜单恢复会话时，会匹配来源和目录对应的项目配置并携带其 CLI 启动参数、供应商覆盖、环境变量和 Shell；多个匹配配置通过支持搜索与分组的项目选择弹窗确认后再启动；跨 CLI 转换后若没有同来源配置、但会话 `cwd` 唯一命中本地或 WSL 项目目录，会直接在该目录执行目标 CLI 的裸恢复命令，不继承原 CLI 配置；其他无匹配场景展示全部项目和“使用新窗口”。
- 历史会话详情的聊天气泡支持消息级操作：悬停显示复制、编辑、删除和"在此后插入"操作栏；工具调用、思考等结构化消息只提供复制，不开放编辑入口，避免破坏 tool_use/tool_result 协议配对。
- 编辑在气泡内以行内文本框完成（Ctrl+Enter 保存、Esc 取消），保存后直接写回 Claude / Codex 本地会话 JSONL 原始行：仅替换文本块内容，uuid、parentUuid、usage、图片等其他字段与非文本块原位保留；`claude --resume` / `codex resume` 后 AI 基于修正后的历史继续。
- 删除消息会自动修复 Claude parentUuid 会话链（后继消息重链到被删消息的父节点）；Codex 会同步删除配对的 `event_msg` TUI 重放行。
- 支持批量删除：详情页顶部进入选择模式后勾选多条消息一次性删除；批量删除为单次原子写回，任一目标行校验失败整批拒绝，连续删除时 Claude 会话链跨代上溯重链到幸存祖先，Codex 同步移除全部配对行。
- 插入消息可选用户 / 助手角色，在锚点消息之后注入上下文：Claude 侧生成新 uuid 并接管锚点子链，Codex 侧同步写入 `response_item` + `event_msg` 双行，保证模型上下文与 TUI 重放一致。
- 首次修改某个会话文件前自动备份原文件到 `.cli-manager/history-backups/`；详情页"编辑记录"弹层以字符级 diff 高亮（删除红色删除线、新增绿色底色，−/＋ 行符）展示每次操作差异，支持单条撤回（编辑恢复原文本 / 插入移除消息 / 删除就近恢复消息），也支持一键还原到编辑前的原始文件。
- 双守卫防写坏：写入前校验文件指纹（外部改动过则拒绝并自动重载会话）与目标行角色 + 规范文本（行号漂移即拒绝）；写回使用临时文件 + 原子替换。
- 正在终端中运行（hook 绑定 sessionId）的活跃会话，编辑 / 删除 / 插入前会弹出警告确认；收藏快照兜底的只读会话不提供编辑入口，收藏会话编辑后快照自动同步。

## [V1.2.6] - 2026-07-06

### Codex / Claude 历史同步

- 首次启动且尚未维护项目时，应用会延迟扫描本机 Claude / Codex 历史项目，并弹出同步选择窗口供用户确认要纳入管理的项目；已维护项目列表时启动不再自动扫描或提示。
- 同步记录会持久化保存；后续在历史会话列表点击刷新时会同时检测缺失的历史项目，也可移除已同步历史记录。
- 左侧项目列表支持展示同步来的历史分组；这些分组可打开聚合同项目历史上下文的只读终端。
- 同步历史终端会先写入聚合上下文，再发送对应 `claude` / `codex` resume 命令，避免上下文输出与启动命令抢写。
- 同步逻辑会过滤 CLI-Manager 自己生成的同步中间会话，避免重复导入。

### 用量分析与模型价格

- 历史统计、实时统计和今日项目用量按本地模型价格表估算费用；裸 GPT 版本模型带 reasoning effort 时会按 `模型名(effort)` 独立价格处理，例如 `gpt-5.4(high)`、`gpt-5.6(xhigh)`；`gpt-5.3-codex-spark` 这类带用途后缀的模型保留原模型名。未配置对应价格时计入未定价 Token。
- 实时统计和历史详情侧栏的“今日项目用量”会展示总 Token、费用，以及输入、输出、缓存命中、缓存写入和未计价 Token 明细，便于判断费用偏低是否由缓存命中或未计价 Token 导致。
- 模型价格页支持识别本地模型后只同步缺失价格，或按当前筛选/搜索范围同步；远程基础模型价格可作为 effort 变体候选手动确认，不会自动套用。

### 终端显示与启动兜底

- 关闭后恢复工作区会话：再次打开时若检测到上次遗留的终端标签，会弹窗询问是否恢复。确认后一次性还原全部标签（顺序、选中项、关联项目一致）；可在设置 -> 开发者关闭该功能，关闭后启动时清理当前环境快照且不再提示。开发版使用独立 `sessions.dev.json`，不会读取或覆盖安装版 `sessions.json`。
- 终端设置页按“终端行为 / Shell 终端类型 / 终端主题库 / 终端背景”组织展示，减少长设置页滚动；终端主题库使用浅色 / 深色 / 跟随系统分段 Tab，浅色与深色为独立终端主题筛选，跟随系统会使用当前系统主题色配置对应的终端配色；宽屏右侧的终端预览固定展开，窗口变窄回到单列/中间区域时会进入“终端主题库”分组并显示在主题颜色板前，便于直接查看当前主题和字体效果；Shell 终端类型会按当前 Windows/macOS/Linux 平台扫描，只有启用且可用的项才显示在新建/编辑项目 Shell 下拉中，并支持添加名称 + 可执行文件路径的自定义终端。
- 终端行为提供默认开启的“Workspan开发”开关：开启时使用顶层 Workspan 工作区；关闭时恢复单一 Pane 树和 Pane 内终端 Tab。关闭会保留活动 Workspan 的分屏并把其他会话追加到活动 Pane，不重建 PTY；再次开启时当前布局整体作为一个 Workspan。
- “终端 Tab 悬浮信息”开启后，Pane 内终端 Tab 与单终端 Workspan 顶层 Tab 均显示 CLI、Shell、项目、路径和 Session ID，悬浮卡片样式跟随当前终端主题，各字段带语义图标，已知 CLI 显示厂商图标，路径与 Session ID 可复制完整值；多终端 Workspan 不展示单一 Session 信息。
- Windows 内置终端会随包提供新版 Windows Terminal `conpty.dll` 与 `OpenConsole.exe`；开发者设置中的“Windows 兼容修复”默认开启时，直接 ConPTY 适配器优先加载该兼容运行时，降低 Codex 清屏重绘导致滚动条/回滚历史缺失及内部终端光标乱闪的概率。该开关仅 Windows 显示，已有明确配置仍保持原选择，修改后需重启生效；其下方另有默认关闭的“隐藏CODEX运行时光标”开关，开启后仅对 Codex 会话延迟合并光标显示序列，首帧识别和重新聚焦也会继续维持隐藏，减少执行期间闪烁。
- 内置终端支持输入提示：根据本地命令历史、命令模板、内置 AI CLI 命令和当前目录路径候选在光标后以内联灰字显示建议后缀，仅在当前输入位于光标前且光标后没有已有内容时显示，支持 `cd D:/work/`、`ls src/` 等目录/文件补全；有候选时 `Tab` / 方向键右键 / `Ctrl+Space` 只补全后缀、不自动回车执行，无候选时 `Tab` 与方向键右键继续交给 shell/CLI。
- 输入提示内置只读 AI CLI 命令库，覆盖 Claude Code、OpenAI Codex CLI、Gemini CLI、Aider、OpenCode、GitHub Copilot CLI 等常用 AI 开发命令；内置命令仅作为候选源，不写入用户命令模板。
- LLM 二阶段推测底层能力暂时保留但不在设置中开放，启动时会回落到本地提示来源，避免不成熟的模型推测影响日常输入。
- 设置侧栏在 Hook 下方提供独立“命令提示”页，可开关终端输入提示，查看本地命令历史已存指令数和估算占用空间，并一键清理命令历史存储；终端输入不再新增应用命令历史。
- 终端分屏 Pane 尺寸变化、Pane 放大/还原、终端侧栏打开与宽度变化带轻量过渡；拖拽分隔条、终端 Tab、Workspan、工具栏或侧栏宽度时保持即时响应。
- 顶层 Workspan Tab 支持右键关闭、通过应用内弹窗设置持久化自定义名称，以及按左右位置批量关闭；提交空白名称时恢复单会话终端标题或多会话 `Workspan · N`。Tab 全部可见时不显示右侧下拉按钮，Tab 过多时隐藏原生横向滚动条，并通过右侧下拉列表只展示当前视口外或被裁剪的工作区；切换后当前激活 Tab 会自动滚入可视区域，同时保留鼠标横向滚动浏览；当前 Workspan 内无其他可切换会话时，鼠标前进/后退侧键与 Tab 切换快捷键会切换相邻 Workspan。
- 终端字体大小可通过 `Ctrl + 滚轮` 和设置页调整。
- 终端右键粘贴、`Ctrl+V` 与 `Ctrl+Shift+V` 使用 Tauri 原生剪贴板文本读取，避免 WebView 剪贴板授权弹框影响粘贴；复制能力保持原有路径。
- 终端侧边栏可打开系统资源监控，面板深色下使用中性灰白与绿色/黄红语义色，展示 IP 复制、运行时间、CPU 趋势与按需展开的核心占用、GPU（可用时）、内存可用/已用/缓存/空闲、网络双向上/下载趋势与今日累计流量、合并磁盘容量/读写速度和高占用进程排行；进程排行优先展示软件名称和软件图标，识别失败时回退到命令；CPU 核心占用展开后按面板宽度自适应排布，隐藏的资源卡片不会触发对应采样，关闭 CPU 卡片后底部 CPU 小猫也不再独立轮询。
- 内置终端接入 Unicode 11 宽字符表，改善中文、Emoji 等宽字符在 Claude / Codex TUI 中的对齐和显示。
- 内置终端会去重 xterm 正常输入事件与 Windows IME 原生 `insertText` 兜底在极短时间内提交的同一段中文文本，避免中文输入法重复上屏，同时保留中文标点兜底输入。
- 应用初始化或界面渲染失败时，会展示带错误详情的失败页，便于复制报错继续排查。
- 应用与 PTY daemon 使用独立的 `~/.cli-manager/logs/crash.log` 滚动日志记录 Rust panic、React/浏览器未捕获异常、Promise 拒绝、WebGL context loss 和启动失败；异常终止会在下次启动通过运行标记补记，并对终端、文件和窗口上下文做长度限制与敏感信息脱敏。
- 应用设置、会话恢复、同步配置和外部会话同步记录统一保存在用户目录 `.cli-manager` 下；安装版会话恢复使用 `sessions.json`，开发版使用 `sessions.dev.json`。从旧 Tauri 数据目录迁移时只补齐缺失配置，不覆盖已有用户设置，并会在恢复 SQLite 项目/模板数据前备份当前空库。
- 启动加载项目列表前会预检并修复已知 SQLite migration 13-15 分支编号漂移，避免迁移记录校验失败导致“项目加载失败”。
- 项目树加载失败时，界面只展示可读提示，原始 SQLite/Tauri 错误会写入日志，避免直接向用户显示英文底层错误。
- 项目树支持在滚动列表中低延迟拖拽排序；跨分组拖拽到已有节点时显示插入位置预选，并按预选位置落位；拖拽结束不会触发项目选择，避免误切到带“路径不存在”提示的项目。
- 普通 WebView 诊断日志跟随“调试模式”输出，关闭调试模式时减少控制台噪声。
- 开发者设置集中管理终端会话恢复、Worktree开发、软连接兼容性、低内存模式、禁用硬件加速和调试模式；终端会话恢复与 Worktree开发默认开启。关闭 Worktree开发后，新增/编辑项目隐藏对应配置区域，同项目已有终端时再次打开也不会提示或自动隔离。
- 应用会低频对账前端终端会话和 daemon PTY 会话；后端仅清理持续失联且由 PtyHost 持有的 PTY 根进程树，避免按进程名误杀外部任务。

### Hook 通知

- 系统级 Hook 通知使用短正文并截断过长详情，避免 Windows 通知内容不需要滚动时仍出现滚动条。
- Windows 任务栏提醒与系统级 Toast 使用独立总开关，共享每种 Hook 事件筛选；任务栏可设置有限闪烁次数或持续到窗口聚焦，并在聚焦时主动停止。
- Hook 设置支持“窗口聚焦时不发送系统通知”开关；开启后 CLI-Manager 主窗口已聚焦时只显示应用内通知，关闭后即使窗口聚焦也会按系统通知设置发送操作系统级通知。
- Windows 本地 Hook 系统通知支持选择、试听和清除全局自定义 WAV 声音；选择按钮旁明确提示仅支持 `.wav` 格式，声音路径仅保存在本机，文件失效时回退系统默认声音，不影响通知点击、应用内 toast、任务栏提醒、第三方通知或 WSL fallback。
- 应用内 Hook toast 会取消外层通知容器的滚动裁剪，保持卡片自身控制文本省略和圆角裁剪。
- Hook 设置页支持“三方 Hook 通知”：可配置多组 DingTalk、Feishu、WeCom、Bark、PushPlus、WxPusher、ServerChan、Telegram、ntfy、Gotify 和 Custom HTTP 目标，按事件筛选并测试发送。
- 三方 Hook 通知支持全局启停开关；关闭后保留所有目标配置和凭证，不自动派发，方便随时恢复。
- 第三方通知由实际接收 Hook 的主进程或 daemon 异步派发；daemon 托管后台任务时，即使前端未连接也能继续发送命中的第三方通知。
- 第三方通知只发送带 emoji 的安全摘要（CLI 来源、项目名、事件、时间、通知 ID），不发送 Prompt、终端输出、绝对路径、session/tab id 或 transcript；失败不会影响本地 Hook 状态、toast 或系统通知。
- 三方 Hook 通知的全局开关、目标、事件筛选和完整平台凭证可通过 WebDAV 或本地 zip 同步；下载时可独立选择是否恢复该域，旧快照缺少对应字段时保留本地配置，远端目标恢复前会过滤未知平台、重复 ID、畸形记录和超过 20 个的记录。

### 历史会话收藏

- 历史会话点击收藏时，会把完整会话详情快照写入本地 SQLite。
- 原始 Claude/Codex 历史 JSONL 被删除后，已收藏会话仍会在历史列表中展示，并可打开收藏时保存的只读快照。
- 取消收藏会同步删除该会话快照。
- 历史会话侧栏支持只看收藏；开启后列表和全局搜索结果都只显示收藏会话。
- 历史会话列表按 20 条分页加载，继续滚动或点击加载更多会追加下一批。
- 历史会话列表优先从独立 SQLite 摘要缓存加载，后台按文件指纹增量同步新增、变化和删除的 Claude/Codex 会话，不再在首屏请求中全量解析 JSONL。
- 历史会话全文搜索使用 FTS5 trigram 索引，输入至少 3 个字符后开始搜索；首次建索引时显示进度并允许查询已完成部分，索引完成后自动刷新结果。
- 历史会话列表会把图片型首条消息标题显示为 `[图片]`；多张图片显示为 `[图片1][图片2]`，并补充同一条消息中的剩余文字，且会忽略图片闭合标签和重复图片前缀。
- 历史会话列表的展开、关闭和 Subagent 标识使用轻量图标样式。
- 历史会话列表支持主会话与 sub-agent 层级树；Codex rollout 从会话元数据读取父线程关系，Claude 继续兼容 `subagents/agent-*.jsonl` 路径关系。
- 历史会话原文中的本地图片标记会优先以内联缩略图展示，点击可放大预览，预览中按 Esc 会优先关闭图片，并保留图片标记后的正文；图片缺失或读取失败时降级显示原始标记文本。
- 历史会话详情支持在同一行编辑会话别名、标签并保存；标签输入可选择已有标签，也可继续手动输入新标签。
- 历史会话全局搜索支持直接按 `sessionId` 定位 Claude / Codex 历史会话。
- 历史会话详情“变更”页读取当前会话 JSONL 中的结构化文件操作，支持还原 Codex 工具调用中的转义 Patch，并按操作时间列出真实文件路径和增删行数；文件图标与文件浏览器一致，彩色 Tag 区分新增、修改、删除，增加行与删除行分别使用绿色和红色；右键可跳转到产生修改的原文对话；点击文件时先将 Apply Patch 转为标准 unified diff，再使用文件浏览器同款只读 Diff 查看器进行左右分栏对比。
- Claude / Codex 历史转换始终生成新的目标会话；目标 CLI 正在运行时仍可新增转换，Codex 共享索引使用单次追加写并由 SQLite 锁协调 thread 注册。转换成功后直接激活后端已解析的目标详情，不等待历史索引刷新；Claude 目标项目键与写入后的实际目录一致，后续可正常重新打开；“继续对话”只使用与当前视图身份一致的详情。
- 用户确认删除本地历史会话后，即使其他同来源 Claude/Codex 进程仍在运行也可执行；删除继续先备份并在失败时回滚，备份恢复仍要求来源 CLI 停止运行。历史索引自动刷新和手动刷新保留当前已加载分页与虚拟列表，不再让滚动位置跳回顶部。
- WSL 中的 Codex 历史会话支持查看、恢复后的实时回读和 Claude -> Codex 转换；应用以 rollout `cwd` 识别真实项目，转换索引使用 Linux 路径，并避免从 Windows 跨 UNC 写入 Codex WAL 状态库。

### Git 变更面板

- 项目侧边栏 Git 工作区入口在浅色主题下保持按钮背景不变，通过更深的主题色边框与图标表达选中状态，避免出现突兀的黑色按钮。
- Git 日志表、分支树、提交详情与变更列表统一使用 6px 半透明细滚动条，悬浮时加深显示，在深浅主题下均避免高亮粗滚动条干扰内容。
- 项目侧栏底部提供独立 Git 工作区入口，在不销毁终端、分屏、Workspan 或 PTY 的前提下切换到 JetBrains 风格的 Git Log；工作区由引用树、彩色 DAG 提交表格和提交详情三栏组成，左右栏宽度可调整，关闭后直接返回原终端状态。
- Git 工作区支持多仓库切换、提交/作者/SHA 搜索和每批 50 条的连续加载；本地分支、按 remote 分组的远程分支及已加载历史中的标签集中展示，提交行显示作者、引用和中英文 24 小时时间。搜索结果对缺失父提交显示中断连线，不跨隐藏提交伪造拓扑。
- Git 工作区“变更”页复用既有 Git 变更面板和 Local/WSL/SSH Transport Lease；提交文件继续通过共享只读 Diff Viewer 打开，项目、Worktree、嵌套仓库、SSH 根仓库空 ID 与旧 Agent 升级提示保持现有契约。
- Git 工作区以底部工具窗口形式展开，支持拖拽调整高度；提交详情按目录层级展示变更文件，目录可独立展开或收起。
- Git 工作区引用树支持最近分支、收藏分支和搜索；分支、远程分支及标签可按上下文执行检出、比较、复制、创建分支、推送/删除标签等操作。
- 历史支持当前分支、全部引用或指定引用，并可按作者、日期和路径过滤；提交菜单支持复制 SHA/提交信息、查看父提交、导出 Patch、Cherry-pick、Revert、Reset 和创建标签。
- Git 工具弹窗提供 Stash、Remote、Reflog、文件历史/Blame、Bisect、Submodule 和受控提交整理；危险操作二次确认，强制推送仅使用 `--force-with-lease`，整理前创建恢复引用。
- Local/WSL/SSH 使用统一 Git Transport；SSH Agent `0.1.14` / protocol `1.15` 通过 `gitWorkspaceTools` capability 协商，旧 Agent 显示升级提示。
- Git 面板支持“变更 / 历史”双视图；历史按每页 50 条浏览并可按提交信息、作者或 SHA 搜索，展示短 SHA、作者、24 小时时间与引用标签。提交默认保持收起，点击后按需加载文件和增删统计，再次点击可收起；点击文件使用现有只读 Diff 查看器，Git 状态后台刷新不会触发已打开 Diff 重载；Merge 提交与第一父提交比较。
- Git 提交历史覆盖本地、WSL、Worktree、嵌套仓库和 SSH 项目；SSH Agent 通过独立 `gitHistory` capability 协商，旧 Agent 不接收未知协议帧并提示升级。仓库、分支或 transport 切换会清理分页和选中状态，迟到响应不会污染当前视图。
- Git 变更面板当前分支区域支持打开分支菜单，查看本地分支和远程分支。
- 分支菜单提供搜索框，可按本地分支名、远程分支名或 upstream 快速过滤；大量分支时先渲染前 80 条并提示继续搜索，减少菜单卡顿。
- 分支菜单支持切换本地分支；从远程分支 checkout 时会创建本地跟踪分支。
- 分支菜单支持基于搜索框中的新分支名显式新建并切换分支。
- 分支菜单支持执行 `fetch --prune` 刷新远端分支和提交信息，不会合并远端提交或修改工作区。
- 切换分支遇到本地改动会被覆盖时不会强制 checkout；Git 面板支持 Smart Checkout 确认流程，可在用户确认后先 `stash push -u`，切换分支，再 `stash apply` 恢复改动，并保留 stash 记录作为兜底恢复点。
- Smart Checkout 的 stash/apply 阶段如果失败或产生冲突，会展示明确错误并刷新 Git 面板状态，便于继续处理冲突；stash apply 冲突不会再误用 pull/merge 的中止操作。
- Git 变更文件列表的文件右键菜单支持打开源文件，会直接在文件编辑器 Pane 中打开对应项目文件；删除状态文件不会触发打开。
- Git 变更文件与目录节点右键菜单支持统一的绝对路径、AI 路径和相对路径复制。
- Git 变更面板的文件和目录行支持拖放到应用内终端；文件覆盖已修改、已添加、已删除、重命名、冲突和未跟踪状态，同项目/Worktree 终端插入相对 CLI 路径，跨项目、Worktree 或 SSH 根目录终端插入来源 Git 仓库的绝对路径，目录保留尾斜杠。拖拽复用文件浏览器的指针阈值、预览与投递契约，行内暂存、丢弃、删除操作不触发拖拽。
- 未跟踪文件和未跟踪目录的右键菜单支持从磁盘删除；删除前会二次确认，后端只允许删除当前 Git 状态仍为未跟踪的仓库内文件。
- 当前活动页是项目文件编辑器时，右侧 Git 变更面板会关联到该文件编辑器所属项目。
- WSL UNC 项目如果实际软连接到 `/mnt/<盘符>/...` 的 Windows 工作区，Git 变更面板会回退到 Windows/native Git 状态，避免 WSL Git 将全部文件误判为修改。
- Windows 上 libgit2 的仓库所有者校验误判时，Git 变更面板与 Worktree 操作会在受保护的兼容重试后继续读取仓库；重试仍失败时显示具体错误，不再误显示“无文件变更”。
- 打开的目录不是 Git 仓库时，Git 变更面板显示图标加说明的友好空态（“当前目录不是 Git 仓库”与初始化提示），不再展示 libgit2 原始错误或裸错误码；native、WSL 与 SSH 链路统一返回稳定错误码 
ot_git_repository`，所有权、权限等真实故障仍按原始错误提示，文件浏览器也只对该目录探测一次 Git 状态。

### Git Worktree 并行隔离

- 项目支持 Worktree 隔离策略：不提醒（默认）、提醒、并行时自动隔离、始终自动隔离。
- 项目已配置任意 CLI 工具（不限 Claude/Codex）且已有同项目终端时，新开终端可提示或自动创建独立 worktree，避免多个 AI 任务修改同一工作区；选择“不提醒”时始终直接普通打开；普通未配置 CLI 的终端项目不会因启动命令或运行状态触发隔离。
- 设置 -> 开发者关闭“Worktree开发”后，全局忽略项目隔离策略并直接普通打开，但不删除已有项目配置、Worktree 记录或手动 Worktree 操作入口。
- Worktree 使用项目同级 `<项目名>-worktrees/<任务名>` 目录和 `wt/<任务名>` 分支，也支持项目级自定义根目录。
- 项目树显示 worktree 子条目；项目级供应商徽章显示在主项目行，Worktree 级供应商徽章只显示在对应 worktree 子项；终端 Tab 仅使用 Worktree 任务名作为标题，并显示 worktree 徽标。
- Worktree 右键菜单支持打开目录、浏览文件、切换供应商、会话历史、安装依赖、完成任务和丢弃；切换供应商只影响当前 worktree，浏览文件会进入 worktree 独立目录，查看历史时项目筛选仍显示主仓库项目，实际列表限定在 worktree 独立目录，历史列表会用 `WT` 标记 worktree 会话。
- Worktree 终端 Tab 下，右侧实时统计、Git 分支查询和文件面板使用 worktree 自己的目录；“今日项目用量”按 worktree 实际路径聚合；已绑定 CLI sessionId 的实时统计严格匹配当前 session，不回退到同项目其他会话。
- 完成任务向导支持提交全部改动、合并回主工作区和清理 worktree/分支；主工作区脏时阻止合并并给出可读处理建议，冲突时自动中止合并、列出冲突文件并说明主工作区已恢复到合并前状态。
- 完成任务时，如果 Worktree 与主工作区没有内容差异，会阻止无意义合并并提示无需合并；Worktree 没有未提交改动时可直接进入合并/清理阶段，不再要求输入提交信息。

### Git Worktree 任务管理

- 项目 Worktree 设置可勾选“开启 Worktree 后检测依赖安装”；默认不勾选时，新建或打开 worktree 不会自动检测或弹出依赖安装提示。
- 对 worktree 右键选择“安装依赖”时，会在该 worktree 路径打开专用终端 Tab 执行检测到的安装命令；若无需安装会直接提示。
- 包含 worktree 的项目行支持展开/收起子列表，折叠状态会随侧边栏项目树状态保存。
- Worktree 使用线性分支图标展示，项目树、折叠侧栏浮层、拖拽预览和终端 Tab 徽标保持同一图标语言。
- 终端 Tab 中的 Worktree 徽章操作菜单会跟随终端主题色显示，与 Tab 右键菜单保持一致。
- Worktree 终端 Tab 与项目树联动：切换 Tab 时会自动选中左侧对应 worktree，点击已打开的 worktree 子项会切回对应 Tab。
- Worktree 历史入口不改变普通会话恢复/继续对话逻辑；普通项目会话仍按原项目目录打开历史和恢复上下文。
- 丢弃 worktree 前会关闭关联终端页签；后端会对 Windows 文件锁类删除失败做有限重试，包括 `os error 32` 和 stale 目录直接删除；若 Git 登记已清除但只残留空目录，或 Git 仍登记 path/branch 但目录 `.git` 已损坏，会作为 stale worktree 清理并删除对应 `wt/` 分支。

### 文件浏览器搜索与菜单

- **系统剪贴板粘贴（TEMP，2026-09-14）**：选中文件夹后按 Ctrl/Command+V 或右键“粘贴”，可导入 Windows 资源管理器复制的文件与整个文件夹；复制的截图保存为 `screenshot-日期-时间-唯一后缀.png`。选中文件则写入其父目录，列表空白处写入项目根目录；文件树、紧凑目录链、两种搜索、侧栏与嵌入面板共用逻辑。
- 系统剪贴板新内容优先于过期的内部复制/剪切（Windows 序号判定）；空或纯文本剪贴板不创建文件、不误贴旧选择。外部剪切也只复制、不删除源文件。重名先确认，文件夹覆盖为整体替换、不合并；保护未保存缓冲区与项目切换，仅重试冲突项。
- 外部文件以磁盘复制导入，不经 WebView/base64；截图限 1200 万像素、PNG 64 MiB。导入不是临时终端附件，不按两天策略清理。完整暂存后才替换原目标，拒绝链接、路径越界、自身/祖先/子目录覆盖以及危险名称；失败时保留原文件。

- **多选与批量操作（V1.4.0，2026-09-13）**：文件树、文件名搜索和代码搜索支持 Ctrl/Command 点击追加/取消选择，允许文件与文件夹混选，多选时不打开文件或展开目录。侧栏和嵌入面板显示选中数量，普通点击恢复单选，Esc 或点击列表空白区域清空选择。
- 选中项可整体拖到当前项目目录，也可用 Ctrl+X/C/V 或右键剪切/复制/粘贴；拖到终端则组合既有路径文本，不执行文件移动。单项重命名保留，多选不提供批量重命名，SSH 写操作在菜单、键盘、拖拽与 store 层保持禁用。
- Delete/右键删除统一确认并显示数量、部分路径及永久删除警告（不经过回收站）；父子项去重，同名冲突明确确认后仅重试冲突项，文件夹覆盖是整体替换而非合并。批次内同名目标、自身/祖先覆盖、子目录移动、未保存内容及链接路径均受保护；部分失败按项说明，不将失败项当作成功。
- 项目/Worktree 切换清空选择与文件剪贴板，过期操作快照不能作用于新项目；批次统一刷新列表/Git/搜索，保护等待期间的新编辑，不改变 Live Server、终端快捷键、项目配置或历史会话。
- 重命名成功后同步迁移选中项的路径与名称，确保后续删除、剪切和复制操作使用新路径；失败时不改变原选择。Windows 本地移动的祖先目录保护按路径组件和大小写不敏感语义判断，WSL UNC/POSIX 保持大小写敏感。

- 文件编辑器按实际项目目录保留内存工作区；切换项目、Workspan 或分屏不会关闭已打开文件或丢失未保存内容，切回后恢复原活动文件，关闭对应文件编辑器 Tab 或退出应用后清理。
- Markdown 文件的渲染预览支持 Ctrl + 滚轮字号缩放（8–32px）；字号会直接作用于 Markdown 渲染根节点，源码编辑模式保持 Monaco 原有行为。
- 文件预览禁止打开视频；文本/其他文件超过 1 MiB、图片超过 5 MiB 或光栅图片超过 1200 万像素时会在读取前拒绝预览，本地与 SSH 路径均有后端限制，避免 WebView/GPU 因大内容解码卡死。
- TypeScript `.ts` 文件在本地与 SSH 文件预览中统一作为文本源码读取，不再误判为视频；其他视频扩展名仍会在读取内容前拒绝。
- 文件浏览器搜索框支持在输入框内直接切换“文件 / 代码”搜索模式。
- 项目文件预览、编辑、代码搜索和 Git Diff 支持自动识别 UTF-8/BOM、UTF-16 BOM 以及 GBK、Big5、Shift_JIS、Windows-125x 等常见文本编码；编辑保存保持原编码和 BOM，无法映射的新字符会被阻止写入。非 UTF-8 Diff 仅关闭可能破坏原始字节的行级/Hunk 级回滚，整文件操作仍可用。
- 文件浏览器搜索框与右键菜单会跟随所在侧边面板主题配色，避免深色侧边栏中出现浅色控件割裂。
- 文件浏览器右键菜单使用应用根级主题化 Portal，并由 Radix Popper 按完整菜单宽高执行窗口边缘翻转和钳制；在项目侧栏及左/右停靠、合并/独立终端辅助面板的任意位置触发时均完整显示，右键目标文件或目录在菜单打开期间保持选中高亮，主题配色、键盘交互与“复制路径为”原位替换保持不变。
- 文件浏览器顶部操作按钮的悬浮提示使用应用主题背景、边框与文字颜色，浅色与深色主题下保持一致可读性；文件树、搜索结果和顶部项目区域不显示路径悬浮提示。
- 文件浏览器的常见缓存、构建产物、AI 工具目录、`.gitignore` 命中项及手动忽略目录都按正常文件树层级显示在自身父级原位，通过整行降透明度区分忽略态；淡化沿子树继承，被淡化行在悬浮或选中时提亮以保留交互反馈。`.git`、`.hg`、`.svn` 元数据始终不显示（含 git worktree 中 `.git` 为文件的情形）。
- 文件树、文件搜索结果、代码搜索结果和根空白区域右键菜单支持打开所在文件夹；文件项会在系统文件管理器中选中目标文件。
- 本地项目与 Worktree 的 `.html` / `.htm` 文件可从文件树、文件搜索或代码搜索结果右键“使用 Live Server 打开”，由应用内 loopback 服务在系统默认浏览器中预览；同一项目复用端口，HTML/CSS/JS/图片等项目文件变化会触发页面自动刷新，根区域右键菜单可停止服务。SSH、WSL、非 HTML 入口和目录不提供该操作，也不会回退到外部运行时。
- 文件树、文件搜索结果、代码搜索结果、根区域及 Git 变更面板的文件/目录右键菜单支持直接复制绝对路径；点击“复制路径为”后原位切换为仅显示 AI 路径和项目相对路径的菜单，二级菜单保持文件菜单样式并使用语义图标；本地项目与 SSH 项目分别使用本地根路径和远端 `remote_path`。
- 文件浏览器顶部项目区域支持双击打开项目位置。
- 从文件浏览器拖拽文件/目录到内置终端时，拖拽预览按动画帧紧跟指针，并插入与右键“复制 AI 路径”一致的路径格式（不带项目名的 `@相对路径`，目录带尾 `/`）。
- 文件树、文件搜索结果和代码搜索结果的右键菜单支持复制相对路径与绝对路径；文件面板拖拽到同一项目终端时保留相对路径，拖拽到其他项目或 Worktree 终端时自动改用来源项目的绝对路径；跨项目拖拽不会切换原文件面板的项目与目录位置，并在插入路径后自动追加空格。

## 0. V1.2.5 增补

### 0.1 终端外部文件拖拽

- 从系统文件管理器拖拽文件到内置终端时，会在终端输入位置显示本地文件路径。
- 多文件拖入时会按空格拼接多个路径。
- 路径会按当前终端 Shell 使用对应引号规则，降低空格路径或特殊字符路径输入失败的概率。

### 0.2 文件浏览器自动刷新

- 打开项目文件浏览器后，终端或外部工具修改并保存项目文件时，文件树会自动刷新，不再依赖手动点击顶部刷新按钮。
- 切换终端 Tab 时，相同本地目录或相同 SSH 主机与远端根目录会复用已加载文件树并保留滚动位置；只有项目文件位置实际变化时才重新加载。
- 已展开目录会尽量保持展开状态，不因自动刷新而整棵树塌陷。
- 已打开且没有未保存修改的文件预览会自动重读最新磁盘内容。
- 已打开但仍有未保存修改的文件不会被自动覆盖。
- 自动刷新生命周期由 App 常驻控制器管理；关闭或隐藏文件侧栏后，仍会保留本地 watcher、WSL watcher 失败后的低频轮询及窗口重新聚焦补刷。
- SSH 项目在远端文件上下文就绪且存在已打开文件时，以 15 秒静默轮询和窗口重新聚焦补刷最新内容；自动检查不重复创建后台操作，也不会回退到本地文件命令。
- 文件 watcher 会携带变更路径，文件浏览器优先刷新受影响目录。
- 连续文件系统事件会串行合并后刷新，避免一次保存或批量生成文件触发多次重载和刷新乱序。
- 
ode_modules`、`target`、`dist`、`.gitnexus` 等高噪音目录变化不会触发文件树刷新。
- 当本机 watcher 不可用时，会自动降级为低频轮询，并在窗口重新聚焦时补一次刷新。

### 0.3 文件浏览器 Git 状态

- 文件树和文件搜索结果会标记 Git 状态，覆盖修改、新增、删除、重命名、冲突和未跟踪文件。
- 文件状态使用不同颜色区分：新增为绿色、修改为蓝色、删除为红色，未跟踪和冲突使用独立颜色。
- 目录会聚合其子文件的 Git 状态，折叠时也能看出目录内存在变更。
- 打开有 Git 变更的文本文件时，编辑器左侧标记和概览标尺会显示新增、修改、删除位置。
- 有 Git 变更的文件可通过右键菜单在文件编辑器 Tab 中打开 Diff。
- 内嵌 Diff 支持文件级、代码块级和选中行级回滚；文件级与代码块级回滚执行前需要二次确认，并跟随当前终端主题背景。

### 0.4 Git 面板多子仓库监控

- 项目根目录下存在多个 git 仓库时，Git 面板顶部显示仓库切换下拉，列出主仓库与限深扫描到的子仓库（含当前分支）。
- 切换子仓库后，变更列表、分支状态、暂存、提交、推送、拉取与 Diff 均针对所选子仓库生效。
- 扫描排除 
ode_modules`、`target`、`dist` 等常见大目录并跳过符号链接，避免面板首开卡顿。
- 子仓库内文件改动会触发面板自动刷新；子仓库 `.git/objects` 等内部写入不会引发刷新风暴。
- 单仓库项目 UI 保持不变；主仓库变更列表不再展示嵌套子仓库目录条目。
- 文件 Diff 弹窗支持按 Esc 键关闭。

## 0.2 V1.2.4 增补

### 0.2.1 左侧项目列表与终端粘贴

- 左侧项目列表显示每个项目当前已打开的终端数量。
- 左侧项目列表支持“全部 / 已开启”过滤，仅查看拥有内置终端的项目；过滤控件可在“设置 -> 侧边栏”中隐藏。目录右键菜单可批量停止当前目录及子目录的内置终端，并沿用终端关闭确认设置。
- 项目侧边栏支持通过可配置快捷键展开/收起，默认使用 `Ctrl+B`；终端全屏时触发该快捷键会先退出全屏。
- 开启“按项目聚焦终端”后，点击项目时右侧终端区域只展示该项目关联的终端；点击“全部终端”恢复完整终端列表。
- 终端所有粘贴入口都会先去除剪贴板内容首尾换行。
- 当 `Ctrl+Shift+V` 粘贴内容仍包含换行时，会自动加单引号包裹，避免多行文本被逐行立即发送。
- 普通 `Ctrl+V` 和右键菜单粘贴只做首尾换行清理，不额外包裹内容。
- 终端粘贴剪贴板图片时，图片统一保存到用户数据目录 `.cli-manager/attachments`，并将生成文件的绝对路径写入当前终端；附件默认保留 2 天。

### 0.2.2 推荐操作流程

1. 在左侧项目列表中通过搜索定位项目。
2. 单击项目，右侧终端区域只显示该项目已打开的终端。
3. 需要查看全部终端时，点击项目列表顶部“全部终端”；如不需要该行为，可在设置-终端中关闭“按项目聚焦终端”。
4. 需要安全粘贴多行提示词时，在终端中使用 `Ctrl+Shift+V`。

### 0.2.3 macOS 内置终端环境

- macOS 上选择内置 `zsh` 时，会按 login shell 启动并加载 `~/.zprofile` 等登录环境配置。
- macOS 上选择内置 `bash` 时，会按 `--login -i` 启动并加载 `~/.bash_profile` 等登录环境配置。

## 0.1 V1.2.3 增补

### 0.1.1 左侧项目列表与终端聚焦

- 项目列表支持瞬时搜索：当左侧项目树有焦点时，可直接输入英文开始搜索，也支持在项目列表内按 `Ctrl+F` 呼起搜索框。
- 搜索框不常驻；清空关键词或按 `Escape` 会退出搜索并恢复完整树结构。
- 点击左侧项目列表空白区域会把焦点回到项目树容器，便于继续直接输入搜索，或在非输入框焦点下通过 `Escape` 退出搜索。
- 开启“按项目聚焦终端”后，左侧项目列表顶部会出现“全部终端”虚拟项；点击项目时，右侧仅展示该项目关联的终端与 sub-agent transcript。
- 当选中的项目没有已打开终端时，右侧显示项目级空状态，并支持直接为该项目启动终端。

### 0.1.2 推荐操作流程

1. 开启“按项目聚焦终端”后，可在左侧项目列表中单击项目切换当前终端展示范围；需要查看全部终端时，点击“全部终端”。
2. 需要快速定位项目时，先聚焦左侧项目列表，再直接输入英文关键词，或按 `Ctrl+F` 呼起搜索。
3. 搜索过程中可随时按 `Escape` 或清空关键词退出；点击项目列表空白区域后，仍可继续输入搜索或按 `Escape` 收起搜索框。

## 1. 产品定位

CLI-Manager 是一款面向 Windows 开发者的多项目 CLI 工作台，核心目标是把多项目开发中的 **项目、终端、命令、AI CLI 会话、历史回看、统计与同步** 集中到一个桌面应用中管理。

当前项目已经覆盖：

- 多项目与分组管理
- 内置 PTY 终端
- Tab 与 JetBrains 风格分屏
- Claude Code / Codex CLI / Kimi Code / Pi Agent / Grok Build Hook 状态桥接（支持独立启用与关闭，关闭后自动折叠详情但保留真实安装状态徽标与统一刷新；Kimi 使用当前版 TOML Hook，Pi 通过 Extension、Grok 通过原生 Hook 绑定会话并提供实时统计；Codex 子任务转录在 Hook 缺失时按终态兜底关闭）
- 命令模板与终端输入提示
- Claude / Codex / Kimi / Grok 历史会话回看、恢复；Kimi 与 Grok 支持本地删除
- Diff 查看
- 历史用量分析看板（ccusage 风格）
- 历史会话统计面板与终端实时统计面板
- cc-switch 供应商集成与项目级供应商切换
- WebDAV 云同步
- 主题、快捷键、终端背景等个性化设置

---

## 2. 项目与工作区管理

### 2.1 项目管理

- 新建项目
- 编辑项目
- 删除项目
- 克隆项目配置
- 项目路径配置
- 项目 Shell 配置
- 项目启动命令配置（仅 CLI 工具为空时在表单显示；填写 CLI 工具后隐藏并在保存时清空）
- 项目环境变量配置（默认 3 行的 JSON 等宽多行文本域，可纵向调整；字段旁提供可一键复制的悬浮格式示例，支持键盘聚焦查看）
- 项目 CLI 工具标识配置
- 项目 CLI 启动参数配置（自由文本整串透传，拼接在 CLI 工具后；与供应商切换自动追加参数兼容；仅 CLI 工具非空时在表单显示，CLI 工具为空时隐藏并在保存时清空；新建或编辑成功后按当前 CLI 工具记录历史候选，按使用次数排序且仅显示前 10 条；克隆不记录）
- 项目路径健康检查
- WSL UNC 项目路径健康检查会在 WSL 内按存在路径或软连接节点判断有效性；通用设置开启“软连接兼容性”后，项目路径旁提供内置 WSL 入口，并只展示指向真实目标的 Linux 目录软连接路径
- 路径失效时在项目树展示警告状态

### 2.2 分组管理

- 新建分组
- 重命名分组
- 删除分组
- 支持嵌套分组
- 项目可移动到分组内
- 分组可折叠 / 展开

### 2.3 项目树交互

- 项目搜索
- 项目 / 分组拖拽排序，放手后立即更新列表位置并持久化
- 折叠分组参与拖拽排序
- 分组折叠状态持久化，重启保持上次状态
- 项目树上下文菜单（精简行内按钮，功能统一走右键菜单）
- 普通项目右键菜单可显式使用外部终端启动项目，并沿用项目路径、Shell 与 CLI 启动命令；精简模式或全局外部终端模式不会重复显示入口
- 多选后右键当前已选项目时，菜单仅保留取消选择、启动已选、批量修改 Shell 和删除已选
- 项目右键菜单新增「打开所在目录」
- 项目状态点展示
- CLI 工具轻量标识
- 侧栏折叠时悬浮项目图标显示项目名称
- claude 项目有供应商覆盖时显示独立徽标（网络拓扑图标 + 供应商名）
- 侧栏宽度持久化
- 侧栏密度设置
- 侧栏折叠 / 精简显示

### 2.4 项目启动

- 从项目树启动内部终端
- 项目和 Worktree 右键“新建终端”在对应目录打开普通 Shell，不继承项目 CLI 或配置的启动命令
- 从项目树启动外部 Windows Terminal
- 批量启动选中项目
- 启动筛选后的项目列表
- 精简模式下默认使用外部终端启动项目
- 切换已有项目终端时同步左侧项目树选中态

### 2.5 SSH 远程项目与远程终端（MVP）

- 设置 -> SSH 主机统一管理连接档案，支持名称、手动分组、地址、端口、用户、SSH Config、Agent、私钥、密码询问、交互认证、跳板机、ProxyCommand、连接超时、KeepAlive、初始化命令与备注。
- SSH 主机编辑器按基本信息、认证、跳板与代理、连接设置、初始化分区展示；字段随连接来源和认证模式联动，保存或测试前会校验当前生效配置。
- SSH Config 连接由系统配置完整托管；手动连接支持 Agent、指定私钥、密码和 Keyboard-interactive，认证方式切换会清理失效参数，主机列表可直接打开 SSH 终端完成密码、MFA 或私钥口令交互。
- SSH 登录密码可保存到系统原生凭据库：Windows Credential Manager、macOS Keychain、Linux Secret Service；OpenSSH 通过一次性 AskPass 通道读取，数据库和同步数据只保留凭据引用。
- SSH 主机支持系统 OpenSSH 探测和分阶段连接诊断；密码、私钥口令和 Token 不写入 SQLite、日志或会话快照。
- 新增终端弹窗通过“本地项目 / SSH 远程项目”Tab 切换；两类表单分别保留草稿，编辑已有项目时锁定运行环境类型。
- SSH 远程项目选择已有主机和绝对 POSIX 远程目录，支持连接后浏览、手工输入、目录存在/可进入/Git 仓库检测；密码或 MFA 模式无法执行非交互浏览时降级为手工路径。
- 远程终端由 Rust 根据结构化 Launch Plan 生成 OpenSSH 参数，安全进入远程目录并启动项目 CLI；前端不拼接完整 SSH 命令。
- SSH 会话支持普通 Tab、复制 Tab、终端分屏、Workspan、项目分组、命令模板和 daemon 后台托管；Tab hover 展示 SSH 主机、远程目录、连接状态和断开原因。
- SSH 会话状态包括连接中、等待认证、已连接、已断开和连接失败；区分远端 Shell 退出、远端命令异常、SSH/网络中断和本机 SSH 进程异常。
- daemon 仍持有的 SSH PTY 在应用重开后直接 attach；已退出会话只显示断开状态并回放输出，不重新执行启动命令。
- 每台 SSH 主机可分别配置 Claude、Codex、Kimi Code 与 Grok Build 的 CLI 配置目录，默认使用远端用户的 `$HOME/.claude` / `$HOME/.codex` / `$HOME/.kimi-code` / `$HOME/.grok`；SSH 项目可设置独立覆盖目录。启动时按项目覆盖、主机配置、CLI 原生默认的顺序解析，并安全注入 `CLAUDE_CONFIG_DIR` / `CODEX_HOME` / `KIMI_CODE_HOME` / `GROK_HOME`；Kimi 与 Grok 目录只承载 Hook，本阶段远程历史仍仅支持 Claude/Codex。保存或浏览目录不会安装 Agent、写入 Hook 或扫描供应商。
- SSH 主机“CLI 集成”页提供显式 `cli-manager-ssh-agent` 检测；打开页面不会自动建立 SSH 连接，点击检测后通过复用认证/跳板机/代理/AskPass 的一次性 `ssh -T` 读取 `version/status/doctor`。检测限制 8KB 登录提示、64KB stdout 和 8KB stderr，校验 Agent 身份、协议主版本及 Linux x64/arm64 目标，只持久化脱敏状态与版本元数据。打开页面会自动检查签名 Agent 发布版本（内置优先，无 SSH）；已安装且更旧时展示新版本，用户可按下“更新”进入原有预览确认后再升级。
- SSH Agent 新版本检测的 Rust 测试导入保持 `rustfmt` 格式，后端格式检查可通过。
- `cli-manager-ssh-agent` 支持按 Host 显式预览并执行安装、升级、回滚和卸载；可使用默认 XDG 目录或自定义绝对/`~/...` 安装根。桌面端先验证 Minisign manifest、协议、目标、制品大小与 SHA-256，再通过当前 SSH 配置的 stdin 上传；远端使用安装锁、discovery record、版本目录和 `current/previous` 原子链接，默认禁止降级并在切换失败时恢复旧状态。发布页同时提供可先下载审阅的 POSIX 安装脚本，支持 HTTPS、显式受信 HTTP 镜像、dry-run、自定义目录和卸载；Agent 生命周期操作本身不会修改 Claude/Codex/Kimi/Grok Hook。
- SSH 主机“CLI 集成”可按 Claude/Codex/Kimi/Grok 及项目覆盖目录分别检查、预览、安装/升级和卸载远端 Hook，展示规范化配置根与实际 `settings.json`、`hooks.json`、`hooks/cli-manager.json`、`config.toml` 地址和指纹；只精确管理包含当前 Agent installation id 的条目，保留第三方字段、顺序、matcher、symlink 目标和用户自有配置。Kimi candidate 会先通过当前 Kimi Code 的 `doctor config` 校验，旧 `kimi-cli` 明确 unsupported。Grok 远端规划 `hooks/cli-manager.json` 并关闭 `compat.claude.hooks` / `compat.cursor.hooks`，不调用 Grok CLI doctor，installation record 不携带 history candidate。显式安装可创建缺失的标准默认目录，自定义缺失目录拒绝；修改根目录后旧 Hook 进入显式待清理状态，Agent 卸载会阻止遗留自有 Hook。
- SSH Claude/Codex/Kimi/Grok 启动由 Rust 覆盖注入 Host/client/project/Tab/bridge epoch 绑定；普通 SSH、IDE、tmux 或另一个桌面客户端缺少匹配绑定时 Hook 快速 no-op，不写 spool。只有有效配置根已验证为 Hook `installed` 且 Agent 身份仍匹配时才建立每主机一个 protocol 1.1 Agent bridge；全局最多 4 条 bridge、2 条并发连接/重连，具备有界 preamble/hello/响应超时、10 秒 heartbeat、抖动退避、旧 socket 接管和最后会话回收。断线事件按 Host/client/installation 隔离保存，采用 24 小时、10000 条、32 MiB 上限、逐条有界读取、流式 ACK、sequence/event id、去重和 gap 告警；远端 transcript 引用保持不透明，不进入本地文件 API 或第三方通知。
- SSH 主机删除会解除项目的本机 Host 绑定并保留远程目录；活动终端或跳板机仍引用时禁止删除，远端 Agent/Hook 不会被隐式卸载，远端集成记录保留为待重新绑定状态。
- SSH 项目文件面板仍保持只读：远端 Agent 只接受受约束的 root/relative path，跳过 symlink，并限制目录条目、搜索结果、遍历文件数、单文件和图片大小；SSH Host 独立的 SFTP 面板通过 `filePut` 写入用户当前选择的远程目录。SSH Git 面板通过 protocol `1.7` 的 `gitFull` capability 和独立串行 Git lane 支持当前本地面板全部操作；两个面板首次获取远程上下文与数据时显示加载状态，Git 根路径始终来自登记项目的 `remote_path`，文件刷新继续携带同一远程上下文。Agent 使用参数数组执行 Git，校验项目根、仓库/文件路径、分支、Patch、symlink 与输入上限，网络操作只使用远端已有非交互凭据，写操作超时/断线不自动重放。Worktree、外部 Windows Terminal 和远程资源监控仍禁用或降级提示，远程路径不会进入本地文件/Git/历史 API。
- 系统资源面板在 SSH 项目中仍可查看，但明确标注为“本机资源”，不代表远端服务器资源。
- WebDAV/本地导入导出保留 SSH 项目的远程目录与项目配置，但不同步 SSH 主机、凭据引用和私钥路径；导入到其他设备后项目显示重新绑定提示。

---

## 3. 内置终端工作区

### 3.1 PTY 终端能力

- 基于 Rust 后端 PTY 创建真实终端会话
- 前端使用 xterm.js 渲染终端
- 支持终端输入写入 PTY
- 支持终端尺寸 resize 同步
- 支持关闭 PTY 会话
- 支持查询终端进程状态
- 后端输出按安全边界切分，避免截断 UTF-8 字符或 ANSI 转义序列
- PTY resize 有最小尺寸保护，避免不可见容器导致异常尺寸

### 3.2 Tab 管理

- 新建终端 Tab
- 关闭当前 Tab
- 重命名 Tab
- 复制 Tab（按当前 Tab 的项目、路径、Shell、环境变量、启动命令和标题快速创建同配置的新终端）
- Tab 拖拽排序
- Tab 溢出滚动
- Tab 溢出列表控制
- Tab 溢出列表浮层跟随终端菜单配色，避免透明模糊影响可读性
- 关闭其它 Tab
- 关闭左侧 Tab
- 关闭右侧 Tab
- 切换 Tab 时保留终端缓冲区
- 隐藏 Tab 有界输出缓冲，降低后台占用

### 3.3 终端状态展示

- Tab 状态点
- 项目树聚合状态点
- 状态类型：
  - 运行中
  - 待审批
  - 已完成
  - 异常退出
- 状态优先级：待审批 > 异常退出 > 运行中 > 已完成
- 基于 OSC 133 shell integration 的标准化状态检测
- PowerShell / pwsh 使用 history id 判断命令真正执行
- 支持 OSC 133 / 633（VS Code）/ 777（私有）三种序列
- 支持 BEL 与 ST 两种终止符
- 跨 chunk 前缀缓冲
- Git Bash / CMD 注入 133 标记
- Shell runtime monitoring 可在设置页关闭（默认关闭以提升启动速度）

### 3.4 终端输出与输入优化

- 高频 PTY 输出合并后限频写入 xterm
- 非激活终端降频刷新
- WebGL 渲染失败或上下文丢失时回落 Canvas 渲染
- 中文输入法组合输入期间暂停自动 fit，降低界面抖动
- 终端粘贴支持
- 支持从系统文件管理器拖拽文件到终端并显示本地路径
- 支持本地历史/模板/路径输入提示，候选以光标后的 ghost 文本显示，可用 `Tab`、方向键右键或 `Ctrl+Space` 采纳补全
- 终端复制支持
- Codex 高频重绘场景下合并光标显示序列，减少闪烁
- 终端输出搜索浮层
- `Ctrl+F` 搜索终端输出
- 搜索结果高亮
- 上一个 / 下一个搜索结果导航
- 关闭搜索后恢复终端焦点
- HTTP/HTTPS 与 OSC 8 超链接悬停显示链接图标，并通过 `Ctrl + 单击`交由系统默认浏览器打开
- 本地终端输出中的 Windows 文件地址支持 `D:/path/to/file`、`D:\\path\\to\\file` 和 `/D:/path/to/file` 格式识别并通过 `Ctrl + 单击`打开；传给 Windows Explorer 前会规范化为原生路径，文件会定位到其实际所在目录并被选中
- 终端回滚行数默认 9000 行；可通过默认关闭的“自定义回滚行数”开关启用 1000-50000 行调整
- 终端回滚行数支持热更新，无需重启应用
- Grok Build 手动启动后支持按设置使用 `Alt+Enter` / `Shift+Enter` / `Ctrl+Enter` 换行；仅在精确提交 `grok` 且可见 TUI 输入提示仍存在时发送其所需的 `ESC + CR` 序列，普通 Shell 不受影响

### 3.5 终端中文输入法修复

- 修复中文输入法下输入中文符号时需要输入两次的问题
- 修复 macOS + Fcitx5 在内置终端确认中文候选时的重复上屏：共享输入层会在 xterm helper textarea 的近期 Process-key 检查点内去除同源 CJK 重发，并保留原有跨来源去重；对普通 Shell、Codex 与其他 CLI 一致生效，新的 composition 会清除检查点以允许用户有意连续输入相同文本
- 修复中文输入法候选框错位问题
- 修复 Claude Code / Codex 流式重绘导致候选框漂移
- IME composition 锚点冻结增强
- IME 候选框锚点增加静默光标采样、TUI 边框输入行识别
- 纯结构识别候选框锚点位置，免疫 TUI 流式重绘
- 支持空框 / 单行 / 多行场景
- 窄窗口优先使用输入区域内的真实 buffer cursor；全屏、分屏或窗口缩放后重新钉住闲置 helper textarea，输入期间 reflow 时废弃旧 composition 锚点并重新定位

---

## 4. JetBrains 风格终端分屏

### 4.1 分屏布局

- Split Right
- Split Down
- Unsplit
- 左右分屏
- 上下分屏
- 混合嵌套分屏
- 运行时 pane tree 管理
- 拖拽分隔线按动画帧直接更新 Pane 几何，松手后一次性持久化相邻 pane 比例
- Workspan 仅在单 Pane 且单会话时隐藏本地标签头；分屏后的单会话 Pane 保留紧凑 Tab，显示名称、关闭、拖拽、全屏及当前 Tab 还原操作；当前 Tab 还原会将其分离为独立顶层 Tab，Workspan 右键还原会分离其中全部 Tab；文件编辑器、同步历史、并行子任务等多视图 pane 保留紧凑切换条，其中子任务活动时仅保留关闭操作

### 4.2 Tab 与 Pane 交互

- 顶层 Workspan 标签：单终端显示原名称，多终端显示 `Workspan · N`
- 顶层标签栏内低延迟拖拽调整 Workspan 顺序
- 拖拽 Workspan 到当前具体 pane 的左 / 右 / 上 / 下时即时显示布局预览，并整体合并来源布局
- pane 内 Tab 拖拽排序
- Tab 拖到其它 pane
- Tab 拖到 pane 边缘创建分屏
- 拖拽时显示 DragOverlay
- 边缘落点显示半透明预览
- Workspan 右键菜单支持将其中全部 Tab 分离为独立顶层 Tab；范围过滤时禁用，避免隐藏会话被改动
- Pane 内 Tab 拖到顶部 Workspan 标签或标签栏空白区可分离为独立顶层 Workspan，按目标位置插入或追加；拖拽过程中实时显示插入位置预选线；范围过滤时禁用
- 拖拽落点中心区域扩大，只有靠近 Pane 外缘时才创建分屏
- Move to Other Split
- pane 内关闭其它 / 关闭左侧 / 关闭右侧
- Claude / Codex 子任务可自动打开只读 transcript 分屏，并在 Windows、WSL、Linux、macOS 上持续增量展示运行中输出

### 4.3 分屏入口

- 终端 Tab 右键菜单分屏
- 命令面板分屏动作
- 侧栏项目树分屏启动项目
- 可选择空终端或指定项目创建分屏

### 4.4 分屏行为设置

- Unsplit 时合并到相邻 pane
- Unsplit 时关闭当前 pane 内会话
- “Workspan开发”默认开启；关闭后隐藏顶层 Workspan 标签并让单会话 Pane 继续显示终端 Tab
- Workspan 分屏布局随会话数据持久化；重启恢复 PTY 时会把旧会话 ID 映射到新会话 ID 并重建布局

---

## 5. Shell 与外部终端

### 5.1 内置终端 Shell

支持以下 Shell：

- PowerShell
- CMD
- pwsh
- WSL
- Bash
- Git Bash

### 5.2 Git Bash 支持

- 独立 `Git Bash` Shell 选项
- 从 Git for Windows 常见路径解析
- 从 PATH 解析
- 从 Windows 注册表解析
- 找不到 Git Bash 时给出明确错误，不静默回退到缺失的 `bash.exe`

### 5.3 外部 Windows Terminal

- 通过 Windows Terminal 打开项目
- 支持一个窗口内批量打开多个 Tab
- 支持按项目 Shell 配置启动
- 支持 cwd
- 支持标题
- 支持启动命令
- 使用 `--suppressApplicationTitle` 避免标题被 Shell 覆盖

---

## 6. Claude Code / Codex CLI / Kimi Code / Grok Build 集成

### 6.1 Hook Bridge

- 应用启动本地回环 Hook 通知服务
- 仅监听 `127.0.0.1`
- 使用 bearer token 校验 Hook 请求
- 为每个 PTY 会话注入：
  - `CLI_MANAGER_TAB_ID`
  - `CLI_MANAGER_NOTIFY_PORT`
  - `CLI_MANAGER_NOTIFY_TOKEN`

### 6.2 Claude Code Hook

支持事件：

- `UserPromptSubmit`
- `Notification`（细分 matcher：permission_prompt / idle_prompt 置 attention，auth_success 等不干扰）
- `PreToolUse`（仅 `AskUserQuestion`，桥接为 `Notification`）
- `Stop`
- `StopFailure`
- `SessionStart`（会话启动 / 恢复时回传 sessionId 并绑定到终端 Tab）

能力：

- Claude 提交用户 Prompt 时标记对应 Tab 为运行中
- Claude 需要处理时标记对应 Tab 为待审批
- Claude 等待用户选择或回答时发送专用通知
- Claude 完成时标记对应 Tab 为已完成
- Claude 失败时标记对应 Tab 为异常退出
- 支持悬浮通知卡片
- 切换到目标 Tab 后自动清理通知
- Hook running 状态增加 30 分钟超时回退
- Hook 事件按 timestamp 丢弃乱序旧事件
- Hook 上报脚本优先使用 `curl.exe`（失败回退 `Invoke-RestMethod`）
- Hook 命令加 `-NoProfile` 降低延迟

### 6.3 Codex CLI Hook

支持事件：

- `UserPromptSubmit`
- `PermissionRequest`
- `PreToolUse`（仅 `request_user_input`，桥接为 `Notification`）
- `Stop`

能力：

- Codex 提交用户 Prompt 时标记对应 Tab 为运行中
- Codex 权限请求时标记对应 Tab 为待审批
- Codex 等待用户选择或回答时发送专用通知
- Codex 停止时标记对应 Tab 为已完成
- 与 Claude 共用终端 Tab 通知逻辑
- 按来源区分 Claude / Codex 通知
- 项目内 Trellis 的 Codex 执行默认由主会话直接完成；仅当用户明确要求，或主会话先征得明确确认后，才派发研究、实现或检查子任务

### 6.4 Kimi Code Hook

- 仅支持当前 Kimi Code 的 `$KIMI_CODE_HOME/config.toml`（默认 `~/.kimi-code/config.toml`）；旧 `kimi-cli` 和 `~/.kimi` 不修改、不迁移。
- 六个设置模块精确管理九个 definition：`SessionStart`、`TurnStarted -> UserPromptSubmit`、`PermissionRequest`、`PermissionResult`、`Stop`、`Interrupt`、`StopFailure`、`SubagentStart`、`SubagentStop`。
- `PermissionResult` 静默恢复运行状态，`Interrupt` 静默清除运行/待审批状态；Subagent 事件进入绑定、通知与 Replay，但不创建 transcript 分屏。
- 本地状态与安装直接使用结构化 TOML planner，不探测或启动 Kimi CLI/`doctor` 子进程；缺少 Kimi CLI 时仍可快速检查和安装 Hook。同目录临时 candidate 在写前重验后原子替换，解析、owner 冲突、并发修改或替换失败均保留原配置；卸载只删除带精确 CLI-Manager owner 的条目，保留用户/第三方 Hook、注释和顺序。SSH Agent 远端安装仍保留 current-product doctor candidate 校验。
- 本地自定义 Kimi 目录仅用于 Hook 管理，不自动注入 `KIMI_CODE_HOME` 或切换凭据/会话；SSH 项目覆盖目录会安全注入远端 Kimi 进程。
- 本地与 WSL 历史会话读取 `$KIMI_CODE_HOME` 或 `~/.kimi-code` 的 `session_index.jsonl` 与 `agents/main/wire.jsonl`，支持列表、搜索、统计、删除整棵会话目录，以及 `kimi --session <id>` 恢复；wire 解析按当前协议折叠 `context.append_loop_event.event` 中的助手内容、工具调用/结果，并按 `inputOther` / `output` / `inputCacheRead` / `inputCacheCreation` 统计 Token，等值的 `step.end.usage` 与 `usage.record` 只计一次，任一缺失时由另一项回退；索引读取遵循 latest-wins 与 deletion tombstone，畸形普通记录不覆盖前一有效状态，删除只追加 tombstone，不覆盖并发追加记录；历史或 Hook 会话 ID 在拼接恢复命令前执行字母数字、`_`、`-` 白名单校验。历史工作区列表/搜索走 catalog 索引（含子 Agent 排除），列表标题优先 `state.json.title`；Hook 绑定的实时统计按精确 sessionId 直查，不回退项目最近会话。旧 `~/.kimi` 不扫描。SSH Kimi 历史仍 unsupported。
- Kimi Hook 回归验证兼容 Windows CRLF 与 Unix LF checkout；远端 SSH Agent 的 Kimi TOML 规划用例仅在 Linux Agent 对应的 Unix 路径语义下执行，不放宽远端配置根校验。
- Kimi CLI 诊断命令构造保持无冗余可变绑定，后端编译检查无该新增警告。

### 6.5 Grok Build Hook

- 在 `~/.grok/hooks/cli-manager.json` 安装 CLI-Manager 自有 Hook，支持会话启动、运行、注意、停止、失败、子 Agent 和工具生命周期事件。
- 安装时仅关闭 Grok 对 Claude/Cursor Hook 的兼容扫描，保留 skills、rules、MCP 和 agents 等其他兼容项；实际写入的兼容值带当前 SSH Agent installation id 与原始状态 marker，卸载只恢复同一实例仍拥有的值。用户预先关闭的值、其他实例的配置、用户后续手动修改与无关项不会被重新开启或覆盖。
- Grok Hook 通过 `source=grok` 绑定终端会话，终端实时统计读取 Grok 历史与 signals 用量；历史会话可使用 `grok --resume <sessionId>` 恢复，会话 ID 在进入 shell 命令前执行白名单校验。
- 本地与 WSL Grok 历史支持删除整个会话目录（备份 `updates.jsonl` / `summary.json` / `signals.json`）；路径必须落在 Grok history home 内且不能是 home 本身，失败不会声称成功。WSL UNC 用 `wsl.exe find` 发现会话，不回退宿主递归；命中路径必须是 POSIX 的 `<workspace>/<sessionId>/updates.jsonl`（即使宿主是 Windows 也不用本机 `Path` 解析 Linux 路径），`/tmp/updates.jsonl` 这类缺少 workspace/session 两层的路径不会当成会话。历史根优先历史来源，其次 Hook 配置目录下的 `sessions`。SSH Grok 历史仍 unsupported。

### 6.6 Hook 设置

- Claude / Codex / Kimi / Pi / Grok Hook 设置拆分展示
- 支持选择 Claude 配置目录
- 支持选择 Codex 配置目录
- 支持安装 Claude Hook
- 支持卸载 Claude Hook
- 支持安装 Codex Hook
- 支持卸载 Codex Hook
- 支持选择 Kimi Code 配置目录，并按模块或全量安装/卸载 Kimi Hook
- 支持选择 Grok 配置目录，并按模块安装或卸载 Grok Hook
- 展示 Hook 安装状态
- 展示「会话启动 Hook（SessionStart）」安装状态检查项
- 展示脚本路径
- 展示配置文件路径
- Claude Hook 安装时合并写入 `settings.json`，不删除用户自定义 Hook
- 安装 Hook 时自动清理旧版本注册条目
- Codex Hook 安装时写入 `hooks.json`
- Codex Hook 安装时确保 `config.toml` 中 `[features].hooks = true`
- Hook 会自动写入本地 CLI 配置并同步 cc-switch 通用配置；Claude Hook 同步到 `common_config_claude.hooks`，Codex Hook 同步到 `common_config_codex` 的 `[features].hooks` 与 CLI-Manager 拥有的 `[hooks.state.*]`，用于在 cc-switch 切换供应商后保留 Hook。未配置或未检测到 cc-switch 时，本地 Hook 配置文件写入仍作为托底生效。
- Codex 未识别 / 未选择有效配置目录时禁用安装按钮，不自动创建默认 `~/.codex`
- Hook 弹框全局开关
- Hook 弹框自动关闭时间配置

### 6.7 Hook 环境注入

- 为每个 PTY 会话注入 Hook 环境变量（按需）
- `pty_create` 新增 `hook_env_enabled` 参数
- 仅当对应工具 Hook 状态为 `installed` 时才注入 `CLI_MANAGER_NOTIFY_*` 等环境变量
- 空终端和未安装 Hook 的项目按普通 shell 处理

---

## 7. 命令面板

### 7.1 基础能力

- `Ctrl+P` 打开全局命令面板
- 模糊搜索
- 默认条目展示
- 键盘上下选择
- Enter 执行
- Esc 关闭
- 打开后显示键盘操作提示

### 7.2 支持动作

- 新建终端
- 打开历史会话
- 分屏
- 取消分屏
- 切换主题
- 启动项目
- 执行命令模板

### 7.3 精简模式适配

- 精简模式下通过命令面板启动项目时使用外部终端
- 命令面板视觉与精简模式保持一致
- 分组标题与选中态优化

---

## 8. 命令模板

### 8.1 模板作用域

- 全局模板
- 项目级模板
- 会话级模板

### 8.2 模板管理

- 新建模板
- 编辑模板
- 删除模板
- 搜索模板
- 设置页集中管理模板
- 终端侧边栏快捷执行、搜索、按作用域筛选、编辑和删除模板，删除前二次确认
- 命令面板检索执行模板

### 8.3 模板变量

支持变量：

- `${projectPath}`
- `${projectName}`

### 8.4 会话级模板

- 会话级模板仅在当前会话有效
- 会话关闭后自动清理
- 模板检索时按当前项目与当前会话合并上下文

---

## 9. 命令提示与命令历史

### 9.1 候选来源

- 命令模板候选
- 本地命令历史候选
- 内置 AI CLI 命令候选
- 当前目录文件与路径补全
- 可选 AI 二阶段提示
- 终端输入不会新增应用命令历史；历史存储统计和清理仍保留

### 9.2 补全交互

- 在光标后显示内联候选后缀
- 通过 Tab、右方向键或 Ctrl+Space 接受后缀
- 只插入候选，不自动回车执行
- 路径候选按当前 Shell 格式处理

---

## 10. 历史会话工作区

### 10.1 会话来源

- Claude Code 历史会话
- Codex CLI 历史会话
- Kimi Code 历史会话（`$KIMI_CODE_HOME` / `~/.kimi-code`，仅 `agents/main/wire.jsonl`；旧 `~/.kimi` 不读取）
- Grok Build 历史会话（`$GROK_HOME` / `~/.grok/sessions` 的 `updates.jsonl` + `summary.json`；支持删除整棵会话目录）
- Claude 默认读取 `~/.claude/projects`
- Codex 默认读取 `~/.codex/sessions`
- Kimi 默认读取 `~/.kimi-code`
- Grok 默认读取 `~/.grok/sessions`
- 支持自定义 Claude / Codex / Kimi 配置目录与 Grok 会话根
- Hook 自定义目录与历史读取目录联动

### 10.2 会话列表

- 来源筛选下拉列表显示对应 CLI 图标，并使用高不透明度背景保证可读性
- 项目筛选
- 全局搜索
- 时间分组：
  - Today
  - Yesterday
  - This Week
  - This Month
  - Earlier
- 虚拟滚动
- 会话列表使用来源图标、标题、更新时间与消息数的紧凑展示
- 支持来源标题、Native Provider 智能标题和手动别名三级展示优先级；智能标题默认关闭，仅保存在本地 CLI-Manager 数据库，不写回第三方历史文件
- 工具栏与设置页共用智能命名开关；支持按会话手动生成、重新生成和清除，自动命名只处理开启后的新会话，SSH 仅在在线可信详情可用时生成
- 设置 → 会话历史 → 历史会话智能命名支持编辑一份全局、本机自定义 Prompt；非空值作为选中 Native Provider 的系统指令，留空或恢复默认使用内置指令。首条真实用户消息继续独立作为输入发送，不支持模板变量；Prompt 最多 4096 个 UTF-8 字节、禁止 NUL，且保持排除在设置同步之外。保存成功有本地化反馈，长时间 Provider 生成通过异步 IPC 保持界面可交互，并会立即在详情按钮和会话列表显示生成中状态、禁用重复操作。
- 非批量模式下会话卡片整行均可单击或用键盘打开；树展开、删除等操作保持独立
- 加载更多
- 当前终端 Tab 打开历史时默认按当前项目过滤
- 历史 Tab 保持打开时切回不重置筛选状态

### 10.3 会话详情

- 查看会话消息
- 默认“对话”页签只突出用户与助手正文；非正文记录不在该页签显示
- “对话”页签只显示用户实际输入与模型正文；系统/开发者注入、工具调用、工具返回、思考过程和元数据不显示，完整信息继续保留在“原文”页签
- “对话”页每条可见消息复用“原文”页的操作栏：复制始终可用；本地可编辑消息可编辑、删除或在后插入。编辑/插入须先通过既有确认，获准后自动切至“原文”页并打开同一原始消息的表单；SSH 与收藏快照保持只读
- 会话内搜索和过程、变更、工具等视图跳转命中折叠内容时，会自动展开并定位到对应消息
- 原文页签按用户 / AI 方形头像与不同配色聊天气泡展示
- 原文气泡上方显示消息时间与可用的 in / out / cache Token；Codex 独立 `token_count` 事件会回填到最近的 AI 气泡
- 原文长消息默认省略预览，并支持展开查看全文
- 历史会话中完整 `md` / `markdown` 源码围栏会先解包再按 GFM 渲染，表格直接显示为表格；普通代码块、嵌套代码围栏和不完整源码围栏仍按代码展示，代码块容器主题跟随应用明暗主题
- 消息气泡悬停操作栏：复制消息、编辑消息、删除消息、在此后插入消息（写回本地 JSONL，resume 生效；结构化消息仅可复制）
- 消息编辑前自动备份原文件，「编辑记录」弹层以字符级 diff 高亮展示操作差异，支持单条撤回与一键整体还原
- 活跃会话（终端内 CLI 正在使用）编辑前警告确认；文件被外部改动时拒绝写入并自动重载
- 会话内搜索
- 搜索高亮
- 搜索命中上下跳转
- 从搜索结果跳转到具体消息
- 会话别名编辑
- 标签编辑
- 智能标题生成、重新生成与清除
- 收藏 / 取消收藏
- 「打开文件」使用系统文件管理器打开所在文件夹并选中当前历史会话源文件
- 「继续对话」/ 列表右键「恢复会话」新开内置终端执行 `claude --resume` / `codex resume`；项目 CLI 工具与会话来源一致且未配置自定义启动命令时，继承项目 CLI 启动参数（如跳过权限模式）与供应商切换参数
- 删除会话
- 删除前二次确认
- 删除时后端校验历史文件边界
- 删除后清理前端状态、会话元数据与历史缓存

### 10.4 会话统计面板

- 历史会话详情右侧新增「统计」面板
- 查看会话项目、路径、分支
- Token 构成（input / output / cache creation / cache read）
- 估算费用
- 模型信息与上下文使用
- 工具 / 扩展调用明细
- 当天项目会话数
- 历史消息解析支持 input / output / cache creation / cache read Token 字段

### 10.5 Prompt Library

- 历史 Prompt 列表
- 按来源 / 项目 / 会话提取 Prompt
- Prompt 搜索
- Prompt 快速复用入口

---

## 11. Diff 回看

### 11.1 Diff 类型

- Unified Diff
- Codex `*** Begin Patch` 风格 Patch
- 从 Claude / Codex tool-call 中提取 patch 内容
- 支持 `custom_tool_call`
- 支持 `file-history-snapshot`

### 11.2 展示能力

- Diff 弹窗
- 新增行高亮
- 删除行高亮
- hunk header 高亮
- 文件 header 高亮
- 大 Diff 使用 worker 解析
- 代码块独立横向滚动
- Markdown 表格的表头、分隔行和数据行在 Diff 中均保持各自单独的一行；Refractor 语法 token 不受 Tailwind `.table` 工具类的布局影响，统一/分栏与换行/不换行模式一致。
- 从 Diff 块跳回触发消息

---

## 12. 历史用量分析看板

### 12.1 入口与筛选

- 侧栏底部分析看板入口
- 全局挂载弹层
- 项目筛选使用自定义项目/分组列表，并按项目路径过滤历史统计
- 来源筛选
- ccusage 风格时间范围：全部 / 日 / 周 / 月 / 年 / 自定义
- 手动开始日期
- 手动结束日期
- 「周」视图口径为「最近 7 天（含今天）」
- 默认回退全部项目
- 显示最近刷新时间与当前统计范围
- 加载骨架屏
- 刷新按钮强制刷新

### 12.2 指标卡片

- 会话数
- 消息数
- 输入 Token
- 输出 Token
- cache read / cache creation Token
- 费用估算
- 未定价 Token 提示

### 12.3 图表

- 会话 / 消息趋势图
- 输入 / 输出 Token 环形构成图
- 项目活跃排行横向柱图
- 模型构成图
- 活跃热力图
- Token / 费用日趋势
- 来源对比图
- 项目效率散点图
- 小时活跃分布图
- 单日（「日」）统计按 24 小时聚合：横轴 0-23 小时、热力图 24 个小时格子

### 12.4 交互

- 图表 hover 详情
- 项目排行点击过滤项目
- 热力图点击日期下钻
- 单日视图点击小时格子下钻查看该小时会话
- 键盘方向键移动热力图焦点
- Enter / Space 下钻
- 点击会话跳转历史工作区

### 12.5 费用与模型估算

- 模型价格匹配与费用估算逻辑
- 支持 Claude、OpenAI GPT / o 系列常见模型
- 保留显式 cost 字段优先策略
- Codex 历史项目归属优先读取 session metadata 中的 cwd

### 12.6 性能

- 前端查询缓存
- 后端历史索引 generation 聚合缓存
- 打开看板不额外触发历史会话列表加载
- 历史统计扫描从 async runtime 移出，避免阻塞主调度
- 统计查询范围扩展到 366 天
- 历史文件 / 索引缓存 TTL 60 秒

---

## 12A. 终端实时统计面板

### 12A.1 入口与开关

- 终端工具栏「统计」按钮入口
- 「通用设置 - 工具栏」可显隐「统计」按钮（默认开启，配置持久化）
- 设置 -> 侧边栏提供“系统资源cat”开关，仅控制终端侧边栏下方 CPU 小猫显示隐藏。
- 设置 -> 通用 -> 工具栏提供“系统资源监控”开关，用于控制终端侧边栏系统资源按钮显示隐藏。
- 设置 -> 侧边栏可调整资源监控卡片显隐和排序，支持系统、CPU、内存、网络、磁盘、GPU、进程排行。
- 系统资源 CPU 信息分别显示物理核心数与逻辑线程数，CPU 明细按逻辑线程展示使用率。
- 启用实时统计但 Hook 未安装时弹出 toast 引导去 Hook 设置页
- 设置 -> 侧边栏提供“一次只打开一个终端侧边面板”开关，默认开启；该开关位于“合并实时统计与 Git 变更面板”下方，打开历史会话、实时统计、系统资源、Git 变更、Replay 或文件面板时自动收起其它终端侧边面板。
- 合并侧边面板的顶部 Tab 跟随终端工具栏对应入口显隐；当前 Tab 被关闭时自动切换到首个仍可用的 Tab，全部关闭时收起面板，宽度不足时仅显示图标。
- 关闭“一次只打开一个终端侧边面板”后，非合并模式下实时统计、系统资源、Git 变更、Replay 与文件面板可继续并排显示。
- 实时统计、文件、Git 变更、时间轴和系统资源面板使用统一的标题栏高度、图标容器、字号、边框与主题背景；Token 用量卡片的缓存命中项同时显示紧凑 Token 数和命中率。

### 12A.2 会话绑定

- 按当前终端对应的 Claude / Codex / Grok CLI 会话 ID 拉取数据
- 会话级卡片按 sessionId ↔ tabId 严格绑定门控
- 仅当会话 `session_id` 与终端 Hook 回传的 `cliSessionId` 一致时才展示真实数据；未绑定时不回退到项目最近会话
- 彻底解决同项目多终端（多窗口）实时统计互相串显
- 最近会话轮询按 `file_path` / `updated_at` 跳过未变化的 jsonl 重解析
- 切换 Tab / 项目 / CLI 来源时清空旧数据

### 12A.3 卡片内容

- 会话信息卡（项目、路径、分支、来源徽章、消息数、时长、角色分布）
- Token 用量构成
- Token 趋势（Codex 暴露 `token_count` 增量趋势点；同一折线按请求归属模型区分颜色，悬浮提示显示模型名）
- 模型与上下文（同一会话内优先显示最新模型，优先使用日志中的精确 `context_window`）
- 工具与扩展明细（含 Codex MCP 调用识别）；实时统计卡片支持在固定高度区域滚动查看全部内置工具、MCP 与 Skill 调用，长图截图会展开完整明细。
- 今日项目用量（唯一的项目级统计卡片，按主项目及其 Worktree 汇总）
- 最新变更卡片按操作时间展示最近三个真实变更文件及增删统计，并支持用 Git 变更同款 Diff 窗口查看对应 patch
- 实时统计显示项目当前 Git 分支（`get_current_git_branch`）
- 项目与路径带图标，路径支持双击在资源管理器中打开

### 12A.4 空态与骨架

- 未绑定会话时保留卡片图形骨架、数据置空（Donut 灰圈、$0.00、模型与上下文「—」等）
- Hook 绑定会话与后台刷新期间保持卡片结构稳定，不切换为整面板加载态
- 终端空闲时面板头部相对时间每 30s 重算（从「刚刚」走字到「N 分钟前」）

### 12A.5 系统资源监控

- 系统资源面板展示本机 IP 复制、系统/主机、运行时间、CPU 总占用、CPU 核心占用、GPU 负载监控（可用时，含当前、趋势、平均、峰值和阈值状态）、内存可用/已用/缓存/空闲、网络上/下载速率与今日累计流量、合并磁盘用量和读写速度、高占用进程排行。
- 资源面板复用终端侧边面板主题变量；深色下避免蓝色大面积强调，CPU 趋势保留更长历史并使用更深面积阴影，CPU 核心占用默认折叠，展开后按宽度自适应排布，核心项使用多段竖条，占用越高从绿色段递进到黄色段和红色段。
- 网络趋势以上行在中线以上、下载在中线以下的双向折线展示，并在右侧显示当前上行/下载速率和当天累计流量；磁盘卡片合并所有磁盘容量，不显示具体盘符，使用环形图、容量条、用量明细和读写速率小块展示当前状态。
- 进程排行固定展示前 5 行，列顺序为 CPU%、内存%、软件、PID；软件列内显示图标，软件名识别失败时显示命令，Windows 尽量读取 exe 版本信息和小图标，macOS/Linux 使用通用图标兜底。
- 面板关闭时不执行全量采样，但同次应用运行内保留最近 48 个采样点，重新打开后继续追加；仅在“系统资源cat”开启时，固定栏底部 CPU 小猫做轻量 CPU 采样（面板打开期间也持续采样不冻结），奔跑频率随 CPU 占用线性加快。
- GPU 查询为 best-effort，系统不支持或查询失败时只显示不可用，不影响面板其它数据。

### 12A.6 AI Replay

- 当前会话级 AI Replay 侧栏，默认绑定当前聚焦内部终端的当前 Replay 会话
- 会话标题优先使用首条用户 prompt 的截断文本，不再直接展示 `Codex CLI running/done` 这类技术状态文案
- 默认“进展”视图按用户 Prompt 聚合对话轮次，最新轮次置顶并在初始加载时展开；每个轮次可独立展开或收起，支持多个轮次同时展开及全部收起，轮次内部按实际执行顺序展示
- 顶部使用紧凑状态区展示 AI 当前动作、运行状态和对话/工具/文件/验证数量，不再使用会话大卡片和独立统计卡片占用首屏
- 对话轮次显示用户需求与 AI 最终回复摘要，可在原位置展开完整文本；历史 transcript 尚未落盘时退化为实时 Hook 记录，不会借用同项目其他会话
- 工具调用按 `toolUseId`、子任务按 `agentId` 精确合并开始/结束事件，并区分工具、MCP、Skill、验证、文件变更、子任务、权限、通知和异常
- 文件变更按轮次聚合显示文件路径与增删行数，可打开完整 Diff；事件输入、结果及对话详情均在原位置按需展开
- 次级“详细日志”保留完整 Hook 事件，支持搜索、类型筛选和原始 payload 内联查看
- 历史 Replay 会话继续通过头部历史入口切换查看
- 代码快照详情支持查看 Diff、回滚到该快照、基于该快照 Fork 新分支
- 代码快照 Diff 文件化存储到 `.cli-manager/replay-snapshots/`，SQLite 仅保存文件引用和快照元数据；旧版本已写入 DB 的内联快照会在每个应用版本首次启动时自动检查、迁移并清理，降低长期 Replay 使用后的 DB 体积

---

## 12B. cc-switch 供应商集成

### 12B.1 供应商页

- 设置新增「供应商」页
- 只读解析 cc-switch 数据库（默认 `~/.cc-switch/cc-switch.db`，可手动选择 .db 文件）
- 按 app_type 分类展示供应商
- 密钥在 Rust 侧脱敏，明文 token 不进入 WebView
- 主从布局：左侧供应商列表，右侧详情面板（BASE_URL、模型、备注、环境变量）

### 12B.2 项目级供应商切换

- claude 项目右键菜单「切换供应商」
- Claude 将 cc-switch 通用配置与所选供应商配置深度合并（供应商优先），把完整 JSON 生成到 CLI-Manager 独立 settings 文件，内部终端启动时追加 `--settings`
- Claude 内部终端启动前会按当前 cc-switch 数据库刷新完整 settings 文件
- Codex 合并通用配置与供应商配置；存在原始 TOML 时完整保留非密钥配置生成独立 profile，内部终端启动时追加 `--profile`
- Codex 供应商密钥不写入 profile，仍仅通过终端环境变量注入
- Grok Build 项目和 Worktree 的「切换供应商」入口直接提示暂不支持，不展示候选供应商、不写入项目覆盖，也不改动全局配置
- WSL/Bash 终端启动时会把 CLI-Manager 配置文件路径转换为 `/mnt/<drive>/...`
- 项目级覆盖绑定项目 ID；同路径、同 Shell 的不同项目可以分别选择不同供应商
- 临时文件 + rename 替换，损坏配置时拒绝写入
- 「跟随全局供应商」：删除当前项目的供应商覆盖，后续启动不再追加项目级配置文件参数
- 显示当前生效状态与全局当前供应商名
- 探测到 `settings.local.json` 配置 `ANTHROPIC_*` 时显示优先级警告
- 项目树为有项目级覆盖的 claude 项目显示独立供应商徽标
- 停用或删除供应商仅扫描项目及活跃 Worktree 的有效 schema-v2 `provider_overrides`；外部导入追踪、旧版 CCS 与损坏覆盖不算原生实际引用，真实引用会显示明确的阻止原因；三点菜单的删除、上移/下移失败均给出可处理反馈

---

## 13. 版本化项目与设置备份

### 13.1 备份配置

- WebDAV URL
- 用户名
- 密码（Windows 存凭据管理器、macOS 存登录钥匙串、Linux/原生 WSL 存 freedesktop Secret Service 专用 collection，重启自动恢复，不落明文配置文件）
- 自定义远程目录（默认 `cli-manager`，支持多级路径如 `backups/cli-mgr`）
- 远程目录字符串层安全校验（`sanitize_remote_dir`：剔除 `..` / `.` 段，统一分隔符，空值回退默认）
- 连接测试
- WebDAV 版本备份 / 本地 ZIP 备份模式切换
- 备份状态与上次备份时间展示
- 退出时仅在内容变化后自动备份

### 13.2 备份范围

- 工作区配置：分组、完整项目字段与时间戳、Active Worktree、持久命令模板
- 跨设备应用偏好，所有设置字段通过穷尽策略明确纳入或排除
- 模型价格
- 三方 Hook 通知设置（全局开关、目标、事件筛选及完整平台凭证）
- CLI-Manager 内部状态栏当前配置与多配置库
- 排除终端标签、分屏、Workspan、滚屏、设备设置、背景图、桌宠资源、会话历史及外部 Claude/Codex/cc-switch 数据

### 13.3 版本快照

- WebDAV 快照不可变并保留历史版本
- 默认设备名来自系统计算机名
- 设备名清洗
- 云端快照列表、预览、下载与删除
- 远端路径使用 `<remoteDir>/backups/<UTC>--<deviceName>--<deviceId>--<snapshotId>.json`
- 每设备自动保留最近 10 份
- V3 manifest 记录设备、应用版本、平台、时间与内容哈希
- 兼容显式导入 V1/V2 设备快照、旧 `sync.json` 和旧 ZIP

### 13.4 备份 / 恢复

- 手动备份始终创建新快照
- 自动备份按规范化内容哈希跳过未变化内容
- 恢复前展示五个数据域摘要，可选择需要完全还原的数据域
- 未选择的数据域保持不变
- 恢复前创建本地安全快照，失败自动回滚，并支持一次撤销
- 工作区与模型价格通过 SQLite 事务替换
- 项目与 Worktree 路径按原值恢复，由现有健康检查标记缺失路径
- 状态栏恢复只写 CLI-Manager 内部文件，不修改外部 CLI 安装配置
- 后端自动递归创建所有父目录
- WebDAV 通过 `PROPFIND Depth: 1` 枚举快照并通过 `DELETE` 删除指定快照

### 13.5 自动备份与 Outbox

- 应用关闭时先写 `.cli-manager/backups/outbox/<targetHash>/<snapshotId>.json`
- 在既有 8 秒退出预算内尝试上传，失败或超时不阻塞退出
- 下次启动仅异步重试 Outbox，不再自动下载或处理冲突
- 旧 `autoSyncOnClose=upload` 自动迁移为退出自动备份，其余旧自动同步动作关闭

### 13.6 本地 ZIP 与明文策略

- 本地文件名为 `cli-manager-backup-YYYYMMDD-HHmmss-<snapshotId>.zip`
- ZIP 内包含 `snapshot.json`，旧 ZIP 的 `sync.json` 仍可导入
- 快照不做应用层加密；项目环境变量、命令模板、命令提示 API Key、三方 Hook Webhook/Token/Secret 会以明文保存
- WebDAV 密码仍只保存在系统凭据库，不进入快照
- 中英文界面均明确提示使用 HTTPS WebDAV 并妥善保管 ZIP

---

## 14. 设置系统

### 14.1 设置入口

- 全屏设置弹层
- Portal 挂载，避免被侧栏裁剪
- 设置页顶部栏
- 设置页侧边导航
- 设置页侧边导航根据窗口可用高度自动启用纵向滚动，确保小屏幕下全部菜单均可访问

### 14.2 设置页分类

- 通用设置
- 终端设置
- 快捷键设置
- 命令模板设置
- 同步设置
- 供应商设置
- Hook 设置
- 关于

### 14.3 通用设置

- 应用主题
- 配色方案
- UI 字体
- 应用字体大小（调整除内置终端外的整体界面字号）
- 文本颜色（自定义应用字体颜色，支持对比度校验）
- 精简模式
- 侧栏密度
- 侧栏宽度
- 树节点徽标 / 状态显示
- 关闭按钮行为：
  - 询问
  - 最小化到托盘
  - 直接退出
- 禁用硬件加速（默认关闭；开启后降低 GPU 占用，WebView2 部分需重启生效）
- 调试模式（调试日志与 F12 DevTools）
- 终端工具栏显示
- 「统计」按钮显隐开关（默认开启）

### 14.4 终端设置

- 默认 Shell
- 外部 PowerShell 配置
- 终端字体支持内置与系统字体搜索选择；带空格、中文、逗号或其他标点的字体名均按完整字体族匹配，已选系统字体不会误显示为自定义项。
- 终端字号：支持 `Ctrl + 滚轮` 持久化缩放；缩放后显示继承终端主题色的当前字号、缩小、放大与恢复控件，并在最后一次操作 2 秒后自动隐藏。
- 终端回滚行数（默认 9000 行；开启自定义后支持 1000-50000 行调整与热更新）
- 终端主题
- 终端主题独立设置，支持浅色 / 深色分组筛选和跟随系统主题色配置
- Hook 下方独立「命令提示」设置页：终端输入提示开关
- 终端实时预览（宽屏右侧吸顶固定展开；窄屏进入终端主题库并位于颜色板前）
- Shell runtime monitoring（默认关闭以提升启动速度）
- Unsplit 行为
- 终端背景图设置

### 14.5 终端背景图设置

- 开关
- 图片选择
- 缩略图预览
- 图片缺失提示
- 不透明度
- 适配模式
- 9 宫格位置
- 高斯模糊
- 暗化覆盖
- 清理未使用背景图

### 14.6 快捷键设置

- 快捷键录制
- 快捷键清空
- 快捷键冲突检测
- 空绑定不参与冲突检测
- 恢复默认快捷键
- 会话历史快捷键可配置
- 终端换行快捷键可配置；Grok Build / Codex 匹配后写入 `\x1b\r`，未匹配的原生 `Alt+Enter` 放行给 xterm，其余未匹配组合键继续拦截
- Tab 切换修饰键可配置
- Claude/Codex 文件粘贴快捷键可配置，默认 `Alt+V`，支持改为 `Ctrl+V`

---

## 15. 快捷键

默认快捷键：

| 快捷键 | 功能 |
|---|---|
| `Ctrl+P` | 打开命令面板 |
| `Ctrl+K` | 打开会话历史 |
| `Ctrl+Shift+T` | 新建终端 |
| `Ctrl+W` | 关闭当前终端 |
| `Alt+ArrowRight` | 下一个 Tab |
| `Alt+ArrowLeft` | 上一个 Tab |
| `F11` | 终端全屏 |
| `Alt+V` | Claude/Codex 终端粘贴文件 |
| `Ctrl+F` | 终端搜索 / 历史会话内搜索 |

补充能力：

- 快捷键可自定义
- 快捷键可清空
- 未设置快捷键时显示“未设置快捷键”
- 历史工具栏提示文案跟随当前快捷键配置更新

---

## 16. 主题与视觉系统

### 16.1 应用视觉

- 新安装及缺少对应设置键时，默认使用浅色 `Apple Mono` 应用配色；已有持久化主题选择不被覆盖。
- Tailwind CSS 4 token 化主题
- Surface Layering
- No-Line Rule
- Primary 渐变 CTA
- Glass 浮层
- 统一状态层级
- lucide-react 图标体系
- 全局 Toaster 通知

### 16.2 终端主题

内置终端主题包括但不限于：

- Tokyo Night Dark
- Tokyo Night Light
- Dracula
- Monokai
- Nord
- Solarized Dark
- Solarized Light
- One Dark
- GitHub Dark
- GitHub Light
- Catppuccin Mocha
- Catppuccin Macchiato
- Catppuccin Latte
- Gruvbox Dark
- Gruvbox Light

能力：

- 新安装及缺少对应设置键时，终端默认使用 `Windows Terminal Campbell`，终端侧边面板默认使用“经典终端”皮肤；已有持久化选择不被覆盖。
- 终端主题独立于应用主题
- 手动选择终端主题
- 主题搜索 / 分类展示
- 主题切换时只更新颜色，不重建终端实例

---

## 17. 应用生命周期与窗口行为

- 主窗口启动时居中显示
- 启动首帧使用默认深色背景，减少白屏闪烁
- 设置加载完成前延迟渲染侧栏与终端布局
- 应用启动后延后重试备份 Outbox 与执行版本检查
- 关闭按钮行为可配置
- 支持最小化到托盘
- 托盘菜单支持显示窗口
- 托盘菜单支持退出
- 退出前向前端发送 `tray-quit-requested`
- 确认退出后显示全屏进度遮罩（正在备份数据 / 正在关闭终端会话），清理完成才关窗，全程有反馈
- 关闭期自动备份限时 8 秒；快照先写 Outbox，网络超时或失败后继续退出并在下次启动重试
- 低内存模式下，后台终端隐藏超过 10 秒会释放 WebGL/GPU 渲染资源，切回时仅重建渲染层
- 启动时按用户设置为 WebView2 注入禁用 GPU 参数，用于降低硬件加速带来的 GPU 占用
- 支持终端全屏模式
- 精简模式切换时调整窗口宽度

---

## 18. 日志、版本与更新

### 18.1 日志

- Rust 后端日志
- 日志文件存储在用户目录 `.cli-manager/logs`
- 前端日志桥接
- 本地时区日志
- 调试日志运行时开关
- 防止 console 日志递归桥接

### 18.2 版本

- 获取应用版本号
- 获取产品名称
- 设置页关于区块展示版本

### 18.3 更新检测

- 启动后静默检查 Cloudflare R2 Release，R2 不可用时回退 GitHub Release
- 发现新版本时展示提示
- 展示 Release Notes
- 下载更新时通过系统浏览器打开更新页面
- Tauri 更新清单和安装包保留 HTTPS、签名校验与 GitHub 备用源

---

## 19. 数据层

### 19.1 SQLite 表

- 数据库文件存储在用户目录 `.cli-manager/cli-manager.db`
- `projects`
- `groups`
- `command_templates`
- `command_history`（保留兼容；终端输入不再新增，命令提示仍可读取并管理已有记录）
- `session_meta`
- `sync_meta`

### 19.2 Store 持久化

- Store 文件存储在用户目录 `.cli-manager`
- 用户设置
- 同步配置
- 终端会话元数据
- 会话恢复相关状态

### 19.3 本地历史来源

- Claude：`~/.claude/projects`
- Codex：`~/.codex/sessions`

### 19.4 本地资源

- 终端背景图片存储在 `$APPLOCALDATA/backgrounds`
- 背景图片按 SHA-256 内容寻址命名

---

## 20. 安全与边界处理

### 20.1 历史会话安全

- 删除历史会话前校验来源
- 删除历史会话前校验项目边界
- 删除历史会话前 canonicalize 路径
- 只允许删除受支持来源下的 `.jsonl` 文件
- 切换 Claude / Codex 配置目录后隔离历史索引与缓存

### 20.2 Hook Bridge 安全

- Hook 服务仅监听本地回环地址
- Hook 请求必须携带 bearer token
- Hook body 有大小限制
- Hook header 有大小限制
- 只接受指定事件类型

### 20.3 背景图片安全

- 图片来源必须是绝对路径
- 只允许 `jpg` / `jpeg` / `png` / `gif`
- 禁止 `webp`
- 保存后的相对路径禁止：
  - 空路径
  - `..`
  - 反斜杠
  - 绝对路径
  - 逃逸 `backgrounds/` 目录
- asset protocol scope 锁定到背景图目录

### 20.4 WebDAV 安全与稳定性

- 使用 Basic Auth
- 密码通过 keyring 存 Windows 凭据管理器、macOS 登录钥匙串或 Linux freedesktop Secret Service；原生 WSL 使用专用 collection 规避 default collection 缺失，不进 tauri store / SQLite / 同步快照
- 项目环境变量、命令模板、命令提示 API Key 与三方 Hook 通知凭证会按产品策略明文进入 WebDAV V3 快照与本地 ZIP 的 `snapshot.json`，不做应用层加密；HTTP 无传输加密，HTTPS 仅保护传输链路
- WebDAV 响应大小限制
- 请求超时限制
- 上传前确保远端目录存在
- 远程下载/删除仅接受备份目录下严格解析的快照文件名；旧 V1/V2 数据只允许显式导入

### 20.5 PTY 稳定性

- PTY 输出避免切断 UTF-8 字符
- PTY 输出避免切断 ANSI / OSC / CSI 转义序列
- 未完成残尾延迟到下一轮拼接
- 异常源端有缓冲兜底，避免无限增长
- resize 有最小列数 / 行数保护

---

## 21. 当前 README Roadmap

README 中当前列出的后续方向：

### 21.1 Claude 配置同步增强

- 同步 Claude Code 配置
- 纳入 Claude 相关配置与扩展能力同步
- 为多工具协作预留接口

> 说明：原 Roadmap 中的「CC-Switch 集成」已在 V1.0.3 落地，详见第 12B 节。

---

## 22. 功能覆盖总览

```text
项目管理
  → 项目树 / 分组 / 搜索 / 拖拽 / 健康检查 / 折叠状态持久化
  → 打开所在目录 / cc-switch 供应商切换

终端工作区
  → PTY / xterm / Tab / Tab 复制 / 状态点 / 搜索 / 性能优化
  → 低内存模式 / 后台终端 WebGL 延迟释放 / 终端回滚行数可配置 / 热更新 / OSC 8 超链接系统浏览器打开
  → 中文输入法修复（候选框锚点 / 符号双输入 / 流式重绘漂移）
  → 修复 Windows/WSL 中 Pi CLI 全屏 TUI 的 MCP 直连工具提示覆盖底部输入框问题
  → 已知限制：Windows Pi 候选上屏后的第二次连续输入仍可能右漂并只显示一个字符（#177，未解决）

分屏系统
  → pane tree / Split Right / Split Down / 拖拽比例 / Tab 跨 pane

AI CLI 支持
  → Claude Code Hook / Codex Hook / 审批提醒 / 完成提醒 / 失败提醒
  → OSC 133 shell integration 标准化状态检测 / SessionStart 会话绑定
  → Hook 环境按需注入 / 30 分钟超时回退 / curl.exe 优先 + -NoProfile

命令复用
  → 命令面板 / 命令模板 / 内联输入提示

历史回看
  → Claude / Codex 会话 / 搜索 / 标签 / 收藏 / 删除 / Prompt Library
  → 会话统计面板（Token 构成、费用估算、模型上下文、工具调用、当天项目会话数）

Diff 回看
  → Unified Diff / Codex Patch / 行级高亮 / 跳回触发消息

统计分析
  → 历史用量分析看板（ccusage 风格：日 / 周 / 月 / 年 / 自定义范围）
  → 会话 / 消息 / Token / cache token / 费用估算
  → 项目 / 模型 / 来源 / 热力图（单日 24 小时）/ 效率散点
  → 终端实时统计面板（会话绑定 / Token 趋势 / Git 分支 / 工具调用）

cc-switch 集成
  → 只读解析供应商数据库 / 脱敏展示 / 按 app_type 分类
  → 项目级供应商切换（Claude --settings / Codex --profile）
  → 跟随全局供应商 / 供应商徽标

备份能力
  → WebDAV 版本快照 / 五域恢复 / Outbox 重试 / 安全快照与撤销 / 本地 ZIP 导入导出

个性化
  → 主题 / 字体 / 字体大小 / 字体颜色 / 快捷键 / 终端背景 / 关闭行为 / 精简模式
```

---

## 23. 简短结论

CLI-Manager 当前的基础能力已经比较完整，主要价值集中在三件事：

1. **把多项目终端集中起来**：项目树、内置终端、分屏、外部终端。
2. **把 Claude / Codex 使用过程管理起来**：Hook 状态、通知、历史会话、Diff、Prompt。
3. **把长期使用数据沉淀下来**：命令模板、统计看板、版本化 WebDAV 备份。

后续最自然的增量方向不是继续堆普通终端功能，而是围绕 README Roadmap 做：

- Claude 配置同步增强
- 项目启动 Profile / Run Profile
- Web 管理：修复桌面端内嵌 Web 服务打包后静态资源路径错误，确保安装包可正常加载页面。
- Web 设备连接设置：编辑配置时不再被后台状态刷新覆盖。
- Web 终端：关闭按钮通过独立 close 命令同步结束桌面端对应终端会话，detach 仍用于临时离开和重连。

- Web 终端：支持当前终端滚动到底部；设备删除使用自定义确认弹窗；桌面终端提前订阅 PTY 输出。
