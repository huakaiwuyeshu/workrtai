# AI 架构治理验收（TEMP，2026-09-07）

## 交付结果

- PR #252 已修复冲突并通过 GitHub CLI 合并，合并提交 `78cd43ed`。
- 原有 25 个超 2000 行文件已按职责拆分；当前全部手写代码通过 2000 物理行、单行 500 字符检查，临时基线清空。
- 前端采用 app/features/shared，跨功能使用独立 api 模块或既有 index/state 入口。全局偏好、翻译、UI、平台适配及数据契约可分别定位。
- Rust 实现归入 features/infrastructure/shared；lib.rs、commands/mod.rs 的显式模块路径保留原命名空间、可见性和 helper/daemon 接口。
- 规范已写入 AGENTS.md 与 Trellis 前后端契约；检查独立于 dev/build，不新增依赖或常驻索引进程。

## PR #252 发现并修复的问题

| 问题 | 修复 |
| --- | --- |
| Agent 协议与当前主线 SFTP 能力冲突 | Agent 0.1.14 使用 protocol 1.15，同时保留下载/删除能力，并分别协商 Git 历史与增强工具 |
| 用量数据库健康判断可能漏掉索引、视图列或过期迁移标记 | 检查完整必需结构，幂等修复索引及 migration marker |
| Git 整理提交的恢复引用可能因秒级命名重复而覆盖 | 纳秒时间戳加原始提交前缀，使用仅创建引用语义 |
| 增强工具弹窗与引用树的交互/安全边界不足 | 应用级确认/输入框、迟到结果隔离、串行写入和无嵌套交互元素 |
| 主线终端布局与 Git Dock 的冲突 | 保留分屏、Workspan、背景和既有文件能力，同时接入 Git 工作区 |

## 自动化验证

| 检查 | 结果 |
| --- | --- |
| 严格架构检查 | 946 个手写源文件，零超长文件、零超长行、零规则违规 |
| TypeScript / 生产前端构建 | 通过；构建 6964 个模块 |
| Node 静态与内存回归 | 657 项通过 |
| CLI 启动桩检查（不启动 Tauri） | 20 项通过 |
| 桌面 Rust 编译 / 库测试 | 通过；1237 项通过，1 项原有忽略 |
| SSH Agent | 97 + 3 项通过 |
| history-core / hook-schema / agent-capabilities-core | 9 / 16 / 10 项通过 |
| 迁移等价性 | 511 个前端模块、2106 条导入关系、243 个 Rust 文件及独立样式比对通过 |

另有逐批提取审计，覆盖翻译键值、CSS 顺序、Hook 顺序、完整 JSX、Store 动作、计时器与串行队列。源码契约测试改读实际模块；仅清理了引用 2026-07-16 已删除函数的过期光标测试，现有鼠标交互测试保留。

## 日常 AI 开发入口

```text
npm run check:architecture -- --strict
npm run report:architecture
```

先读相关功能入口和契约，再定位符号及依赖；不要默认加载全量翻译、CSS 或大文件。常规模块目标 400–1200 行，2000 行是硬上限。报告中的 Token 数是字节/4 的规模估算，不是计费量或实测节省比例。

Rust 检查可解析显式 crate 路径和分组导入；相对 super、局部别名和宏生成依赖仍需编译与审查。自动化通过不等于已验证所有设备和交互场景。

## 人工桌面 / 实机验收（尚未执行）

- [ ] 本地、WSL、SSH 的新建终端、输入、退出、重连和会话恢复。
- [ ] 分屏、Workspan 拖拽、隐藏标签、背景、中文 IME、图片及文件拖入。
- [ ] 历史检索、恢复、Diff、统计筛选与下钻。
- [ ] Git Dock、固定 Diff、切换项目，以及测试仓库中的危险操作确认/取消。
- [ ] 文件编辑、未保存确认、Markdown 跨文件/锚点与 Ctrl+右键。
- [ ] zh-CN/en-US 切换、24 小时时间格式、快捷键和焦点行为。

本次未启动桌面应用、真实远程服务或应用级 e2e；以上检查保留给人工。重构提交仅保存在本地分支，未推送。

## 环境维护限制

重构后已尝试 `npx gitnexus analyze`，但 `.gitnexus/lbug` 返回拒绝访问，索引仍停留在旧提交。只读独占访问及文件 ACL 查询同样受限；未修改权限、删除索引或终止其他进程。需人工检查该文件的权限/占用并重新索引。此次验收使用完整源码/导入关系比对与编译测试补足旧索引覆盖，不应把旧索引当作迁移后路径的权威来源。
