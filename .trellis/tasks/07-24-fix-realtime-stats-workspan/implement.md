# Implementation Plan

1. Add transport event ID, bounded daemon dedupe, and retry tests.
2. Add pure hook target resolver and tests; integrate it into `handleCliHookEvent`.
3. Replace mouse-only pane activation with pointer/focus activation.
4. Update the Hook contract, CHANGELOG `[TEMP]`, and feature inventory notes.
5. Run focused tests, TypeScript check, Rust check/test, GitNexus change detection, and diff validation.
