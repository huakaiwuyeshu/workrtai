# Implementation Plan

1. Extend `ConfirmDialog` with an opt-in explicit-close-only mode.
2. Enable the mode for the startup session restore prompt in `App.tsx`.
3. Run frontend type-check and inspect the diff for unrelated changes.
4. Record the fix in `CHANGELOG.md` (TEMP version) and `docs/功能清单.md`.
