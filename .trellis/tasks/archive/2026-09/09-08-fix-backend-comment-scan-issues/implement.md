# 实施计划

1. 收紧 `valid_pet_id`，要求修剪后的值是单个普通路径组件，同时保留现有长度和字符白名单。
2. 更新卸载命令注释，并为 `.`、`..`、空白变体及合法含点 ID 增加回归断言。
3. 在 Tauri 文件安全清单中固化“路径片段 ID 必须是单个普通组件”的规则。
4. 使用 `TEMP` 更新变更日志和桌面宠物功能清单。
5. 运行定向 Rust 测试、格式检查、编译检查、严格架构检查及 GitNexus 变更检测（不可用时记录降级结果）。
