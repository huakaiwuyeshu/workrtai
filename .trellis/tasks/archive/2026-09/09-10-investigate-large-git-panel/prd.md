# Issue #257：大量 Git 变更导致面板卡死

## 目标
定位 64,887 个文件变更时打开 Git 面板卡住的原因，提交有代码证据和规模验证的修复方案，并完成已获用户同意的修复。

## 背景与约束
- Issue：https://github.com/dark-hxx/CLI-Manager/issues/257；反馈版本 v1.3.9，用户补充规模为 64,887 个变更文件。
- 用户已同意任务规划及最终方案实施；代码变更记录归入 V1.4.0。
- master 与本地 origin/master 记录同步（0/0，未 fetch）。保留已有 Pi 终端及文档等未提交改动。
- 主会话直接排查及后续实现，不分派子代理。
- 尚无用户仓库、性能 trace 或 crash.log；代码确定的扩展性问题与用户现场耗时结论必须区分。

## 要求
- R1：追踪面板打开、状态采集、Transport/IPC、建树、渲染、刷新链路，提交根因陈述和发现清单。证据归档见 research/findings.md 和 research/scale-results.json；主触点为 GitChangesTree.tsx:26、GitTreeNode.tsx:63/272/420、gitStore.ts:261/306/331（均相对 src/features/git 对应目录）。
- R2：保持完整变更数据，不能通过截断或仅默认折叠隐藏性能问题。
- R3：保持目录/模块分组、折叠、已跟踪/未跟踪分区、选择和暂存语义，以及 Diff、源文件、右键和拖拽行为。
- R4：覆盖本地、WSL、SSH、Worktree、项目切换、面板隐藏和焦点；刷新不能积压或让旧结果覆盖新状态。
- R5：后续新增文案兼容中英文，更新 CHANGELOG.md 的 V1.4.0 和 docs/功能清单.md 的 Git 功能板块。

## 验收
- [x] R1：报告有代码位置、规模实验结果、确认与待验证结论。
- [x] R2：64,887 个变更下挂载行数由视口限制，滚动能访问首尾文件，不截断业务数据。
- [x] R3：小仓库和大仓库上述交互通过定向测试。
- [x] R4：刷新合并和过期结果隔离通过延迟响应测试，环境路由不变。
- [x] R5：类型检查、相关 Git 回归、独立严格架构检查通过，浏览器验证有结果或明确限制。

## 范围外
不调整提交历史图、Replay 快照协议、Git 写操作语义、数据库结构或发布版本配置；不操作用户工作树，不提交或回复 issue。
