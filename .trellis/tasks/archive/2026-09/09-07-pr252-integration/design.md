# Integration design

Retain both branches' contracts: master SFTP/terminal layout and contributor Git history/tools.
Agent release 0.1.14 advertises protocol 1.15; capability checks precede frame writes.
Schema readiness validates required objects and final view columns; rewrite recovery refs
are create-only. Dialogs reuse app controls and reject stale-context operations/results.
