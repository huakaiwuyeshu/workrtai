# 修复 Codex 供应商模型编辑与配置预览，简化全局应用提示

## Goal

修复 Codex 模型映射输入失焦、模型预览与全局实际写入不一致，并隐藏全局应用指纹。变更记录使用 V1.4.0。

## Requirements

- R1：CLI 模型支持连续输入、粘贴和中途编辑；新增/删除映射行不串值。已定位 NativeProviderAdvancedConfigSection.tsx:122 的 key 包含输入值。
- R2：模型 abc/sdf 保存后，生效配置和字段来源与实际应用一致。兼容模型表单、供应商 TOML、公共配置继承以及旧 CCS 导入数据。
- R3：隐藏预览快照、当前指纹、目标指纹。保留目标文件、路径、受管字段和新建/更新/无变化状态；内部指纹校验保持。

## Acceptance Criteria

- [x] 连续输入 abc/sdf 不失焦；编辑和删除多行映射不串值。
- [x] 保存重开后模型预览、字段来源与写入目标一致，斜杠完整保留。
- [x] 覆盖公共配置冲突、旧导入双重模型字段；未知 TOML 字段保留。
- [x] 中英文全局应用均不展示三类指纹，快照校验保持。
- [x] 定向测试、类型检查、必要 Rust 检查及严格架构检查通过。
- [x] 更新 CHANGELOG.md 和 docs/功能清单.md，版本 V1.4.0。

人工验收依据：2026-09-11 用户反馈“可以提交，验证通过”。

## Out of Scope

不修改数据库、IPC 协议或版本清单；不主动应用供应商或改写用户 CLI 文件。其他 CLI 仅验证共享组件兼容。
