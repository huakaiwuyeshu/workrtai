# Editor responsibility extraction

Move existing editor declarations into a files feature controller, preserving their order. Replace only the contiguous Markdown navigation callback/effect block with a custom hook at the identical position. Pass the existing refs/state setters into it; do not create another request identity or store. Move the complete final JSX into a typed view. Extract model types and the pure theme helper. Keep Monaco setup once in the controller module and preserve the existing lazy component facade.

Compare all original statements after expanding the one hook boundary and compare complete JSX. The 300-line regression must cover the real controller, view, navigation, types and helper, not just the facade. No UI launch; desktop gestures/languages remain human checks.
