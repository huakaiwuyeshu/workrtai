# Design: 确认缺陷根因修复

## Approach

每项先用最小隔离测试锁定当前错误，再修改产生错误状态的源头。遍历错误改为完整遍历并累计命中；异步恢复确保 future 被执行且 journal 状态机允许补偿；字符串操作改用字符边界安全逻辑；文件/命令操作在破坏性步骤前验证前置条件。

## Compatibility

不改变公共命令签名和持久化格式。错误路径可以从静默错误变为显式失败，但需保持调用方可处理的现有错误类型。

## Rollback

按问题分批提交测试与实现；每批可独立撤销。
